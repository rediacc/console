import styled from 'styled-components'
import {
  PageWrapper as BasePageWrapper,
  SectionStack as BaseSectionStack,
  SectionHeading as BaseSectionHeading,
  ListTitleRow as BaseListTitleRow,
  ListTitle as BaseListTitle,
  ListSubtitle as BaseListSubtitle,
  InlineFormRow as BaseInlineFormRow,
} from '@/pages/system/styles'

export const TeamsPageWrapper = styled(BasePageWrapper)``

export const TeamsSectionStack = styled(BaseSectionStack)``

export const TeamsSectionHeading = styled(BaseSectionHeading)``

export const TeamsListHeader = styled(BaseListTitleRow)``

export const TeamsListTitle = styled(BaseListTitle)``

export const TeamsListSubtitle = styled(BaseListSubtitle)``

export const TeamsInlineFormRow = styled(BaseInlineFormRow)``

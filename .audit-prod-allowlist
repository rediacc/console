# Production dependency vulnerability allowlist
# Only for upstream issues with no available fix.
# Each line: advisory source ID (from npm audit --json)
# Review periodically and remove when upstream publishes a fix.

# fast-xml-parser <=5.5.5 (used by @astrojs/rss build tool, not runtime-exposed)
# @astrojs/rss pins exact version 5.4.1, no override possible
# Tracking: https://github.com/nicbarker/fast-xml-parser/issues
1114772
# fast-xml-parser entity expansion bypass (same root cause, extended range <=5.5.6)
1114952
# fast-xml-parser numeric entity expansion bypass (incomplete fix for CVE-2026-26278, <=5.5.5)
1115134
# flatted prototype pollution via parse() (devDep via @vitest/ui, not runtime-exposed)
1114934
# h3 SSE event injection via unsanitized carriage return (transitive dep, no fix available)
1115114
# h3 double decoding path traversal bypass in serveStatic (transitive dep, no fix available)
1115117
# flatted prototype pollution via parse() (devDep via @vitest/ui, supersedes 1114934)
1115357
# fast-xml-parser entity expansion limits bypassed when set to zero (transitive dep, no fix)
1115359
# yaml stack overflow via deeply nested collections (transitive dep, no fix available)
1115369
# picomatch ReDoS via extglob quantifiers (transitive dep, no fix available)
1115382
1115384
# smol-toml DoS via thousands of consecutive commented lines (transitive dep, no fix)
1115393
# picomatch method injection in POSIX character classes (transitive dep, no fix available)
1115394
1115396
# picomatch method injection + ReDoS (superseded advisories, same root cause, no fix)
1115490
1115492
1115493
1115495
1115549
1115551
1115552
1115554
# path-to-regexp backtracking regex (transitive dep via @ant-design/pro-layout, no fix in v2.x)
1101848
# path-to-regexp ReDoS via sequential optional groups/wildcards (transitive dep, no fix)
1115573
1115582
# xmldom CDATA injection (transitive dep via electron-builder/plist and marp/mathjax, no fix)
1115764
1115765
# lodash / lodash-es Code Injection via _.template imports key names (transitive prod dep, no fix)
1115805
1115806
# lodash / lodash-es Prototype Pollution via array path bypass in _.unset/_.omit (no fix)
1115809
1115810
# vite path traversal in optimized deps .map handling (dev tool, not runtime-exposed)
1116005
1116006
1116123
1116124
# vite server.fs.deny bypass with queries (dev tool, not runtime-exposed)
1116008
1116126
# vite arbitrary file read via dev server WebSocket (dev tool, not runtime-exposed)
1116010
1116011
1116099
1116100
# axios NO_PROXY hostname normalization bypass leading to SSRF (transitive dep, no fix available)
1116365
# axios cloud metadata exfiltration via header injection chain (transitive dep, no fix available)
1116425
1116473
# follow-redirects leaks Custom Authentication Headers to Cross-Domain Redirect Targets
# (GHSA-r4q5-vmmm-2653, <=1.15.11; 1.15.11 is latest, no upstream fix yet; transitive
# via axios in packages/cli/, not runtime-exposed for CLI-only auth flows)
1116560

# hono Improperly Handles JSX Attribute Names Allows HTML Injection in hono/jsx SSR
# (GHSA-458j-xx4x-4375, <4.12.14; transitive via @modelcontextprotocol/sdk's
# server-side hono router, not used for SSR in our build path. Upgrade is
# blocked by @hono/node-server pinning hono ^4 — npm refuses to override the
# pinned 4.12.12 transitive without breaking the lockfile in CI; revisit when
# @modelcontextprotocol/sdk bumps its hono dep)
1116669

# path-to-regexp ReDoS via sequential optional groups / multiple wildcards
# (GHSA-j3q9-mxjg-w52f and GHSA-27v5-c462-wpq7, 8.0-8.3; @ant-design/pro-layout
# 7.22.7 pins path-to-regexp to exactly 8.2.0 and a workspace-side npm override
# does not propagate (verified). Build-time router compile only — bundled
# into the React app where backtracking pathological input is operator-controlled.
# Revisit when pro-layout bumps the pin.)
1115573
1115582
# Transitive cousin (path-to-regexp 2.0-3.2 chain via the same pro-layout pin)
1101848

# protobufjs arbitrary code execution via malicious decode input
# (GHSA-xq3m-2v4x-88gg and sibling advisory for protobufjs 8.0.0).
# 1116756 / 1116832: protobufjs <7.5.5 — transitive via @grpc/proto-loader
#   0.8.0 (pulled in by @opentelemetry/exporter-logs-otlp-grpc) which pins
#   protobufjs ^7.5.3 and is not yet bumped to 7.5.5 upstream.
# 1116757 / 1116833: protobufjs 8.0.0 — transitive via
#   @opentelemetry/otlp-transformer 0.211.x (pulled in by
#   @opentelemetry/sdk-node 0.211.x, which is deliberately pinned in
#   .syncpackrc.json at ^0.211.0 — the 0.215.0 OTel bump that would take
#   us to protobufjs 8.0.1 is gated by the pin).
# Both advisories are not reachable at runtime: the OTLP exporters only
# serialize our own telemetry to our own collector endpoint, there is no
# untrusted protobuf decode path. Revisit either when @grpc/proto-loader
# ships 7.5.5 (removes 1116756 / 1116832) or when the syncpack pin is
# lifted to 0.215.x and the tree moves to protobufjs 8.0.1 (removes
# 1116757 / 1116833).
1116756
1116757
1116832
1116833

name = "edge-rediacc-account-us"
main = "src/index.ts"
compatibility_date = "2026-01-20"
compatibility_flags = ["nodejs_compat"]
upload_source_maps = true

routes = [
  { pattern = "edge-us.rediacc.com", custom_domain = true }
]

[vars]
REGION = "us"
ENVIRONMENT = "edge"
WEBAUTHN_RP_ID = "edge-us.rediacc.com"
WEBAUTHN_ORIGIN = "https://edge-us.rediacc.com"
# Auto-grant lifetime BUSINESS to every new signup. Flip to "false" (or remove
# this line) and redeploy to close intake. See src/types/env.ts for details.
DESIGN_PARTNER_PROGRAM_ENABLED = "false"

[observability]
enabled = true

[observability.logs]
enabled = true
invocation_logs = false

[observability.traces]
enabled = true
head_sampling_rate = 1

[assets]
directory = "./dist"
binding = "ASSETS"
not_found_handling = "404-page"
run_worker_first = ["/*"]

[[d1_databases]]
binding = "DB"
database_name = "edge-account-db-us"
database_id = "cf0c909c-08d0-4125-9029-77dcce9e9a7a"
migrations_dir = "../../private/account/drizzle"

# US: location hint ENAM (no US jurisdiction available in R2, data is zero-knowledge encrypted)
[[r2_buckets]]
binding = "CONFIG_BUCKET"
bucket_name = "edge-rediacc-configs-us"

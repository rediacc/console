name: Console CT - Tests

# Which suites run in which job is driven by the playwright.*.config.ts files.
# Suites deliberately left OUT of CI (concurrent multi-cluster, cluster rehearse,
# image-build, destructive ops, the two ruled csi-sanity deviations, the examples
# program, and the not-yet-enabled CLI migrate-routing suite) are documented, with
# reasons, in packages/e2e-tests/README.md ("Deliberately not in CI").

on:
  workflow_call:
    # Every TEST job here builds a nolicense renet from source on-runner
    # (.ci/scripts/infra/build-renet.sh); console is public, so a prebuilt
    # nolicense binary must never ship via artifacts. Only the embed-assets
    # staging is shared, via the renet-embed-assets cache (same key as
    # ci-build-renet.yml).
    inputs:
      # The expensive suites run on PR and nightly only. This used to gate the
      # CALLER (ci.yml's `tests:` job), which is why migration-test could not
      # live here: on push-to-main the whole workflow was skipped, and D1
      # migrations must be validated on exactly that path. Gating each job
      # instead lets migration-test run on push while the E2E legs still do not.
      full_suite:
        type: string
        required: false
        default: 'true'
      pointer_bump_only:
        type: string
        required: false
        default: 'false'
    secrets:
      APP_PRIVATE_KEY:
        required: false
      DOCKERHUB_USERNAME:
        required: false
      DOCKERHUB_TOKEN:
        required: false
      ACCOUNT_ED25519_PRIVATE_KEY:
        required: false
      ACCOUNT_ED25519_PUBLIC_KEY:
        required: false
      ACCOUNT_SERVER_API_KEY:
        required: false
      ACCOUNT_JWT_SECRET:
        required: false
      STRIPE_SANDBOX_SECRET_KEY:
        required: false
      STRIPE_SANDBOX_WEBHOOK_SECRET:
        required: false
      CLOUDFLARE_API_TOKEN:
        required: false

permissions:
  contents: read
  packages: read    # GHCR pull for test images

jobs:
  # ============================================================================
  # COLUMN 4: TESTS + INFRA
  # Jobs: migration-test, test-unit, test-e2e-workers, test-e2e-ceph,
  #       test-e2e-ceph-workers, test-e2e-k8s, test-e2e-k8s-ceph,
  #       test-e2e-k8s-multinode, test-e2e-migrate, test-fork-isolation,
  #       test-renet, test-account-e2e
  # ============================================================================

  # Validates that D1 migrations apply cleanly against a production data clone.
  # Creates an ephemeral D1 database, clones production, runs migrations, then
  # tears the clone down.
  #
  # NOT gated on full_suite, unlike every other job here. This one used to be a
  # top-level ci.yml job needing only `initialize`, and it ran on push-to-main --
  # the exact path that deploys these migrations. Moving it under `full_suite`
  # would have stopped validating migrations on the release path, which is the
  # one place the check has to hold.
  migration-test:
    name: Migration Test
    runs-on: ubuntu-latest
    timeout-minutes: 10
    if: inputs.pointer_bump_only != 'true'
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          sparse-checkout: .github/actions

      - uses: ./.github/actions/app-token
        id: app-token
        with:
          client-id: ${{ vars.APP_ID }}
          private-key: ${{ secrets.APP_PRIVATE_KEY }}
          preset: readonly
          repositories: console,account

      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          token: ${{ steps.app-token.outputs.token }}

      - name: Init account submodule
        run: git submodule update --init --depth 1 private/account

      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020  # v7.0.0
        with:
          node-version: '22'

      - name: Install wrangler
        run: |
          source .ci/config/constants.sh
          npm install -g "wrangler@${WRANGLER_VERSION}"

      - name: Reap stale migration-test D1 databases
        run: .ci/scripts/housekeeping/cleanup-stale-d1.sh --max-age 60
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ vars.CLOUDFLARE_ACCOUNT_ID }}

      - name: Test migrations against all regional clones (edge first, then stable)
        run: .ci/scripts/deploy/test-d1-migrations.sh
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ vars.CLOUDFLARE_ACCOUNT_ID }}

  test-unit:
    name: Unit
    if: inputs.full_suite == 'true'
    runs-on: ubuntu-slim
    timeout-minutes: 14
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: ./.github/actions/setup-workspace
        with:
          build-packages: 'true'
      - run: .ci/scripts/test/run-unit.sh --coverage

  test-e2e-workers:
    name: E2E Workers (${{ matrix.os-image }})
    if: inputs.full_suite == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 90
    strategy:
      fail-fast: false
      matrix:
        os-image:
          - ubuntu-24.04
          - debian-13
          - fedora-43
          - opensuse-16.0
          - oracle-10
          # NOTE: rocky-10 intentionally excluded. RHEL 10's stock kernel
          # ships without the btrfs module and Rediacc's storage backend
          # requires btrfs; Oracle Linux 10 works because UEK retains btrfs.
          # Full explanation and operator guidance:
          #   packages/www/src/content/docs/en/requirements.md (Why UEK?)
          #   packages/www/src/content/docs/en/limits.md (Why not Rocky 10?)
          #   packages/www/src/content/docs/en/troubleshooting.md (btrfs module missing)
        include:
          # FULL_INTEGRATION legs: 12a/12b/12d (composition suites) + 13b (live
          # CRIU fork). Composition logic is distro-agnostic, so two legs are the
          # honest floor — one apt-family (ubuntu) + one rpm-family (fedora).
          # CRIU_EXPECTED=1 turns 13b's "CRIU absent" guard into a hard failure.
          #
          # CLI_SUITE (suite 23, the rdc-driven migrate-routing e2e) is NOT enabled
          # here yet: it needs a local two-worker validation of its exact argv +
          # machine-registration/SSH wiring first (plan phase 5). To turn it on,
          # add `cli-suite: '1'` to the ubuntu-24.04 leg below. Rationale +
          # local-run instructions in packages/e2e-tests/README.md.
          # cli-suite is declared EMPTY rather than omitted. An omitted matrix
          # property still resolves to "" in `${{ matrix.cli-suite }}`, so the
          # behaviour is identical -- but actionlint rightly flags a reference
          # to a property the matrix never declares, and silencing that check
          # would cost far more than it saves. Flipping the suite on is still a
          # one-character edit: change '' to '1' on the ubuntu leg.
          - os-image: ubuntu-24.04
            full-integration: '1'
            cli-suite: ''
          - os-image: fedora-43
            full-integration: '1'
            cli-suite: ''
    env:
      CI: 'true'
      BRIDGE_TIMEOUT: 240000
      VM_BRIDGE: '1'
      VM_WORKERS: '11 12'
      VM_CEPH_NODES: ''
      VM_IMAGE: ${{ matrix.os-image }}
      FULL_INTEGRATION: ${{ matrix.full-integration }}
      CRIU_EXPECTED: ${{ matrix.full-integration }}
      CLI_SUITE: ${{ matrix.cli-suite }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: ./.github/actions/app-token
        id: app-token
        with:
          client-id: ${{ vars.APP_ID }}
          private-key: ${{ secrets.APP_PRIVATE_KEY }}
          repositories: console,renet,account,elite,homebrew-tap
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          submodules: true
          token: ${{ steps.app-token.outputs.token }}
      - name: Login to GHCR
        uses: docker/login-action@abd2ef45e78c5afb21d64d4ca52ee8550d9572c7 # v4.5.1
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ github.token }}
      - name: Login to Docker Hub
        uses: docker/login-action@abd2ef45e78c5afb21d64d4ca52ee8550d9572c7 # v4.5.1
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      - name: Propagate Docker Hub credentials to root
        run: sudo mkdir -p /root/.docker && sudo cp ~/.docker/config.json /root/.docker/config.json
      - uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e # v7.0.0
        with:
          go-version: 'stable'
          cache: true
          cache-dependency-path: private/renet/go.sum
      - name: Free Disk Space
        run: |
          echo "=== Disk usage before cleanup ==="
          df -h / || true
          sudo rm -rf /usr/share/dotnet /usr/local/lib/android /opt/ghc /usr/local/share/powershell /usr/share/swift 2>/dev/null || true
          docker system prune -af 2>/dev/null || true
          echo "=== Disk usage after cleanup ==="
          df -h / || true
      - uses: ./.github/actions/setup-workspace
        with:
          account: 'true'
          build-packages: 'true'
      - run: .ci/scripts/build/build-cli.sh
      - name: Install CLI Globally
        run: .ci/scripts/setup/install-cli-global.sh

      - name: Install clang for BPF compilation
        run: sudo apt-get update -qq && sudo apt-get install -y -qq clang llvm libbpf-dev linux-headers-generic
      - name: Pre-pull Docker base images (avoid buildx auth issues)
        run: .ci/scripts/infra/docker-prepull.sh debian:bookworm=linux/amd64 debian:bookworm-slim=linux/arm64 ubuntu:24.04=linux/amd64 ubuntu:24.04=linux/arm64
      # Staged embed-assets cache, shared with Build (Renet) via the same key
      # (see ci-build-renet.yml). On a hit, build.sh's content-based receipt
      # makes embed_assets a verified no-op, skipping the ~10-25 min Docker
      # builder staging. The nolicense go build itself stays on-runner:
      # console is public, so dev binaries must never be uploaded as artifacts.
      - uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        id: embed-cache
        with:
          path: private/renet/pkg/embed/assets
          key: renet-embed-assets-${{ hashFiles('private/renet/embed-assets.lock.json') }}
      - name: Build renet
        run: .ci/scripts/infra/build-renet.sh --nolicense
      - uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        if: always() && steps.embed-cache.outputs.cache-hit != 'true'
        with:
          path: private/renet/pkg/embed/assets
          key: renet-embed-assets-${{ hashFiles('private/renet/embed-assets.lock.json') }}
      - name: Install renet
        run: sudo cp "$RENET_BINARY" /usr/bin/renet && sudo chmod +x /usr/bin/renet

      - name: Free Disk Space
        run: |
          echo "=== Disk usage before cleanup ==="
          df -h / || true
          sudo rm -rf /usr/share/dotnet /usr/local/lib/android /opt/ghc /usr/local/share/powershell /usr/share/swift 2>/dev/null || true
          docker system prune -af 2>/dev/null || true
          echo "=== Disk usage after cleanup ==="
          df -h / || true

      # VM base-image cache, one entry per matrix distro. The disks dir is
      # cached whole because every leg also downloads the Ubuntu default image
      # (prepareDriver runs EnsureBaseImage unconditionally) in addition to the
      # VM_IMAGE distro. Monthly key: several of these URLs are moving
      # pointers. Restored before any sudo step touches $RUNNER_TEMP/renet so
      # extraction happens as the runner user.
      - name: Compute VM image cache key
        id: vm-image-key
        run: echo "key=vm-base-image-e2e-${{ matrix.os-image }}-$(date -u +%Y-%m)" >> "$GITHUB_OUTPUT"
      - name: Restore VM base image cache
        id: vm-image-cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ${{ runner.temp }}/renet/disks
          key: ${{ steps.vm-image-key.outputs.key }}

      - name: Setup KVM Host
        run: sudo "$RENET_BINARY" ops host setup

      - name: Create E2E Test Environment
        run: .ci/scripts/env/create-e2e-env.sh --renet-path /usr/bin/renet --output packages/e2e-tests/.env

      - name: Run E2E Tests (Workers)
        run: .ci/scripts/test/run-e2e.sh --workers 1 --fail-on-skip

      - if: always()
        run: .ci/scripts/signal/create-complete.sh --name "e2e-workers-${{ matrix.os-image }}" --status ${{ job.status }}

      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a  # v7.0.1
        if: always()
        with:
          name: test-e2e-workers-${{ matrix.os-image }}-${{ github.sha }}
          path: |
            ${{ runner.temp }}/complete.txt
            packages/e2e-tests/reports/bridge/
            packages/e2e-tests/reports/bridge-logs/
          retention-days: 3
          if-no-files-found: ignore

      # ops up runs unprivileged here (the harness spawns renet directly), so
      # the chown is normally a no-op — kept for parity with the other cached
      # jobs and as a guard should the harness ever run under sudo.
      - name: Fix VM image ownership for cache save
        if: always()
        run: sudo chown -R "$(whoami)" "$RUNNER_TEMP/renet/disks" 2>/dev/null || true

      - name: Save VM base image cache
        if: always() && steps.vm-image-cache.outputs.cache-hit != 'true'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ${{ runner.temp }}/renet/disks
          key: ${{ steps.vm-image-key.outputs.key }}

  test-e2e-ceph:
    name: E2E Ceph
    if: inputs.full_suite == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 45
    env:
      CI: 'true'
      BRIDGE_TIMEOUT: 240000
      VM_BRIDGE: '1'
      VM_WORKERS: ''
      VM_CEPH_NODES: '21 22 23'
      CEPH_MODE: 'true'
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: ./.github/actions/app-token
        id: app-token
        with:
          client-id: ${{ vars.APP_ID }}
          private-key: ${{ secrets.APP_PRIVATE_KEY }}
          repositories: console,renet,account,elite,homebrew-tap
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          submodules: true
          token: ${{ steps.app-token.outputs.token }}
      - name: Login to GHCR
        uses: docker/login-action@abd2ef45e78c5afb21d64d4ca52ee8550d9572c7 # v4.5.1
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ github.token }}
      - name: Login to Docker Hub
        uses: docker/login-action@abd2ef45e78c5afb21d64d4ca52ee8550d9572c7 # v4.5.1
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      - name: Propagate Docker Hub credentials to root
        run: sudo mkdir -p /root/.docker && sudo cp ~/.docker/config.json /root/.docker/config.json
      - uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e # v7.0.0
        with:
          go-version: 'stable'
          cache: true
          cache-dependency-path: private/renet/go.sum
      - name: Free Disk Space
        run: |
          echo "=== Disk usage before cleanup ==="
          df -h / || true
          sudo rm -rf /usr/share/dotnet /usr/local/lib/android /opt/ghc /usr/local/share/powershell /usr/share/swift 2>/dev/null || true
          docker system prune -af 2>/dev/null || true
          echo "=== Disk usage after cleanup ==="
          df -h / || true
      - uses: ./.github/actions/setup-workspace
        with:
          account: 'true'
          build-packages: 'true'
      - run: .ci/scripts/build/build-cli.sh
      - name: Install CLI Globally
        run: .ci/scripts/setup/install-cli-global.sh

      - name: Install clang for BPF compilation
        run: sudo apt-get update -qq && sudo apt-get install -y -qq clang llvm libbpf-dev linux-headers-generic
      - name: Pre-pull Docker base images (avoid buildx auth issues)
        run: .ci/scripts/infra/docker-prepull.sh debian:bookworm=linux/amd64 debian:bookworm-slim=linux/arm64 ubuntu:24.04=linux/amd64 ubuntu:24.04=linux/arm64
      # Staged embed-assets cache, shared with Build (Renet) via the same key
      # (see ci-build-renet.yml). On a hit, build.sh's content-based receipt
      # makes embed_assets a verified no-op, skipping the ~10-25 min Docker
      # builder staging. The nolicense go build itself stays on-runner:
      # console is public, so dev binaries must never be uploaded as artifacts.
      - uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        id: embed-cache
        with:
          path: private/renet/pkg/embed/assets
          key: renet-embed-assets-${{ hashFiles('private/renet/embed-assets.lock.json') }}
      - name: Build renet
        run: .ci/scripts/infra/build-renet.sh --nolicense
      - uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        if: always() && steps.embed-cache.outputs.cache-hit != 'true'
        with:
          path: private/renet/pkg/embed/assets
          key: renet-embed-assets-${{ hashFiles('private/renet/embed-assets.lock.json') }}
      - name: Install renet
        run: sudo cp "$RENET_BINARY" /usr/bin/renet && sudo chmod +x /usr/bin/renet

      # Same VM base-image cache as the E2E Workers ubuntu leg: this job
      # sets no VM_IMAGE, so it downloads only the Ubuntu default image and
      # can share that cache entry (same key, same disks-dir path).
      - name: Compute VM image cache key
        id: vm-image-key
        run: echo "key=vm-base-image-e2e-ubuntu-24.04-$(date -u +%Y-%m)" >> "$GITHUB_OUTPUT"
      - name: Restore VM base image cache
        id: vm-image-cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ${{ runner.temp }}/renet/disks
          key: ${{ steps.vm-image-key.outputs.key }}

      - name: Setup KVM Host
        run: sudo "$RENET_BINARY" ops host setup

      - name: Create E2E Test Environment
        run: .ci/scripts/env/create-e2e-env.sh --ceph --renet-path /usr/bin/renet --output packages/e2e-tests/.env

      - name: Run E2E Tests (Ceph)
        run: .ci/scripts/test/run-e2e.sh --workers 1 --config playwright.ceph.config.ts --fail-on-skip

      - if: always()
        run: .ci/scripts/signal/create-complete.sh --name "e2e-ceph" --status ${{ job.status }}

      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a  # v7.0.1
        if: always()
        with:
          name: test-e2e-ceph-${{ github.sha }}
          path: |
            ${{ runner.temp }}/complete.txt
            packages/e2e-tests/reports/bridge-ceph/
            packages/e2e-tests/reports/bridge-logs/
          retention-days: 3
          if-no-files-found: ignore

      - name: Fix VM image ownership for cache save
        if: always()
        run: sudo chown -R "$(whoami)" "$RUNNER_TEMP/renet/disks" 2>/dev/null || true

      - name: Save VM base image cache
        if: always() && steps.vm-image-cache.outputs.cache-hit != 'true'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ${{ runner.temp }}/renet/disks
          key: ${{ steps.vm-image-key.outputs.key }}

  # Combined workers + Ceph topology: a worker consuming a Ceph RBD datastore
  # (suite 13) and multi-client read-only RBD + per-client COW (suite 14). Fits
  # a 16 GB runner via per-role RAM (bridge 1024 + 2x2560 worker + 3x2560 ceph =
  # 13.5 GB) and a lowered osd_memory_target (~1.6 GB, PBKDF2/FastKDF since
  # renet #78 removed the old 4 GB LUKS-unlock floor).
  test-e2e-ceph-workers:
    name: E2E Ceph Workers
    if: inputs.full_suite == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 90
    env:
      CI: 'true'
      BRIDGE_TIMEOUT: 240000
      VM_BRIDGE: '1'
      VM_WORKERS: '11 12'
      VM_CEPH_NODES: '21 22 23'
      CEPH_MODE: 'true'
      VM_RAM_WORKER: '2560'
      VM_RAM_CEPH: '2560'
      CEPH_OSD_MEMORY_TARGET: '1717986918'
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: ./.github/actions/app-token
        id: app-token
        with:
          client-id: ${{ vars.APP_ID }}
          private-key: ${{ secrets.APP_PRIVATE_KEY }}
          repositories: console,renet,account,elite,homebrew-tap
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          submodules: true
          token: ${{ steps.app-token.outputs.token }}
      - name: Login to GHCR
        uses: docker/login-action@abd2ef45e78c5afb21d64d4ca52ee8550d9572c7 # v4.5.1
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ github.token }}
      - name: Login to Docker Hub
        uses: docker/login-action@abd2ef45e78c5afb21d64d4ca52ee8550d9572c7 # v4.5.1
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      - name: Propagate Docker Hub credentials to root
        run: sudo mkdir -p /root/.docker && sudo cp ~/.docker/config.json /root/.docker/config.json
      - uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e # v7.0.0
        with:
          go-version: 'stable'
          cache: true
          cache-dependency-path: private/renet/go.sum
      - name: Free Disk Space
        run: |
          echo "=== Disk usage before cleanup ==="
          df -h / || true
          sudo rm -rf /usr/share/dotnet /usr/local/lib/android /opt/ghc /usr/local/share/powershell /usr/share/swift 2>/dev/null || true
          docker system prune -af 2>/dev/null || true
          echo "=== Disk usage after cleanup ==="
          df -h / || true
      - uses: ./.github/actions/setup-workspace
        with:
          account: 'true'
          build-packages: 'true'
      - run: .ci/scripts/build/build-cli.sh
      - name: Install CLI Globally
        run: .ci/scripts/setup/install-cli-global.sh

      - name: Install clang for BPF compilation
        run: sudo apt-get update -qq && sudo apt-get install -y -qq clang llvm libbpf-dev linux-headers-generic
      - name: Pre-pull Docker base images (avoid buildx auth issues)
        run: .ci/scripts/infra/docker-prepull.sh debian:bookworm=linux/amd64 debian:bookworm-slim=linux/arm64 ubuntu:24.04=linux/amd64 ubuntu:24.04=linux/arm64
      # Staged embed-assets cache, shared with Build (Renet) via the same key
      # (see ci-build-renet.yml). On a hit, build.sh's content-based receipt
      # makes embed_assets a verified no-op, skipping the ~10-25 min Docker
      # builder staging. The nolicense go build itself stays on-runner:
      # console is public, so dev binaries must never be uploaded as artifacts.
      - uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        id: embed-cache
        with:
          path: private/renet/pkg/embed/assets
          key: renet-embed-assets-${{ hashFiles('private/renet/embed-assets.lock.json') }}
      - name: Build renet
        run: .ci/scripts/infra/build-renet.sh --nolicense
      - uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        if: always() && steps.embed-cache.outputs.cache-hit != 'true'
        with:
          path: private/renet/pkg/embed/assets
          key: renet-embed-assets-${{ hashFiles('private/renet/embed-assets.lock.json') }}
      - name: Install renet
        run: sudo cp "$RENET_BINARY" /usr/bin/renet && sudo chmod +x /usr/bin/renet

      # Same VM base-image cache as the E2E Workers ubuntu leg: this job sets
      # no VM_IMAGE, so it downloads only the Ubuntu default image and can share
      # that cache entry (same key, same disks-dir path).
      - name: Compute VM image cache key
        id: vm-image-key
        run: echo "key=vm-base-image-e2e-ubuntu-24.04-$(date -u +%Y-%m)" >> "$GITHUB_OUTPUT"
      - name: Restore VM base image cache
        id: vm-image-cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ${{ runner.temp }}/renet/disks
          key: ${{ steps.vm-image-key.outputs.key }}

      - name: Setup KVM Host
        run: sudo "$RENET_BINARY" ops host setup

      # Explicit --vm-workers AND --vm-ceph-nodes (NOT --ceph, which zeroes
      # workers). Per-role RAM + osd_memory_target are passed through so the
      # combined topology fits the runner.
      - name: Create E2E Test Environment
        run: |
          .ci/scripts/env/create-e2e-env.sh \
            --renet-path /usr/bin/renet \
            --output packages/e2e-tests/.env \
            --vm-workers "11 12" \
            --vm-ceph-nodes "21 22 23" \
            --vm-ram-worker "$VM_RAM_WORKER" \
            --vm-ram-ceph "$VM_RAM_CEPH" \
            --ceph-osd-memory-target "$CEPH_OSD_MEMORY_TARGET"

      - name: Run E2E Tests (Ceph Workers)
        run: .ci/scripts/test/run-e2e.sh --workers 1 --config playwright.ceph-workers.config.ts --fail-on-skip

      - if: always()
        run: .ci/scripts/signal/create-complete.sh --name "e2e-ceph-workers" --status ${{ job.status }}

      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a  # v7.0.1
        if: always()
        with:
          name: test-e2e-ceph-workers-${{ github.sha }}
          path: |
            ${{ runner.temp }}/complete.txt
            packages/e2e-tests/reports/bridge-ceph-workers/
            packages/e2e-tests/reports/bridge-ceph-workers-logs/
          retention-days: 3
          if-no-files-found: ignore

      - name: Fix VM image ownership for cache save
        if: always()
        run: sudo chown -R "$(whoami)" "$RUNNER_TEMP/renet/disks" 2>/dev/null || true

      - name: Save VM base image cache
        if: always() && steps.vm-image-cache.outputs.cache-hit != 'true'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ${{ runner.temp }}/renet/disks
          key: ${{ steps.vm-image-key.outputs.key }}

  test-e2e-k8s:
    name: E2E K8s
    if: inputs.full_suite == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 60
    env:
      CI: 'true'
      BRIDGE_TIMEOUT: 240000
      VM_BRIDGE: '1'
      VM_WORKERS: '11'
      VM_CEPH_NODES: ''
      K8S_MODE: '1'
      VM_RAM_WORKER: '4096'
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: ./.github/actions/app-token
        id: app-token
        with:
          client-id: ${{ vars.APP_ID }}
          private-key: ${{ secrets.APP_PRIVATE_KEY }}
          repositories: console,renet,account,elite,homebrew-tap
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          submodules: true
          token: ${{ steps.app-token.outputs.token }}
      - name: Login to GHCR
        uses: docker/login-action@abd2ef45e78c5afb21d64d4ca52ee8550d9572c7 # v4.5.1
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ github.token }}
      - name: Login to Docker Hub
        uses: docker/login-action@abd2ef45e78c5afb21d64d4ca52ee8550d9572c7 # v4.5.1
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      - name: Propagate Docker Hub credentials to root
        run: sudo mkdir -p /root/.docker && sudo cp ~/.docker/config.json /root/.docker/config.json
      - uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e # v7.0.0
        with:
          go-version: 'stable'
          cache: true
          cache-dependency-path: private/renet/go.sum
      - name: Free Disk Space
        run: |
          echo "=== Disk usage before cleanup ==="
          df -h / || true
          sudo rm -rf /usr/share/dotnet /usr/local/lib/android /opt/ghc /usr/local/share/powershell /usr/share/swift 2>/dev/null || true
          docker system prune -af 2>/dev/null || true
          echo "=== Disk usage after cleanup ==="
          df -h / || true
      - uses: ./.github/actions/setup-workspace
        with:
          account: 'true'
          build-packages: 'true'
      - run: .ci/scripts/build/build-cli.sh
      - name: Install CLI Globally
        run: .ci/scripts/setup/install-cli-global.sh

      - name: Install clang for BPF compilation
        run: sudo apt-get update -qq && sudo apt-get install -y -qq clang llvm libbpf-dev linux-headers-generic
      - name: Pre-pull Docker base images (avoid buildx auth issues)
        run: .ci/scripts/infra/docker-prepull.sh debian:bookworm=linux/amd64 debian:bookworm-slim=linux/arm64 ubuntu:24.04=linux/amd64 ubuntu:24.04=linux/arm64
      # Staged embed-assets cache, shared with Build (Renet) via the same key
      # (see ci-build-renet.yml). On a hit, build.sh's content-based receipt
      # makes embed_assets a verified no-op, skipping the ~10-25 min Docker
      # builder staging. The nolicense go build itself stays on-runner:
      # console is public, so dev binaries must never be uploaded as artifacts.
      - uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        id: embed-cache
        with:
          path: private/renet/pkg/embed/assets
          key: renet-embed-assets-${{ hashFiles('private/renet/embed-assets.lock.json') }}
      - name: Build renet
        run: .ci/scripts/infra/build-renet.sh --nolicense
      - uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        if: always() && steps.embed-cache.outputs.cache-hit != 'true'
        with:
          path: private/renet/pkg/embed/assets
          key: renet-embed-assets-${{ hashFiles('private/renet/embed-assets.lock.json') }}
      - name: Install renet
        run: sudo cp "$RENET_BINARY" /usr/bin/renet && sudo chmod +x /usr/bin/renet

      # Same Ubuntu VM base-image cache as the E2E Workers ubuntu leg (no
      # VM_IMAGE set -> Ubuntu default image, same key + disks-dir path).
      - name: Compute VM image cache key
        id: vm-image-key
        run: echo "key=vm-base-image-e2e-ubuntu-24.04-$(date -u +%Y-%m)" >> "$GITHUB_OUTPUT"
      - name: Restore VM base image cache
        id: vm-image-cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ${{ runner.temp }}/renet/disks
          key: ${{ steps.vm-image-key.outputs.key }}

      - name: Setup KVM Host
        run: sudo "$RENET_BINARY" ops host setup

      # Single worker, no Ceph: a k8s repo runs a self-contained k3s inside a
      # datastore-backed image. VM_RAM_WORKER=4096 gives k3s + a workload pod room.
      - name: Create E2E Test Environment
        run: |
          .ci/scripts/env/create-e2e-env.sh \
            --renet-path /usr/bin/renet \
            --output packages/e2e-tests/.env \
            --vm-workers "11" \
            --vm-ram-worker "$VM_RAM_WORKER"

      - name: Run E2E Tests (K8s)
        run: .ci/scripts/test/run-e2e.sh --workers 1 --config playwright.k8s.config.ts --fail-on-skip

      - if: always()
        run: .ci/scripts/signal/create-complete.sh --name "e2e-k8s" --status ${{ job.status }}

      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a  # v7.0.1
        if: always()
        with:
          name: test-e2e-k8s-${{ github.sha }}
          path: |
            ${{ runner.temp }}/complete.txt
            packages/e2e-tests/reports/bridge-k8s/
            packages/e2e-tests/reports/bridge-k8s-logs/
          retention-days: 3
          if-no-files-found: ignore

      - name: Fix VM image ownership for cache save
        if: always()
        run: sudo chown -R "$(whoami)" "$RUNNER_TEMP/renet/disks" 2>/dev/null || true

      - name: Save VM base image cache
        if: always() && steps.vm-image-cache.outputs.cache-hit != 'true'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ${{ runner.temp }}/renet/disks
          key: ${{ steps.vm-image-key.outputs.key }}

  test-e2e-k8s-ceph:
    name: E2E K8s Ceph
    if: inputs.full_suite == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 90
    env:
      CI: 'true'
      BRIDGE_TIMEOUT: 240000
      VM_BRIDGE: '1'
      VM_WORKERS: '11 12'
      VM_CEPH_NODES: '21 22 23'
      K8S_MODE: '1'
      VM_RAM_WORKER: '3072'
      VM_RAM_CEPH: '2560'
      CEPH_OSD_MEMORY_TARGET: '1717986918'
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: ./.github/actions/app-token
        id: app-token
        with:
          client-id: ${{ vars.APP_ID }}
          private-key: ${{ secrets.APP_PRIVATE_KEY }}
          repositories: console,renet,account,elite,homebrew-tap
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          submodules: true
          token: ${{ steps.app-token.outputs.token }}
      - name: Login to GHCR
        uses: docker/login-action@abd2ef45e78c5afb21d64d4ca52ee8550d9572c7 # v4.5.1
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ github.token }}
      - name: Login to Docker Hub
        uses: docker/login-action@abd2ef45e78c5afb21d64d4ca52ee8550d9572c7 # v4.5.1
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      - name: Propagate Docker Hub credentials to root
        run: sudo mkdir -p /root/.docker && sudo cp ~/.docker/config.json /root/.docker/config.json
      - uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e # v7.0.0
        with:
          go-version: 'stable'
          cache: true
          cache-dependency-path: private/renet/go.sum
      - name: Free Disk Space
        run: |
          echo "=== Disk usage before cleanup ==="
          df -h / || true
          sudo rm -rf /usr/share/dotnet /usr/local/lib/android /opt/ghc /usr/local/share/powershell /usr/share/swift 2>/dev/null || true
          docker system prune -af 2>/dev/null || true
          echo "=== Disk usage after cleanup ==="
          df -h / || true
      - uses: ./.github/actions/setup-workspace
        with:
          account: 'true'
          build-packages: 'true'
      - run: .ci/scripts/build/build-cli.sh
      - name: Install CLI Globally
        run: .ci/scripts/setup/install-cli-global.sh

      - name: Install clang for BPF compilation
        run: sudo apt-get update -qq && sudo apt-get install -y -qq clang llvm libbpf-dev linux-headers-generic
      - name: Pre-pull Docker base images (avoid buildx auth issues)
        run: .ci/scripts/infra/docker-prepull.sh debian:bookworm=linux/amd64 debian:bookworm-slim=linux/arm64 ubuntu:24.04=linux/amd64 ubuntu:24.04=linux/arm64
      # Staged embed-assets cache, shared with Build (Renet) via the same key
      # (see ci-build-renet.yml). On a hit, build.sh's content-based receipt
      # makes embed_assets a verified no-op, skipping the ~10-25 min Docker
      # builder staging. The nolicense go build itself stays on-runner:
      # console is public, so dev binaries must never be uploaded as artifacts.
      - uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        id: embed-cache
        with:
          path: private/renet/pkg/embed/assets
          key: renet-embed-assets-${{ hashFiles('private/renet/embed-assets.lock.json') }}
      - name: Build renet
        run: .ci/scripts/infra/build-renet.sh --nolicense
      - uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        if: always() && steps.embed-cache.outputs.cache-hit != 'true'
        with:
          path: private/renet/pkg/embed/assets
          key: renet-embed-assets-${{ hashFiles('private/renet/embed-assets.lock.json') }}
      - name: Install renet
        run: sudo cp "$RENET_BINARY" /usr/bin/renet && sudo chmod +x /usr/bin/renet

      # Same Ubuntu VM base-image cache as the E2E Workers ubuntu leg (no
      # VM_IMAGE set -> Ubuntu default image, same key + disks-dir path).
      - name: Compute VM image cache key
        id: vm-image-key
        run: echo "key=vm-base-image-e2e-ubuntu-24.04-$(date -u +%Y-%m)" >> "$GITHUB_OUTPUT"
      - name: Restore VM base image cache
        id: vm-image-cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ${{ runner.temp }}/renet/disks
          key: ${{ steps.vm-image-key.outputs.key }}

      - name: Setup KVM Host
        run: sudo "$RENET_BINARY" ops host setup

      # Combined k8s + Ceph topology (2 k3s workers + 3 ceph nodes). Explicit
      # --vm-workers AND --vm-ceph-nodes (NOT --ceph, which zeroes workers);
      # per-role RAM + osd_memory_target keep the 14.5G fleet inside the runner.
      - name: Create E2E Test Environment
        run: |
          .ci/scripts/env/create-e2e-env.sh \
            --renet-path /usr/bin/renet \
            --output packages/e2e-tests/.env \
            --vm-workers "11 12" \
            --vm-ceph-nodes "21 22 23" \
            --vm-ram-worker "$VM_RAM_WORKER" \
            --vm-ram-ceph "$VM_RAM_CEPH" \
            --ceph-osd-memory-target "$CEPH_OSD_MEMORY_TARGET"

      - name: Run E2E Tests (K8s Ceph)
        run: .ci/scripts/test/run-e2e.sh --workers 1 --config playwright.k8s-ceph.config.ts --fail-on-skip

      - if: always()
        run: .ci/scripts/signal/create-complete.sh --name "e2e-k8s-ceph" --status ${{ job.status }}

      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a  # v7.0.1
        if: always()
        with:
          name: test-e2e-k8s-ceph-${{ github.sha }}
          path: |
            ${{ runner.temp }}/complete.txt
            packages/e2e-tests/reports/bridge-k8s-ceph/
            packages/e2e-tests/reports/bridge-k8s-ceph-logs/
          retention-days: 3
          if-no-files-found: ignore

      - name: Fix VM image ownership for cache save
        if: always()
        run: sudo chown -R "$(whoami)" "$RUNNER_TEMP/renet/disks" 2>/dev/null || true

      - name: Save VM base image cache
        if: always() && steps.vm-image-cache.outputs.cache-hit != 'true'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ${{ runner.temp }}/renet/disks
          key: ${{ steps.vm-image-key.outputs.key }}

  test-e2e-k8s-multinode:
    name: E2E K8s Multinode
    if: inputs.full_suite == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 90
    env:
      CI: 'true'
      BRIDGE_TIMEOUT: 240000
      VM_BRIDGE: '1'
      VM_WORKERS: '11 12'
      VM_CEPH_NODES: '21 22 23'
      K8S_MODE: '1'
      VM_RAM_WORKER: '3072'
      VM_RAM_CEPH: '2560'
      CEPH_OSD_MEMORY_TARGET: '1717986918'
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: ./.github/actions/app-token
        id: app-token
        with:
          client-id: ${{ vars.APP_ID }}
          private-key: ${{ secrets.APP_PRIVATE_KEY }}
          repositories: console,renet,account,elite,homebrew-tap
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          submodules: true
          token: ${{ steps.app-token.outputs.token }}
      - name: Login to GHCR
        uses: docker/login-action@abd2ef45e78c5afb21d64d4ca52ee8550d9572c7 # v4.5.1
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ github.token }}
      - name: Login to Docker Hub
        uses: docker/login-action@abd2ef45e78c5afb21d64d4ca52ee8550d9572c7 # v4.5.1
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      - name: Propagate Docker Hub credentials to root
        run: sudo mkdir -p /root/.docker && sudo cp ~/.docker/config.json /root/.docker/config.json
      - uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e # v7.0.0
        with:
          go-version: 'stable'
          cache: true
          cache-dependency-path: private/renet/go.sum
      - name: Free Disk Space
        run: |
          echo "=== Disk usage before cleanup ==="
          df -h / || true
          sudo rm -rf /usr/share/dotnet /usr/local/lib/android /opt/ghc /usr/local/share/powershell /usr/share/swift 2>/dev/null || true
          docker system prune -af 2>/dev/null || true
          echo "=== Disk usage after cleanup ==="
          df -h / || true
      - uses: ./.github/actions/setup-workspace
        with:
          account: 'true'
          build-packages: 'true'
      - run: .ci/scripts/build/build-cli.sh
      - name: Install CLI Globally
        run: .ci/scripts/setup/install-cli-global.sh

      - name: Install clang for BPF compilation
        run: sudo apt-get update -qq && sudo apt-get install -y -qq clang llvm libbpf-dev linux-headers-generic
      - name: Pre-pull Docker base images (avoid buildx auth issues)
        run: .ci/scripts/infra/docker-prepull.sh debian:bookworm=linux/amd64 debian:bookworm-slim=linux/arm64 ubuntu:24.04=linux/amd64 ubuntu:24.04=linux/arm64
      # Staged embed-assets cache, shared with Build (Renet) via the same key
      # (see ci-build-renet.yml). On a hit, build.sh's content-based receipt
      # makes embed_assets a verified no-op, skipping the ~10-25 min Docker
      # builder staging. The nolicense go build itself stays on-runner:
      # console is public, so dev binaries must never be uploaded as artifacts.
      - uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        id: embed-cache
        with:
          path: private/renet/pkg/embed/assets
          key: renet-embed-assets-${{ hashFiles('private/renet/embed-assets.lock.json') }}
      - name: Build renet
        run: .ci/scripts/infra/build-renet.sh --nolicense
      - uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        if: always() && steps.embed-cache.outputs.cache-hit != 'true'
        with:
          path: private/renet/pkg/embed/assets
          key: renet-embed-assets-${{ hashFiles('private/renet/embed-assets.lock.json') }}
      - name: Install renet
        run: sudo cp "$RENET_BINARY" /usr/bin/renet && sudo chmod +x /usr/bin/renet

      # Same Ubuntu VM base-image cache as the E2E Workers ubuntu leg (no
      # VM_IMAGE set -> Ubuntu default image, same key + disks-dir path).
      - name: Compute VM image cache key
        id: vm-image-key
        run: echo "key=vm-base-image-e2e-ubuntu-24.04-$(date -u +%Y-%m)" >> "$GITHUB_OUTPUT"
      - name: Restore VM base image cache
        id: vm-image-cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ${{ runner.temp }}/renet/disks
          key: ${{ steps.vm-image-key.outputs.key }}

      - name: Setup KVM Host
        run: sudo "$RENET_BINARY" ops host setup

      # Multi-node k8s topology (2 k3s workers = server + agent, 3 ceph nodes).
      # Explicit
      # --vm-workers AND --vm-ceph-nodes (NOT --ceph, which zeroes workers);
      # per-role RAM + osd_memory_target keep the 14.5G fleet inside the runner.
      - name: Create E2E Test Environment
        run: |
          .ci/scripts/env/create-e2e-env.sh \
            --renet-path /usr/bin/renet \
            --output packages/e2e-tests/.env \
            --vm-workers "11 12" \
            --vm-ceph-nodes "21 22 23" \
            --vm-ram-worker "$VM_RAM_WORKER" \
            --vm-ram-ceph "$VM_RAM_CEPH" \
            --ceph-osd-memory-target "$CEPH_OSD_MEMORY_TARGET"

      - name: Run E2E Tests (K8s Multinode)
        run: .ci/scripts/test/run-e2e.sh --workers 1 --config playwright.k8s-multinode.config.ts --fail-on-skip

      - if: always()
        run: .ci/scripts/signal/create-complete.sh --name "e2e-k8s-multinode" --status ${{ job.status }}

      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a  # v7.0.1
        if: always()
        with:
          name: test-e2e-k8s-multinode-${{ github.sha }}
          path: |
            ${{ runner.temp }}/complete.txt
            packages/e2e-tests/reports/bridge-k8s-multinode/
            packages/e2e-tests/reports/bridge-k8s-multinode-logs/
          retention-days: 3
          if-no-files-found: ignore

      - name: Fix VM image ownership for cache save
        if: always()
        run: sudo chown -R "$(whoami)" "$RUNNER_TEMP/renet/disks" 2>/dev/null || true

      - name: Save VM base image cache
        if: always() && steps.vm-image-cache.outputs.cache-hit != 'true'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ${{ runner.temp }}/renet/disks
          key: ${{ steps.vm-image-key.outputs.key }}

  test-e2e-migrate:
    name: E2E Migrate
    if: inputs.full_suite == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 60
    env:
      CI: 'true'
      BRIDGE_TIMEOUT: 240000
      # Group A (source) — minimal: 1 control + 1 k3s worker, no Ceph.
      VM_BRIDGE: '1'
      VM_WORKERS: '11'
      VM_CEPH_NODES: ''
      K8S_MODE: '1'
      DUAL_GROUP: '1'
      VM_RAM_WORKER: '4096'
      # Group B (destination) — a second concurrent group on renet12 with
      # disjoint VM IDs. Both fleets fit in ~10G (2x [1024 bridge + 4096 worker]).
      GROUP_B_NET: 'renet12'
      GROUP_B_NET_BASE: '192.168.112'
      GROUP_B_BRIDGE: '5'
      GROUP_B_WORKER: '51'
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: ./.github/actions/app-token
        id: app-token
        with:
          client-id: ${{ vars.APP_ID }}
          private-key: ${{ secrets.APP_PRIVATE_KEY }}
          repositories: console,renet,account,elite,homebrew-tap
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          submodules: true
          token: ${{ steps.app-token.outputs.token }}
      - name: Login to GHCR
        uses: docker/login-action@abd2ef45e78c5afb21d64d4ca52ee8550d9572c7 # v4.5.1
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ github.token }}
      - name: Login to Docker Hub
        uses: docker/login-action@abd2ef45e78c5afb21d64d4ca52ee8550d9572c7 # v4.5.1
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      - name: Propagate Docker Hub credentials to root
        run: sudo mkdir -p /root/.docker && sudo cp ~/.docker/config.json /root/.docker/config.json
      - uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e # v7.0.0
        with:
          go-version: 'stable'
          cache: true
          cache-dependency-path: private/renet/go.sum
      - name: Free Disk Space
        run: |
          echo "=== Disk usage before cleanup ==="
          df -h / || true
          sudo rm -rf /usr/share/dotnet /usr/local/lib/android /opt/ghc /usr/local/share/powershell /usr/share/swift 2>/dev/null || true
          docker system prune -af 2>/dev/null || true
          echo "=== Disk usage after cleanup ==="
          df -h / || true
      - uses: ./.github/actions/setup-workspace
        with:
          account: 'true'
          build-packages: 'true'
      - run: .ci/scripts/build/build-cli.sh
      - name: Install CLI Globally
        run: .ci/scripts/setup/install-cli-global.sh

      - name: Install clang for BPF compilation
        run: sudo apt-get update -qq && sudo apt-get install -y -qq clang llvm libbpf-dev linux-headers-generic
      - name: Pre-pull Docker base images (avoid buildx auth issues)
        run: .ci/scripts/infra/docker-prepull.sh ubuntu:24.04=linux/amd64 ubuntu:24.04=linux/arm64
      # Staged embed-assets cache, shared with Build (Renet) via the same key
      # (see ci-build-renet.yml). On a hit, build.sh's content-based receipt
      # makes embed_assets a verified no-op, skipping the ~10-25 min Docker
      # builder staging. The nolicense go build itself stays on-runner:
      # console is public, so dev binaries must never be uploaded as artifacts.
      - uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        id: embed-cache
        with:
          path: private/renet/pkg/embed/assets
          key: renet-embed-assets-${{ hashFiles('private/renet/embed-assets.lock.json') }}
      - name: Build renet
        run: .ci/scripts/infra/build-renet.sh --nolicense
      - uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        if: always() && steps.embed-cache.outputs.cache-hit != 'true'
        with:
          path: private/renet/pkg/embed/assets
          key: renet-embed-assets-${{ hashFiles('private/renet/embed-assets.lock.json') }}
      - name: Install renet
        run: sudo cp "$RENET_BINARY" /usr/bin/renet && sudo chmod +x /usr/bin/renet

      # Same Ubuntu VM base-image cache as the other E2E legs.
      - name: Compute VM image cache key
        id: vm-image-key
        run: echo "key=vm-base-image-e2e-ubuntu-24.04-$(date -u +%Y-%m)" >> "$GITHUB_OUTPUT"
      - name: Restore VM base image cache
        id: vm-image-cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ${{ runner.temp }}/renet/disks
          key: ${{ steps.vm-image-key.outputs.key }}

      - name: Setup KVM Host
        run: sudo "$RENET_BINARY" ops host setup

      # Group A .env drives the harness global setup (source fleet). Group B is
      # booted separately below; both auto-detect the same RENET_DATA_DIR
      # ($RUNNER_TEMP/renet) so the mesh keypair is shared and a cross-group
      # migrate can authenticate A worker -> B worker.
      - name: Create E2E Test Environment (group A)
        run: |
          .ci/scripts/env/create-e2e-env.sh \
            --renet-path /usr/bin/renet \
            --output packages/e2e-tests/.env \
            --vm-workers "11" \
            --vm-ram-worker "$VM_RAM_WORKER" \
            --k8s

      # Boot the SECOND KVM group (renet12 / 192.168.112, disjoint IDs 5+51).
      # DOCKER_REGISTRY is intentionally unset: renet derives 192.168.112.5:5000
      # from the group's bridge IP (the wave-8 opsconfig fix).
      - name: Boot second KVM group (B)
        env:
          VM_NET: 'renet12'
          VM_NET_BASE: '192.168.112'
          VM_NET_OFFSET: '0'
          VM_BRIDGE: '5'
          VM_WORKERS: '51'
          VM_CEPH_NODES: ''
          VM_RAM_WORKER: '4096'
          PROVISION_CEPH_CLUSTER: 'false'
        run: "$RENET_BINARY ops up --force --parallel"

      # Suite 18 boots nothing itself: the migrate config's global setup resets
      # group A, then the suite's beforeAll opens the cross-LAN route (host routes
      # between the two private LANs), peers the fleets, and migrates A -> B.
      - name: Run E2E Tests (Dual-group migrate)
        run: .ci/scripts/test/run-e2e.sh --workers 1 --config playwright.migrate.config.ts --fail-on-skip

      - if: always()
        run: .ci/scripts/signal/create-complete.sh --name "e2e-migrate" --status ${{ job.status }}

      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a  # v7.0.1
        if: always()
        with:
          name: test-e2e-migrate-${{ github.sha }}
          path: |
            ${{ runner.temp }}/complete.txt
            packages/e2e-tests/reports/bridge-migrate/
            packages/e2e-tests/reports/bridge-migrate-logs/
          retention-days: 3
          if-no-files-found: ignore

      - name: Fix VM image ownership for cache save
        if: always()
        run: sudo chown -R "$(whoami)" "$RUNNER_TEMP/renet/disks" 2>/dev/null || true

      - name: Save VM base image cache
        if: always() && steps.vm-image-cache.outputs.cache-hit != 'true'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ${{ runner.temp }}/renet/disks
          key: ${{ steps.vm-image-key.outputs.key }}

  test-fork-isolation:
    name: Concurrent Fork Isolation
    if: inputs.full_suite == 'true'
    runs-on: ubuntu-latest
    # 30 min covered the bind-isolation phases; the console#440 checkpoint
    # phase (counter warm-up + fork --checkpoint + restore) adds ~3 min.
    timeout-minutes: 35
    env:
      CI: 'true'
      VM_NET_BASE: '192.168.111'
      VM_BRIDGE: '1'
      VM_WORKERS: '11'
      VM_CEPH_NODES: ''
      REDIACC_SKIP_MACHINE_ACTIVATION: '1'
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: ./.github/actions/app-token
        id: app-token
        with:
          client-id: ${{ vars.APP_ID }}
          private-key: ${{ secrets.APP_PRIVATE_KEY }}
          repositories: console,renet,account,elite,homebrew-tap
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          submodules: true
          token: ${{ steps.app-token.outputs.token }}
      - name: Login to GHCR
        uses: docker/login-action@abd2ef45e78c5afb21d64d4ca52ee8550d9572c7 # v4.5.1
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ github.token }}
      - name: Login to Docker Hub
        uses: docker/login-action@abd2ef45e78c5afb21d64d4ca52ee8550d9572c7 # v4.5.1
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      - name: Propagate Docker Hub credentials to root
        run: sudo mkdir -p /root/.docker && sudo cp ~/.docker/config.json /root/.docker/config.json
      - uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e # v7.0.0
        with:
          go-version: 'stable'
          cache: true
          cache-dependency-path: private/renet/go.sum
      - name: Free Disk Space
        run: |
          echo "=== Disk usage before cleanup ==="
          df -h / || true
          sudo rm -rf /usr/share/dotnet /usr/local/lib/android /opt/ghc /usr/local/share/powershell /usr/share/swift 2>/dev/null || true
          docker system prune -af 2>/dev/null || true
          echo "=== Disk usage after cleanup ==="
          df -h / || true
      - uses: ./.github/actions/setup-workspace
        with:
          account: 'true'
          build-packages: 'true'
      - run: .ci/scripts/build/build-cli.sh
      - name: Install CLI Globally
        run: .ci/scripts/setup/install-cli-global.sh

      - name: Install clang for BPF compilation
        run: sudo apt-get update -qq && sudo apt-get install -y -qq clang llvm libbpf-dev linux-headers-generic
      - name: Pre-pull Docker base images (avoid buildx auth issues)
        run: .ci/scripts/infra/docker-prepull.sh debian:bookworm=linux/amd64 debian:bookworm-slim=linux/arm64 ubuntu:24.04=linux/amd64 ubuntu:24.04=linux/arm64
      # Staged embed-assets cache, shared with Build (Renet) via the same key
      # (see ci-build-renet.yml). On a hit, build.sh's content-based receipt
      # makes embed_assets a verified no-op, skipping the ~10-25 min Docker
      # builder staging. The nolicense go build itself stays on-runner:
      # console is public, so dev binaries must never be uploaded as artifacts.
      - uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        id: embed-cache
        with:
          path: private/renet/pkg/embed/assets
          key: renet-embed-assets-${{ hashFiles('private/renet/embed-assets.lock.json') }}
      - name: Build renet
        run: .ci/scripts/infra/build-renet.sh --nolicense
      - uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        if: always() && steps.embed-cache.outputs.cache-hit != 'true'
        with:
          path: private/renet/pkg/embed/assets
          key: renet-embed-assets-${{ hashFiles('private/renet/embed-assets.lock.json') }}
      - name: Add renet to PATH
        run: sudo cp "$RENET_BINARY" /usr/bin/renet && sudo chmod +x /usr/bin/renet

      # VM base-image cache shared with the OPS Provision / E2E jobs. Key and
      # path MUST match those jobs exactly (`vm-base-image-e2e-ubuntu-24.04-<YYYY-MM>`
      # over the whole disks/ dir) or the same Ubuntu image is stored twice under
      # two keys, doubling its footprint in the 10GB/repo pool and hastening the
      # LRU eviction that forces live upstream re-downloads. Restored before any
      # sudo step touches $RUNNER_TEMP/renet so extraction happens as the runner user.
      - name: Compute VM image cache key
        id: vm-image-key
        run: echo "key=vm-base-image-e2e-ubuntu-24.04-$(date -u +%Y-%m)" >> "$GITHUB_OUTPUT"
      - name: Restore VM base image cache
        id: vm-image-cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ${{ runner.temp }}/renet/disks
          key: ${{ steps.vm-image-key.outputs.key }}

      - name: Setup KVM Host
        run: sudo "$RENET_BINARY" ops host setup
      - name: Generate SSH keypair
        run: |
          ssh-keygen -t ed25519 -f ~/.ssh/id_ed25519 -N "" -q
          cp ~/.ssh/id_ed25519 ~/.ssh/id_rsa
          cp ~/.ssh/id_ed25519.pub ~/.ssh/id_rsa.pub
      - name: Provision VM
        run: sudo -E "$RENET_BINARY" ops up --force --parallel
        env:
          VM_NET_BASE: ${{ env.VM_NET_BASE }}
          VM_WORKERS: '11'
          VM_CEPH_NODES: ''
          VM_BRIDGE: '1'
      - name: Fix renet data dir ownership
        run: sudo chown -R "$USER:$USER" "$RUNNER_TEMP/renet"
      - name: Wait for VM SSH readiness
        run: .ci/scripts/infra/wait-for-vm-ssh.sh "${VM_NET_BASE}.1" "${VM_NET_BASE}.11"
      - name: Run concurrent-fork isolation test
        run: .ci/scripts/private/concurrent-fork-isolation-test.sh
        env:
          VM_NET_BASE: ${{ env.VM_NET_BASE }}
          VM_WORKERS: '11'
          SSH_USER: ${{ env.USER }}

      # Curated-repo healthcheck convergence smoke test. Deploys
      # app-postgres on the worker and asserts the db container's
      # pg_isready healthcheck (probing localhost) reaches `healthy`.
      # Catches integration regressions that pure unit-level eBPF
      # tests can miss — the class of bug that hid nextcloud-talk's
      # 3-day silent failure.
      - name: Run compose healthcheck smoke test
        run: .ci/scripts/private/compose-healthcheck-smoke-test.sh
        env:
          VM_NET_BASE: ${{ env.VM_NET_BASE }}
          VM_WORKERS: '11'
          SSH_USER: ${{ env.USER }}

      - if: always()
        run: .ci/scripts/signal/create-complete.sh --name "fork-isolation" --status ${{ job.status }}

      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a  # v7.0.1
        if: always()
        with:
          name: test-fork-isolation-${{ github.sha }}
          path: |
            ${{ runner.temp }}/complete.txt
          retention-days: 3
          if-no-files-found: ignore

      - name: Fix VM image ownership for cache save
        if: always()
        run: sudo chown -R "$(whoami)" "$RUNNER_TEMP/renet/disks" 2>/dev/null || true

      - name: Save VM base image cache
        if: always() && steps.vm-image-cache.outputs.cache-hit != 'true'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ${{ runner.temp }}/renet/disks
          key: ${{ steps.vm-image-key.outputs.key }}

  test-renet:
    name: Renet
    if: inputs.full_suite == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 90
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: ./.github/actions/app-token
        id: app-token
        with:
          client-id: ${{ vars.APP_ID }}
          private-key: ${{ secrets.APP_PRIVATE_KEY }}
          repositories: console,renet,account,elite,homebrew-tap
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          submodules: true
          token: ${{ steps.app-token.outputs.token }}

      - uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e # v7.0.0
        with:
          go-version: 'stable'
          cache: true
          cache-dependency-path: private/renet/go.sum

      # Login to Docker Hub to avoid rate limiting
      - name: Login to Docker Hub
        uses: docker/login-action@abd2ef45e78c5afb21d64d4ca52ee8550d9572c7 # v4.5.1
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      - name: Propagate Docker Hub credentials to root
        run: sudo mkdir -p /root/.docker && sudo cp ~/.docker/config.json /root/.docker/config.json

      # Unit tests
      - name: Run renet tests
        env:
          # Declares that this job has no account server, so renet's
          # subscription system tests are expected NOT to run here. Without
          # this declaration run-tests.sh now FAILS rather than warning, because
          # a job that silently omits the only real-licence-issuance tests looks
          # exactly like one that ran and passed them.
          #
          # This job has no `services:` block and provisions no server; the
          # enforcing-licence coverage lives in the License Enforcement leg
          # below, against a per-run ephemeral key. Giving this job a real
          # account server is T1's remaining tier in
          # docs/config-universe-follow-up/03-testing-pillar.md.
          RENET_EXPECT_NO_ACCOUNT_SERVER: >-
            test-renet provisions no account server; real-issuance coverage is
            tracked as T1 tier 2 and the enforcing-binary path is covered by the
            License Enforcement job.
        run: .ci/scripts/private/run-renet.sh test

      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a  # v7.0.1
        if: always()
        with:
          name: test-renet-coverage-${{ github.sha }}
          path: private/renet/coverage.out
          retention-days: 3
          if-no-files-found: ignore

      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a  # v7.0.1
        if: always()
        with:
          name: test-renet-results-${{ github.sha }}
          path: private/renet/test-results.xml
          retention-days: 3
          if-no-files-found: ignore

      # eBPF isolation tests
      - name: Test eBPF
        run: |
          cd private/renet
          sudo go test ./pkg/ebpf/ -v -timeout 60s

      # eBPF socket-isolation behavioral regression tests (ebpf_e2e tag). Loud-skip
      # guard + bpffs mount live in the script so it runs identically on a dev box.
      # Verified locally 2026-07-16: 5/5 pass once bpffs is mounted.
      - name: Test eBPF socket isolation (ebpf_e2e)
        run: sudo .ci/scripts/private/renet-ebpf-e2e.sh

      # Root-tagged renet repository state tests (SaveState/LoadState). They
      # t.Fatal off-root, so under sudo they must run and PASS.
      # Verified locally 2026-07-16: 3/3 pass.
      - name: Test renet repository (root-tagged)
        run: sudo .ci/scripts/private/renet-root-tests.sh

      # CSI driver conformance (csi-sanity, spec 09 §12) on a scratch loop-BTRFS
      # datastore. Two ruled deviations (spec 09 §16) stay red by design and are
      # skipped in the script. Verified locally 2026-07-16: 48/50 pass.
      - name: Test renet CSI driver (csi-sanity conformance)
        run: sudo .ci/scripts/private/renet-csi-sanity.sh

      # Integration tests (needs sudo for KVM)
      - name: Run integration tests
        run: sudo .ci/scripts/private/renet-integration.sh --no-cleanup

      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a  # v7.0.1
        if: always()
        with:
          name: test-renet-integration-${{ github.sha }}
          path: private/renet/test-results.xml
          retention-days: 3
          if-no-files-found: ignore

  # ============================================================================
  # LICENSE ENFORCEMENT: prove a release-shaped renet really refuses unlicensed
  # work and really accepts a valid licence, against a key minted for this run.
  #
  # Every other leg in this file builds --nolicense, so the only licensing code
  # CI has ever executed is the stub in runtime_nolicense.go, which returns
  # success unconditionally for all three validators. renet's unit tests cover
  # the crypto negatives well but never execute cmd/renet, so nothing proved the
  # command wiring, the ldflags key injection, or the exit-10 contract.
  #
  # DELIBERATELY UNLIKE ITS SIBLINGS, and each omission was measured:
  #   - no embed-assets cache. pkg/embed/assets is gitignored, and a fresh
  #     checkout carries the committed .gitkeep placeholders that keep the
  #     `//go:embed assets/<arch>/base/*` patterns matching. Proven in a
  #     throwaway git worktree: the enforcing binary links in 4.6s at 44MB with
  #     no payload staged, against 233MB with it. Nothing here reads an embedded
  #     asset.
  #   - no Docker Hub login, no VM, no BTRFS, no LUKS. The licence gate fires
  #     before any storage work, so acceptance shows up as renet getting past
  #     the gate and then failing at the same later point an unlicensed twin
  #     fails at.
  # Whole battery measured at ~11s locally against an asset-free checkout.
  #
  # `sudo` is required only to install the licence fixtures: RepoLicenseDir is
  # the hardcoded /var/lib/rediacc/license/repos with no env override.
  # ============================================================================
  test-license-enforcement:
    name: License Enforcement
    if: inputs.full_suite == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: ./.github/actions/app-token
        id: app-token
        with:
          client-id: ${{ vars.APP_ID }}
          private-key: ${{ secrets.APP_PRIVATE_KEY }}
          repositories: console,renet,account,elite,homebrew-tap
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          submodules: true
          token: ${{ steps.app-token.outputs.token }}

      - uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e # v7.0.0
        with:
          go-version: 'stable'
          cache: true
          # Both module graphs: renet's, and the mint tool's own (it imports the
          # real pkg/license and pkg/subscription through a replace directive so
          # the licence payload cannot drift from the verifier).
          cache-dependency-path: |
            private/renet/go.sum
            .ci/scripts/private/license-mint/go.sum

      # The script IS the local-dev entry point; there is no CI-only variant to
      # drift from. It mints its own throwaway key when ACCOUNT_ED25519_PUBLIC_KEY
      # is unset, which is the case here on purpose: no production key is
      # involved, and nothing it signs outlives the job.
      - name: Run the license enforcement battery
        run: .ci/scripts/private/license-e2e.sh

  # ============================================================================
  # ACCOUNT E2E: Full Playwright E2E tests for the account portal
  # Spins up account server + RustFS via Docker, runs Playwright tests
  # ============================================================================
  test-account-e2e:
    name: Account E2E
    if: inputs.full_suite == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: ./.github/actions/app-token
        id: app-token
        with:
          client-id: ${{ vars.APP_ID }}
          private-key: ${{ secrets.APP_PRIVATE_KEY }}
          repositories: console,renet,account,elite,homebrew-tap
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          submodules: true
          token: ${{ steps.app-token.outputs.token }}
      - uses: ./.github/actions/setup-workspace
        with:
          account: 'true'
      - run: npm run build:packages
      - name: Install Stripe CLI
        if: env.STRIPE_SANDBOX_SECRET_KEY != ''
        run: |
          curl -s https://packages.stripe.dev/api/security/keypair/stripe-cli-gpg/public | gpg --dearmor | sudo tee /usr/share/keyrings/stripe.gpg >/dev/null
          echo "deb [signed-by=/usr/share/keyrings/stripe.gpg] https://packages.stripe.dev/stripe-cli-debian-local stable main" | sudo tee /etc/apt/sources.list.d/stripe.list
          sudo apt-get update -qq && sudo apt-get install -y -qq stripe
        env:
          STRIPE_SANDBOX_SECRET_KEY: ${{ secrets.STRIPE_SANDBOX_SECRET_KEY }}
      - name: Run Account Portal E2E tests
        run: .ci/scripts/test/run-account-e2e.sh
        env:
          ED25519_PRIVATE_KEY: ${{ secrets.ACCOUNT_ED25519_PRIVATE_KEY }}
          ED25519_PUBLIC_KEY: ${{ secrets.ACCOUNT_ED25519_PUBLIC_KEY }}
          API_KEY: ${{ secrets.ACCOUNT_SERVER_API_KEY }}
          JWT_SECRET: ${{ secrets.ACCOUNT_JWT_SECRET }}
          ROOT_EMAIL: ${{ vars.ROOT_EMAIL }}
          STRIPE_SANDBOX_SECRET_KEY: ${{ secrets.STRIPE_SANDBOX_SECRET_KEY }}
          STRIPE_SANDBOX_WEBHOOK_SECRET: ${{ secrets.STRIPE_SANDBOX_WEBHOOK_SECRET }}
      - name: Upload E2E report
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a  # v7.0.1
        if: failure()
        with:
          name: account-e2e-report-${{ github.sha }}
          path: |
            private/account/e2e/reports/e2e/
            private/account/e2e/test-results/
          retention-days: 3

name: Console CI

on:
  pull_request:
    branches:
      - main
      - develop
    types: [opened, synchronize, reopened, edited]
  push:
    branches:
      - main
      - develop

# Required for cancel-on-failure to work
permissions:
  actions: write
  contents: read

# Cancel in-progress runs when a new run is triggered for the same branch/PR
concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  # ============================================================================
  # TIER 2: PR VALIDATION (PR-only, no dependencies)
  # ============================================================================
  pr-title:
    name: 📋 PR - Title Format
    runs-on: ubuntu-slim
    if: github.event_name == 'pull_request'

    steps:
      - name: Check PR title format
        uses: actions/github-script@ed597411d8f924073f98dfc5c65a23a2325f34cd  # v8
        with:
          script: |
            const title = context.payload.pull_request.title;
            const pattern = /^(feat|fix|docs|style|refactor|perf|test|chore|ci|build|revert)(\(.+\))?: .+/;

            if (!pattern.test(title)) {
              core.setFailed(
                'PR title must follow Conventional Commits format:\n' +
                'type(scope): description\n\n' +
                'Types: feat, fix, docs, style, refactor, perf, test, chore, ci, build, revert\n' +
                'Example: feat(ui): add dark mode toggle'
              );
            } else {
              core.info('✅ PR title follows Conventional Commits format');
            }

      - name: Check PR description
        uses: actions/github-script@ed597411d8f924073f98dfc5c65a23a2325f34cd  # v8
        with:
          script: |
            const body = context.payload.pull_request.body || '';

            if (body.length < 20) {
              core.setFailed('PR description is too short. Please provide a detailed description.');
            } else {
              core.info('✅ PR description is adequate');
            }

      - name: Check for linked issues
        uses: actions/github-script@ed597411d8f924073f98dfc5c65a23a2325f34cd  # v8
        with:
          script: |
            const body = context.payload.pull_request.body || '';
            const issuePattern = /(close|closes|closed|fix|fixes|fixed|resolve|resolves|resolved|ref|refs)\s+#\d+/i;

            if (!issuePattern.test(body)) {
              core.warning('PR does not reference any issues. Consider linking related issues.');
            } else {
              core.info('✅ PR references issues');
            }

      - name: Cancel workflow on failure
        if: failure()
        uses: actions/github-script@ed597411d8f924073f98dfc5c65a23a2325f34cd  # v8
        with:
          script: |
            console.log('Job failed - cancelling workflow run...');
            await github.rest.actions.cancelWorkflowRun({
              owner: context.repo.owner,
              repo: context.repo.repo,
              run_id: context.runId
            });

  pr-size:
    name: 📊 PR - Size
    runs-on: ubuntu-slim
    if: github.event_name == 'pull_request'

    steps:
      - name: Checkout code
        uses: actions/checkout@8e8c483db84b4bee98b60c0593521ed34d9990e8  # v5
        with:
          fetch-depth: 0

      - name: Check PR size
        uses: actions/github-script@ed597411d8f924073f98dfc5c65a23a2325f34cd  # v8
        with:
          script: |
            const { data: files } = await github.rest.pulls.listFiles({
              owner: context.repo.owner,
              repo: context.repo.repo,
              pull_number: context.payload.pull_request.number,
            });

            const additions = files.reduce((sum, file) => sum + file.additions, 0);
            const deletions = files.reduce((sum, file) => sum + file.deletions, 0);
            const totalChanges = additions + deletions;
            const fileCount = files.length;

            core.info(`Files changed: ${fileCount}`);
            core.info(`Lines added: ${additions}`);
            core.info(`Lines deleted: ${deletions}`);
            core.info(`Total changes: ${totalChanges}`);

            // Warning thresholds
            if (totalChanges > 1000) {
              core.warning(
                `⚠️ Large PR detected (${totalChanges} lines changed). ` +
                'Consider breaking it into smaller PRs for easier review.'
              );
            }

            if (fileCount > 30) {
              core.warning(
                `⚠️ Many files changed (${fileCount} files). ` +
                'Consider breaking it into smaller PRs.'
              );
            }

            // Add comment summary
            const comment = `## PR Size Summary\n\n` +
              `- **Files changed:** ${fileCount}\n` +
              `- **Lines added:** ${additions}\n` +
              `- **Lines deleted:** ${deletions}\n` +
              `- **Total changes:** ${totalChanges}\n`;

            core.summary.addRaw(comment).write();

      - name: Cancel workflow on failure
        if: failure()
        uses: ./.github/actions/cancel-on-failure

  pr-conflicts:
    name: 🔀 PR - Conflicts
    runs-on: ubuntu-slim
    if: github.event_name == 'pull_request'

    steps:
      - name: Check mergeable state
        uses: actions/github-script@ed597411d8f924073f98dfc5c65a23a2325f34cd  # v8
        with:
          script: |
            const { data: pr } = await github.rest.pulls.get({
              owner: context.repo.owner,
              repo: context.repo.repo,
              pull_number: context.payload.pull_request.number,
            });

            if (pr.mergeable_state === 'dirty') {
              core.setFailed('❌ PR has merge conflicts. Please resolve them.');
            } else if (pr.mergeable_state === 'unknown') {
              core.info('⏳ Merge status is being calculated...');
            } else {
              core.info('✅ No merge conflicts detected');
            }

      - name: Cancel workflow on failure
        if: failure()
        uses: actions/github-script@ed597411d8f924073f98dfc5c65a23a2325f34cd  # v8
        with:
          script: |
            console.log('Job failed - cancelling workflow run...');
            await github.rest.actions.cancelWorkflowRun({
              owner: context.repo.owner,
              repo: context.repo.repo,
              run_id: context.runId
            });

  # ============================================================================
  # TIER 1: CODE QUALITY & SECURITY (Fast checks, no dependencies)
  # ============================================================================
  quality-versions:
    name: 📦 Quality - Versions
    runs-on: ubuntu-slim

    steps:
      - name: Checkout code
        uses: actions/checkout@8e8c483db84b4bee98b60c0593521ed34d9990e8  # v5

      - name: Block legacy-peer-deps workarounds
        run: |
          # Fail if .npmrc contains legacy-peer-deps or force
          if [ -f .npmrc ]; then
            if grep -qiE "legacy-peer-deps|force" .npmrc; then
              echo "❌ .npmrc contains legacy-peer-deps or force=true"
              echo "Fix peer dependency conflicts properly instead of bypassing checks."
              exit 1
            fi
          fi
          echo "✅ No legacy-peer-deps workarounds detected"

      - name: Setup Node.js
        uses: actions/setup-node@395ad3262231945c25e8478fd5baf05154b1d79f  # v6
        with:
          node-version: '22'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Verify no peer dependency conflicts
        run: |
          # npm ls returns non-zero if there are invalid peer deps
          if ! npm ls 2>&1 | grep -q "invalid"; then
            echo "✅ No peer dependency conflicts"
          else
            echo "❌ Peer dependency conflicts detected:"
            npm ls 2>&1 | grep "invalid" || true
            echo ""
            echo "Fix by aligning versions in package.json, not with --legacy-peer-deps"
            exit 1
          fi

      - name: Check version consistency
        run: npm run version:check

      - name: Cancel workflow on failure
        if: failure()
        uses: ./.github/actions/cancel-on-failure

  quality-lint:
    name: ✨ Quality - Lint
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@8e8c483db84b4bee98b60c0593521ed34d9990e8  # v5

      - name: Setup Node.js
        uses: actions/setup-node@395ad3262231945c25e8478fd5baf05154b1d79f  # v6
        with:
          node-version: '22'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Run ESLint (zero warnings policy)
        run: npm run lint -- --max-warnings 0

      - name: Check unused code (knip)
        run: npm run lint:unused

      - name: Cancel workflow on failure
        if: failure()
        uses: ./.github/actions/cancel-on-failure

  quality-format:
    name: 🎨 Quality - Format
    runs-on: ubuntu-slim

    steps:
      - name: Checkout code
        uses: actions/checkout@8e8c483db84b4bee98b60c0593521ed34d9990e8  # v5

      - name: Setup Node.js
        uses: actions/setup-node@395ad3262231945c25e8478fd5baf05154b1d79f  # v6
        with:
          node-version: '22'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Check code formatting (Biome)
        run: npm run format:check

      - name: Cancel workflow on failure
        if: failure()
        uses: ./.github/actions/cancel-on-failure

  quality-types:
    name: 🔍 Quality - TypeScript
    runs-on: ubuntu-slim

    steps:
      - name: Checkout code
        uses: actions/checkout@8e8c483db84b4bee98b60c0593521ed34d9990e8  # v5

      - name: Setup Node.js
        uses: actions/setup-node@395ad3262231945c25e8478fd5baf05154b1d79f  # v6
        with:
          node-version: '22'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Run TypeScript project references
        run: npm run typecheck

      - name: Cancel workflow on failure
        if: failure()
        uses: ./.github/actions/cancel-on-failure

  quality-security:
    name: 🔒 Quality - Security
    runs-on: ubuntu-slim

    steps:
      - name: Checkout code
        uses: actions/checkout@8e8c483db84b4bee98b60c0593521ed34d9990e8  # v5

      - name: Setup Node.js
        uses: actions/setup-node@395ad3262231945c25e8478fd5baf05154b1d79f  # v6
        with:
          node-version: '22'
          cache: 'npm'

      - name: Run npm audit
        run: npm audit --audit-level=moderate

      - name: Check for vulnerable dependencies
        run: |
          echo "Checking for known vulnerabilities..."
          npm audit --json > audit-report.json
          cat audit-report.json

      - name: Upload audit report
        uses: actions/upload-artifact@330a01c490aca151604b8cf639adc76d48f6c5d4  # v4
        if: always()
        with:
          name: security-audit-${{ github.sha }}
          path: audit-report.json
          retention-days: 30

      - name: Cancel workflow on failure
        if: failure()
        uses: ./.github/actions/cancel-on-failure

  # ============================================================================
  # TIER 3: BUILD & UNIT TESTS (Depends on Tier 1)
  # ============================================================================
  test-unit:
    name: 🧪 Test - Unit
    runs-on: ubuntu-slim
    needs: [quality-versions, quality-lint, quality-format, quality-types, quality-security]

    steps:
      - name: Checkout code
        uses: actions/checkout@8e8c483db84b4bee98b60c0593521ed34d9990e8  # v5

      - name: Setup Node.js
        uses: actions/setup-node@395ad3262231945c25e8478fd5baf05154b1d79f  # v6
        with:
          node-version: '22'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build shared libraries
        run: npm run build:packages

      - name: Run unit tests
        run: |
          npm run test -w @rediacc/shared
          npm run test -w @rediacc/shared-desktop
          npm run test:run -w @rediacc/web
          npm run test:unit -w @rediacc/cli

      - name: Generate coverage report
        if: always()
        run: npm run test:coverage

      - name: Upload coverage to artifacts
        uses: actions/upload-artifact@330a01c490aca151604b8cf639adc76d48f6c5d4  # v4
        if: always()
        with:
          name: coverage-report-${{ github.sha }}
          path: packages/web/coverage/
          retention-days: 30

      - name: Cancel workflow on failure
        if: failure()
        uses: ./.github/actions/cancel-on-failure

  build-cli:
    name: 🔧 Build - CLI
    runs-on: ubuntu-slim
    needs: [quality-versions, quality-lint, quality-format, quality-types, quality-security]

    steps:
      - name: Checkout code
        uses: actions/checkout@8e8c483db84b4bee98b60c0593521ed34d9990e8  # v5

      - name: Setup Node.js
        uses: actions/setup-node@395ad3262231945c25e8478fd5baf05154b1d79f  # v6
        with:
          node-version: '22'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build shared libraries
        run: npm run build:packages

      - name: Build CLI workspace
        run: npm run build:cli && npm run build:bundle -w @rediacc/cli

      - name: Check CLI build output
        run: |
          if [ ! -d "packages/cli/dist" ]; then
            echo "Error: CLI dist directory not created"
            exit 1
          fi
          if [ ! -f "packages/cli/dist/index.js" ]; then
            echo "Error: CLI index.js not found in dist"
            exit 1
          fi
          echo "CLI build successful!"
          ls -lah packages/cli/dist/

      - name: Cancel workflow on failure
        if: failure()
        uses: ./.github/actions/cancel-on-failure

  test-vscode:
    name: 🔍 Test - VS Code (${{ matrix.os }})
    timeout-minutes: 15
    needs: [quality-versions, quality-lint, quality-format, quality-types, quality-security]
    strategy:
      fail-fast: true
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    runs-on: ${{ matrix.os }}

    steps:
      - name: Checkout code
        uses: actions/checkout@8e8c483db84b4bee98b60c0593521ed34d9990e8  # v5

      - name: Setup Node.js
        uses: actions/setup-node@395ad3262231945c25e8478fd5baf05154b1d79f  # v6
        with:
          node-version: '22'
          cache: 'npm'

      - name: Install dependencies
        if: runner.os != 'Windows'
        run: npm ci

      - name: Install dependencies (Windows)
        if: runner.os == 'Windows'
        run: npm ci --ignore-scripts

      - name: Build shared libraries
        run: npm run build:packages

      - name: Build CLI
        run: npm run build:cli && npm run build:bundle -w @rediacc/cli

      - name: Create minimal test env (Unix)
        if: runner.os != 'Windows'
        working-directory: packages/cli/tests
        run: |
          echo "CLI_API_URL=http://localhost/api" > .env
          echo "CLI_MASTER_EMAIL=test@example.com" >> .env
          echo "CLI_MASTER_PASSWORD=testpass" >> .env
          echo "CLI_TIMEOUT=30000" >> .env
          echo "CI=true" >> .env

      - name: Create minimal test env (Windows)
        if: runner.os == 'Windows'
        working-directory: packages/cli/tests
        shell: pwsh
        run: |
          @"
          CLI_API_URL=http://localhost/api
          CLI_MASTER_EMAIL=test@example.com
          CLI_MASTER_PASSWORD=testpass
          CLI_TIMEOUT=30000
          CI=true
          "@ | Out-File -FilePath .env -Encoding utf8

      - name: Run VS Code detection tests
        working-directory: packages/cli
        run: npx playwright test --config tests/playwright.config.ts tests/tests/09-vscode/detection.test.ts
        env:
          # Skip global setup since we're only testing detection (no API needed)
          PLAYWRIGHT_SKIP_GLOBAL_SETUP: 'true'

      - name: Upload test results on failure
        if: failure()
        uses: actions/upload-artifact@330a01c490aca151604b8cf639adc76d48f6c5d4  # v4
        with:
          name: vscode-detection-results-${{ matrix.os }}-${{ github.sha }}
          path: packages/cli/tests/test-results/
          retention-days: 7
          if-no-files-found: ignore

      - name: Cancel workflow on failure
        if: failure()
        uses: ./.github/actions/cancel-on-failure

  # ============================================================================
  # TIER 4: INTEGRATION TESTS (Depends on Tier 3, long-running)
  # Only runs after all builds and unit tests pass to avoid wasting resources
  # Shared backend starts first, then each platform downloads tunnel URL
  # ============================================================================
  infra-backend:
    name: 🖥️ Infra - Backend
    runs-on: ubuntu-latest
    needs: [test-unit, build-cli, test-vscode, build-web, build-desktop]
    timeout-minutes: 120

    steps:
      - name: Checkout Elite Repository
        uses: actions/checkout@8e8c483db84b4bee98b60c0593521ed34d9990e8  # v5
        with:
          repository: rediacc/elite
          path: elite

      - name: Pull Elite Docker Images
        working-directory: elite
        env:
          GITHUB_TOKEN: ${{ secrets.GH_PAT }}
          TAG: latest
        run: ./action/ci-pull-images.sh

      - name: Start Elite Services
        uses: ./elite/action
        with:
          keep-alive: 'true'
          ci-mode: 'true'

      - name: Wait for Services
        run: |
          echo "Waiting for Elite services to be ready..."
          timeout 120 bash -c 'until curl -sf http://localhost/health; do sleep 2; done'
          echo "✓ Elite services are ready"

      - name: Create Cloudflare Tunnel
        id: tunnel
        uses: rediacc/action-cloudflare-tunnel@v1
        with:
          url: http://localhost
          timeout: 60

      - name: Verify Tunnel
        run: |
          TUNNEL_URL="${{ steps.tunnel.outputs.tunnel-url }}"
          echo "Tunnel URL: $TUNNEL_URL"
          for i in {1..20}; do
            if curl -sf "$TUNNEL_URL/health"; then
              echo "✓ Tunnel verified"
              exit 0
            fi
            echo "Attempt $i/20 - waiting..."
            sleep 3
          done
          echo "✗ Tunnel verification failed"
          exit 1

      - name: Create Tunnel URL File
        run: echo "${{ steps.tunnel.outputs.tunnel-url }}" > ${{ runner.temp }}/tunnel-url.txt

      - name: Upload Tunnel URL
        uses: actions/upload-artifact@330a01c490aca151604b8cf639adc76d48f6c5d4  # v4
        with:
          name: tunnel-url
          path: ${{ runner.temp }}/tunnel-url.txt
          retention-days: 1

      - name: Wait for Completion Signals
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          echo "Waiting for test jobs to complete..."
          MAX_WAIT=7200  # 2 hours
          ELAPSED=0

          # CLI platforms
          CLI_PLATFORMS=("linux" "windows" "macos")
          # E2E browsers
          E2E_BROWSERS=("chromium" "firefox" "webkit" "msedge")
          # E2E devices
          E2E_DEVICES=("galaxy-s24" "galaxy-tab-s9" "iphone-15-pro-max" "ipad-pro-11")

          COMPLETED=()
          EXPECTED_COUNT=$((${#CLI_PLATFORMS[@]} + ${#E2E_BROWSERS[@]} + ${#E2E_DEVICES[@]}))

          while [ $ELAPSED -lt $MAX_WAIT ]; do
            # Get all artifacts for this run
            ARTIFACTS=$(gh api repos/${{ github.repository }}/actions/runs/${{ github.run_id }}/artifacts --jq '.artifacts[].name' 2>/dev/null || echo "")

            # Check CLI platforms
            for platform in "${CLI_PLATFORMS[@]}"; do
              if [[ ! " ${COMPLETED[*]} " =~ " cli-${platform} " ]]; then
                if echo "$ARTIFACTS" | grep -q "^test-complete-${platform}$"; then
                  echo "✓ CLI ${platform} tests completed"
                  COMPLETED+=("cli-$platform")
                fi
              fi
            done

            # Check E2E browsers
            for browser in "${E2E_BROWSERS[@]}"; do
              if [[ ! " ${COMPLETED[*]} " =~ " e2e-${browser} " ]]; then
                if echo "$ARTIFACTS" | grep -q "^test-complete-e2e-${browser}$"; then
                  echo "✓ E2E ${browser} tests completed"
                  COMPLETED+=("e2e-$browser")
                fi
              fi
            done

            # Check E2E devices
            for device in "${E2E_DEVICES[@]}"; do
              if [[ ! " ${COMPLETED[*]} " =~ " e2e-${device} " ]]; then
                if echo "$ARTIFACTS" | grep -q "^test-complete-e2e-${device}$"; then
                  echo "✓ E2E ${device} tests completed"
                  COMPLETED+=("e2e-$device")
                fi
              fi
            done

            if [ ${#COMPLETED[@]} -ge $EXPECTED_COUNT ]; then
              echo "✓ All platform tests completed (${#COMPLETED[@]}/${EXPECTED_COUNT})"
              break
            fi

            sleep 30
            ELAPSED=$((ELAPSED + 30))
            echo "Waiting... (${ELAPSED}s / ${MAX_WAIT}s) - Completed: ${#COMPLETED[@]}/${EXPECTED_COUNT}"
          done

          if [ ${#COMPLETED[@]} -lt $EXPECTED_COUNT ]; then
            echo "⚠ Timeout waiting for some platforms. Completed: ${#COMPLETED[@]}/${EXPECTED_COUNT}"
          fi

      - name: Cleanup Elite Services
        if: always()
        working-directory: elite
        run: ./go down || true

      - name: Cancel workflow on failure
        if: failure()
        uses: actions/github-script@ed597411d8f924073f98dfc5c65a23a2325f34cd  # v8
        with:
          script: |
            console.log('Job failed - cancelling workflow run...');
            await github.rest.actions.cancelWorkflowRun({
              owner: context.repo.owner,
              repo: context.repo.repo,
              run_id: context.runId
            });

  test-cli-linux:
    name: 🧪 Test - CLI (Linux)
    runs-on: ubuntu-latest
    needs: [test-unit, build-cli, test-vscode, build-web, build-desktop]
    timeout-minutes: 120

    env:
      REDIACC_TEST_EMAIL: admin@rediacc.io
      REDIACC_TEST_PASSWORD: admin
      REDIACC_TEST_TIMEOUT: 30000

    steps:
      - name: Checkout code
        uses: actions/checkout@8e8c483db84b4bee98b60c0593521ed34d9990e8  # v5

      - name: Setup Node.js
        uses: actions/setup-node@395ad3262231945c25e8478fd5baf05154b1d79f  # v6
        with:
          node-version: '22'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build shared libraries
        run: npm run build:packages

      - name: Build CLI
        run: npm run build:cli && npm run build:bundle -w @rediacc/cli

      - name: Download Tunnel URL
        id: tunnel
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          MAX_WAIT=1500  # 25 minutes
          ELAPSED=0
          while [ $ELAPSED -lt $MAX_WAIT ]; do
            if gh run download ${{ github.run_id }} -n tunnel-url 2>/dev/null; then
              TUNNEL_URL=$(cat tunnel-url.txt)
              echo "tunnel-url=$TUNNEL_URL" >> $GITHUB_OUTPUT
              echo "✓ Downloaded tunnel URL: $TUNNEL_URL"
              exit 0
            fi
            echo "Waiting for tunnel URL... (${ELAPSED}s / ${MAX_WAIT}s)"
            sleep 10
            ELAPSED=$((ELAPSED + 10))
          done
          echo "✗ Failed to download tunnel URL"
          exit 1

      - name: Verify Tunnel Connectivity
        run: |
          TUNNEL_URL="${{ steps.tunnel.outputs.tunnel-url }}"
          for i in {1..10}; do
            if curl -sf "$TUNNEL_URL/health"; then
              echo "✓ Tunnel connectivity verified"
              exit 0
            fi
            echo "Attempt $i/10 - waiting..."
            sleep 5
          done
          echo "✗ Could not connect to tunnel"
          exit 1

      - name: Create CLI Test Environment
        working-directory: packages/cli/tests
        run: |
          TUNNEL_URL="${{ steps.tunnel.outputs.tunnel-url }}"
          echo "CLI_API_URL=${TUNNEL_URL}/api" > .env
          echo "CLI_MASTER_EMAIL=$REDIACC_TEST_EMAIL" >> .env
          echo "CLI_MASTER_PASSWORD=$REDIACC_TEST_PASSWORD" >> .env
          echo "CLI_TIMEOUT=30000" >> .env
          echo "CI=true" >> .env
          echo "DEBUG=false" >> .env
          echo "✓ Created .env for CLI tests"
        env:
          REDIACC_TEST_EMAIL: ${{ env.REDIACC_TEST_EMAIL }}
          REDIACC_TEST_PASSWORD: ${{ env.REDIACC_TEST_PASSWORD }}

      - name: Install VS Code (Linux)
        run: |
          sudo apt-get update
          sudo apt-get install -y wget gpg apt-transport-https
          wget -qO- https://packages.microsoft.com/keys/microsoft.asc | gpg --dearmor > packages.microsoft.gpg
          sudo install -D -o root -g root -m 644 packages.microsoft.gpg /etc/apt/keyrings/packages.microsoft.gpg
          echo "deb [arch=amd64,arm64,armhf signed-by=/etc/apt/keyrings/packages.microsoft.gpg] https://packages.microsoft.com/repos/code stable main" | sudo tee /etc/apt/sources.list.d/vscode.list
          sudo apt-get update
          sudo apt-get install -y code
          rm -f packages.microsoft.gpg
          echo "✓ VS Code installed"
          code --version

      - name: Run CLI Tests
        working-directory: packages/cli
        run: npm test

      - name: Create Completion Signal
        if: always()
        run: echo "complete" > ${{ runner.temp }}/complete.txt

      - name: Upload Completion Signal
        if: always()
        uses: actions/upload-artifact@330a01c490aca151604b8cf639adc76d48f6c5d4  # v4
        with:
          name: test-complete-linux
          path: ${{ runner.temp }}/complete.txt
          retention-days: 1

      - name: Upload CLI Test Report
        if: always()
        uses: actions/upload-artifact@330a01c490aca151604b8cf639adc76d48f6c5d4  # v4
        with:
          name: cli-test-report-linux-${{ github.sha }}
          path: packages/cli/tests/reports/cli/
          retention-days: 7

      - name: Upload CLI Test Logs
        if: always()
        uses: actions/upload-artifact@330a01c490aca151604b8cf639adc76d48f6c5d4  # v4
        with:
          name: cli-test-logs-linux-${{ github.sha }}
          path: packages/cli/tests/reports/cli-logs/
          retention-days: 7

      - name: Cancel workflow on failure
        if: failure()
        uses: ./.github/actions/cancel-on-failure

  test-cli-windows:
    name: 🧪 Test - CLI (Windows)
    runs-on: windows-latest
    needs: [test-unit, build-cli, test-vscode, build-web, build-desktop]
    timeout-minutes: 120

    env:
      REDIACC_TEST_EMAIL: admin@rediacc.io
      REDIACC_TEST_PASSWORD: admin
      REDIACC_TEST_TIMEOUT: 30000

    steps:
      - name: Checkout code
        uses: actions/checkout@8e8c483db84b4bee98b60c0593521ed34d9990e8  # v5

      - name: Setup Node.js
        uses: actions/setup-node@395ad3262231945c25e8478fd5baf05154b1d79f  # v6
        with:
          node-version: '22'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci --ignore-scripts

      - name: Build shared libraries
        run: npm run build:packages

      - name: Build CLI
        run: npm run build:cli && npm run build:bundle -w @rediacc/cli

      - name: Download Tunnel URL
        id: tunnel
        shell: pwsh
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          $maxWait = 1500  # 25 minutes
          $elapsed = 0
          while ($elapsed -lt $maxWait) {
            try {
              gh run download ${{ github.run_id }} -n tunnel-url 2>$null
              if (Test-Path "tunnel-url.txt") {
                $tunnelUrl = Get-Content "tunnel-url.txt" -Raw
                $tunnelUrl = $tunnelUrl.Trim()
                "tunnel-url=$tunnelUrl" | Out-File -Append -FilePath $env:GITHUB_OUTPUT
                Write-Host "Downloaded tunnel URL: $tunnelUrl"
                exit 0
              }
            } catch {}
            Write-Host "Waiting for tunnel URL... ($elapsed s / $maxWait s)"
            Start-Sleep -Seconds 10
            $elapsed += 10
          }
          Write-Error "Failed to download tunnel URL"
          exit 1

      - name: Verify Tunnel Connectivity
        shell: pwsh
        run: |
          $tunnelUrl = "${{ steps.tunnel.outputs.tunnel-url }}"
          for ($i = 1; $i -le 10; $i++) {
            try {
              $response = Invoke-WebRequest -Uri "$tunnelUrl/health" -UseBasicParsing -TimeoutSec 10
              if ($response.StatusCode -eq 200) {
                Write-Host "Tunnel connectivity verified"
                exit 0
              }
            } catch {}
            Write-Host "Attempt $i/10 - waiting..."
            Start-Sleep -Seconds 5
          }
          Write-Error "Could not connect to tunnel"
          exit 1

      - name: Create CLI Test Environment
        shell: pwsh
        working-directory: packages/cli/tests
        run: |
          $tunnelUrl = "${{ steps.tunnel.outputs.tunnel-url }}"
          @"
          CLI_API_URL=$tunnelUrl/api
          CLI_MASTER_EMAIL=${{ env.REDIACC_TEST_EMAIL }}
          CLI_MASTER_PASSWORD=${{ env.REDIACC_TEST_PASSWORD }}
          CLI_TIMEOUT=30000
          CI=true
          DEBUG=false
          "@ | Out-File -FilePath .env -Encoding utf8
          Write-Host "Created .env for CLI tests"

      - name: Install VS Code (Windows)
        shell: pwsh
        run: |
          choco install vscode -y
          Write-Host "VS Code installed"
          # Refresh PATH to include VS Code
          $env:Path = [System.Environment]::GetEnvironmentVariable("Path","Machine") + ";" + [System.Environment]::GetEnvironmentVariable("Path","User")
          code --version

      - name: Run CLI Tests
        working-directory: packages/cli
        run: npm test

      - name: Create Completion Signal
        if: always()
        shell: pwsh
        run: |
          "complete" | Out-File -FilePath "${{ runner.temp }}/complete.txt" -Encoding utf8

      - name: Upload Completion Signal
        if: always()
        uses: actions/upload-artifact@330a01c490aca151604b8cf639adc76d48f6c5d4  # v4
        with:
          name: test-complete-windows
          path: ${{ runner.temp }}/complete.txt
          retention-days: 1

      - name: Upload CLI Test Report
        if: always()
        uses: actions/upload-artifact@330a01c490aca151604b8cf639adc76d48f6c5d4  # v4
        with:
          name: cli-test-report-windows-${{ github.sha }}
          path: packages/cli/tests/reports/cli/
          retention-days: 7

      - name: Upload CLI Test Logs
        if: always()
        uses: actions/upload-artifact@330a01c490aca151604b8cf639adc76d48f6c5d4  # v4
        with:
          name: cli-test-logs-windows-${{ github.sha }}
          path: packages/cli/tests/reports/cli-logs/
          retention-days: 7

      - name: Cancel workflow on failure
        if: failure()
        uses: ./.github/actions/cancel-on-failure

  test-cli-macos:
    name: 🧪 Test - CLI (macOS)
    runs-on: macos-latest
    needs: [test-unit, build-cli, test-vscode, build-web, build-desktop]
    timeout-minutes: 120

    env:
      REDIACC_TEST_EMAIL: admin@rediacc.io
      REDIACC_TEST_PASSWORD: admin
      REDIACC_TEST_TIMEOUT: 30000

    steps:
      - name: Checkout code
        uses: actions/checkout@8e8c483db84b4bee98b60c0593521ed34d9990e8  # v5

      - name: Setup Node.js
        uses: actions/setup-node@395ad3262231945c25e8478fd5baf05154b1d79f  # v6
        with:
          node-version: '22'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build shared libraries
        run: npm run build:packages

      - name: Build CLI
        run: npm run build:cli && npm run build:bundle -w @rediacc/cli

      - name: Download Tunnel URL
        id: tunnel
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          MAX_WAIT=1500  # 25 minutes
          ELAPSED=0
          while [ $ELAPSED -lt $MAX_WAIT ]; do
            if gh run download ${{ github.run_id }} -n tunnel-url 2>/dev/null; then
              TUNNEL_URL=$(cat tunnel-url.txt)
              echo "tunnel-url=$TUNNEL_URL" >> $GITHUB_OUTPUT
              echo "✓ Downloaded tunnel URL: $TUNNEL_URL"
              exit 0
            fi
            echo "Waiting for tunnel URL... (${ELAPSED}s / ${MAX_WAIT}s)"
            sleep 10
            ELAPSED=$((ELAPSED + 10))
          done
          echo "✗ Failed to download tunnel URL"
          exit 1

      - name: Verify Tunnel Connectivity
        run: |
          TUNNEL_URL="${{ steps.tunnel.outputs.tunnel-url }}"
          for i in {1..10}; do
            if curl -sf "$TUNNEL_URL/health"; then
              echo "✓ Tunnel connectivity verified"
              exit 0
            fi
            echo "Attempt $i/10 - waiting..."
            sleep 5
          done
          echo "✗ Could not connect to tunnel"
          exit 1

      - name: Create CLI Test Environment
        working-directory: packages/cli/tests
        run: |
          TUNNEL_URL="${{ steps.tunnel.outputs.tunnel-url }}"
          echo "CLI_API_URL=${TUNNEL_URL}/api" > .env
          echo "CLI_MASTER_EMAIL=$REDIACC_TEST_EMAIL" >> .env
          echo "CLI_MASTER_PASSWORD=$REDIACC_TEST_PASSWORD" >> .env
          echo "CLI_TIMEOUT=30000" >> .env
          echo "CI=true" >> .env
          echo "DEBUG=false" >> .env
          echo "✓ Created .env for CLI tests"
        env:
          REDIACC_TEST_EMAIL: ${{ env.REDIACC_TEST_EMAIL }}
          REDIACC_TEST_PASSWORD: ${{ env.REDIACC_TEST_PASSWORD }}

      - name: Install VS Code (macOS)
        run: |
          brew install --cask visual-studio-code
          echo "✓ VS Code installed"
          code --version

      - name: Run CLI Tests
        working-directory: packages/cli
        run: npm test

      - name: Create Completion Signal
        if: always()
        run: echo "complete" > ${{ runner.temp }}/complete.txt

      - name: Upload Completion Signal
        if: always()
        uses: actions/upload-artifact@330a01c490aca151604b8cf639adc76d48f6c5d4  # v4
        with:
          name: test-complete-macos
          path: ${{ runner.temp }}/complete.txt
          retention-days: 1

      - name: Upload CLI Test Report
        if: always()
        uses: actions/upload-artifact@330a01c490aca151604b8cf639adc76d48f6c5d4  # v4
        with:
          name: cli-test-report-macos-${{ github.sha }}
          path: packages/cli/tests/reports/cli/
          retention-days: 7

      - name: Upload CLI Test Logs
        if: always()
        uses: actions/upload-artifact@330a01c490aca151604b8cf639adc76d48f6c5d4  # v4
        with:
          name: cli-test-logs-macos-${{ github.sha }}
          path: packages/cli/tests/reports/cli-logs/
          retention-days: 7

      - name: Cancel workflow on failure
        if: failure()
        uses: ./.github/actions/cancel-on-failure

  test-e2e:
    name: 🌐 Test - E2E (${{ matrix.browser }})
    runs-on: ubuntu-latest
    needs: [test-unit, build-cli, test-vscode, build-web, build-desktop]
    timeout-minutes: 60

    strategy:
      fail-fast: true
      max-parallel: 1
      matrix:
        browser: [chromium, firefox, webkit, msedge]
        include:
          - browser: chromium
            install: npx playwright install --with-deps chromium
          - browser: firefox
            install: npx playwright install --with-deps firefox
          - browser: webkit
            install: npx playwright install --with-deps webkit
          - browser: msedge
            install: npx playwright install --with-deps chromium && npx playwright install msedge

    env:
      REDIACC_TEST_EMAIL: admin@rediacc.io
      REDIACC_TEST_PASSWORD: admin

    steps:
      - name: Checkout code
        uses: actions/checkout@8e8c483db84b4bee98b60c0593521ed34d9990e8  # v5

      - name: Setup Node.js
        uses: actions/setup-node@395ad3262231945c25e8478fd5baf05154b1d79f  # v6
        with:
          node-version: '22'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build shared libraries
        run: npm run build:packages

      - name: Install Playwright Browser (${{ matrix.browser }})
        working-directory: packages/e2e
        run: ${{ matrix.install }}

      - name: Download Tunnel URL
        id: tunnel
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          MAX_WAIT=1500
          ELAPSED=0
          while [ $ELAPSED -lt $MAX_WAIT ]; do
            if gh run download ${{ github.run_id }} -n tunnel-url 2>/dev/null; then
              TUNNEL_URL=$(cat tunnel-url.txt)
              echo "tunnel-url=$TUNNEL_URL" >> $GITHUB_OUTPUT
              echo "✓ Downloaded tunnel URL: $TUNNEL_URL"
              exit 0
            fi
            echo "Waiting for tunnel URL... (${ELAPSED}s / ${MAX_WAIT}s)"
            sleep 10
            ELAPSED=$((ELAPSED + 10))
          done
          echo "✗ Failed to download tunnel URL"
          exit 1

      - name: Verify Tunnel Connectivity
        run: |
          TUNNEL_URL="${{ steps.tunnel.outputs.tunnel-url }}"
          for i in {1..10}; do
            if curl -sf "$TUNNEL_URL/health"; then
              echo "✓ Tunnel connectivity verified"
              exit 0
            fi
            echo "Attempt $i/10 - waiting..."
            sleep 5
          done
          echo "✗ Could not connect to tunnel"
          exit 1

      - name: Create E2E Test Environment
        working-directory: packages/e2e
        run: |
          TUNNEL_URL="${{ steps.tunnel.outputs.tunnel-url }}"
          # E2E tests run against Vite dev server (source code)
          # Vite proxies /api/* to the tunnel URL (backend)
          echo "E2E_BASE_URL=http://localhost:3000/console/" > .env
          echo "VITE_API_URL=${TUNNEL_URL}" >> .env
          echo "TEST_USER_EMAIL=$REDIACC_TEST_EMAIL" >> .env
          echo "TEST_USER_PASSWORD=$REDIACC_TEST_PASSWORD" >> .env
          echo "ADMIN_USER_EMAIL=$REDIACC_TEST_EMAIL" >> .env
          echo "ADMIN_USER_PASSWORD=$REDIACC_TEST_PASSWORD" >> .env
          echo "TEST_VERIFICATION_CODE=111111" >> .env
          echo "CI=true" >> .env
          echo "✓ Created .env for E2E tests (${{ matrix.browser }})"
        env:
          REDIACC_TEST_EMAIL: ${{ env.REDIACC_TEST_EMAIL }}
          REDIACC_TEST_PASSWORD: ${{ env.REDIACC_TEST_PASSWORD }}

      - name: Run E2E Tests (${{ matrix.browser }})
        working-directory: packages/e2e
        run: npx playwright test --project=${{ matrix.browser }}

      - name: Create Completion Signal
        if: always()
        run: echo "complete" > ${{ runner.temp }}/complete.txt

      - name: Upload Completion Signal
        if: always()
        uses: actions/upload-artifact@330a01c490aca151604b8cf639adc76d48f6c5d4  # v4
        with:
          name: test-complete-e2e-${{ matrix.browser }}
          path: ${{ runner.temp }}/complete.txt
          retention-days: 1

      - name: Upload E2E Test Report (${{ matrix.browser }})
        if: always()
        uses: actions/upload-artifact@330a01c490aca151604b8cf639adc76d48f6c5d4  # v4
        with:
          name: e2e-report-${{ matrix.browser }}-${{ github.sha }}
          path: packages/e2e/reports/e2e/
          retention-days: 3
          if-no-files-found: ignore

      - name: Upload E2E Screenshots (${{ matrix.browser }})
        if: always()
        uses: actions/upload-artifact@330a01c490aca151604b8cf639adc76d48f6c5d4  # v4
        with:
          name: e2e-screenshots-${{ matrix.browser }}-${{ github.sha }}
          path: packages/e2e/screenshots/
          retention-days: 3
          if-no-files-found: ignore

      - name: Upload E2E Test Results (${{ matrix.browser }})
        if: always()
        uses: actions/upload-artifact@330a01c490aca151604b8cf639adc76d48f6c5d4  # v4
        with:
          name: e2e-results-${{ matrix.browser }}-${{ github.sha }}
          path: packages/e2e/test-results/
          retention-days: 3
          if-no-files-found: ignore

      - name: Cancel workflow on failure
        if: failure()
        uses: ./.github/actions/cancel-on-failure

  test-e2e-devices:
    name: 🌐 Test - E2E Devices (${{ matrix.device }})
    runs-on: ubuntu-latest
    needs: [test-unit, build-cli, test-vscode, build-web, build-desktop]
    timeout-minutes: 60

    strategy:
      fail-fast: true
      max-parallel: 1
      matrix:
        include:
          - device: galaxy-s24
            browser: chromium
            install: npx playwright install --with-deps chromium
          - device: galaxy-tab-s9
            browser: chromium
            install: npx playwright install --with-deps chromium
          - device: iphone-15-pro-max
            browser: webkit
            install: npx playwright install --with-deps webkit
          - device: ipad-pro-11
            browser: webkit
            install: npx playwright install --with-deps webkit

    env:
      REDIACC_TEST_EMAIL: admin@rediacc.io
      REDIACC_TEST_PASSWORD: admin

    steps:
      - name: Checkout code
        uses: actions/checkout@8e8c483db84b4bee98b60c0593521ed34d9990e8  # v5

      - name: Setup Node.js
        uses: actions/setup-node@395ad3262231945c25e8478fd5baf05154b1d79f  # v6
        with:
          node-version: '22'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build shared libraries
        run: npm run build:packages

      - name: Install Playwright Browser (${{ matrix.browser }})
        working-directory: packages/e2e
        run: ${{ matrix.install }}

      - name: Download Tunnel URL
        id: tunnel
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          MAX_WAIT=1500
          ELAPSED=0
          while [ $ELAPSED -lt $MAX_WAIT ]; do
            if gh run download ${{ github.run_id }} -n tunnel-url 2>/dev/null; then
              TUNNEL_URL=$(cat tunnel-url.txt)
              echo "tunnel-url=$TUNNEL_URL" >> $GITHUB_OUTPUT
              echo "✓ Downloaded tunnel URL: $TUNNEL_URL"
              exit 0
            fi
            echo "Waiting for tunnel URL... (${ELAPSED}s / ${MAX_WAIT}s)"
            sleep 10
            ELAPSED=$((ELAPSED + 10))
          done
          echo "✗ Failed to download tunnel URL"
          exit 1

      - name: Verify Tunnel Connectivity
        run: |
          TUNNEL_URL="${{ steps.tunnel.outputs.tunnel-url }}"
          for i in {1..10}; do
            if curl -sf "$TUNNEL_URL/health"; then
              echo "✓ Tunnel connectivity verified"
              exit 0
            fi
            echo "Attempt $i/10 - waiting..."
            sleep 5
          done
          echo "✗ Could not connect to tunnel"
          exit 1

      - name: Create E2E Test Environment
        working-directory: packages/e2e
        run: |
          TUNNEL_URL="${{ steps.tunnel.outputs.tunnel-url }}"
          echo "E2E_BASE_URL=http://localhost:3000/console/" > .env
          echo "VITE_API_URL=${TUNNEL_URL}" >> .env
          echo "TEST_USER_EMAIL=$REDIACC_TEST_EMAIL" >> .env
          echo "TEST_USER_PASSWORD=$REDIACC_TEST_PASSWORD" >> .env
          echo "ADMIN_USER_EMAIL=$REDIACC_TEST_EMAIL" >> .env
          echo "ADMIN_USER_PASSWORD=$REDIACC_TEST_PASSWORD" >> .env
          echo "TEST_VERIFICATION_CODE=111111" >> .env
          echo "CI=true" >> .env
          echo "✓ Created .env for E2E device tests (${{ matrix.device }})"
        env:
          REDIACC_TEST_EMAIL: ${{ env.REDIACC_TEST_EMAIL }}
          REDIACC_TEST_PASSWORD: ${{ env.REDIACC_TEST_PASSWORD }}

      - name: Run E2E Device Tests (${{ matrix.device }})
        working-directory: packages/e2e
        run: npx playwright test --project=${{ matrix.device }}

      - name: Create Completion Signal
        if: always()
        run: echo "complete" > ${{ runner.temp }}/complete.txt

      - name: Upload Completion Signal
        if: always()
        uses: actions/upload-artifact@330a01c490aca151604b8cf639adc76d48f6c5d4  # v4
        with:
          name: test-complete-e2e-${{ matrix.device }}
          path: ${{ runner.temp }}/complete.txt
          retention-days: 1

      - name: Upload E2E Device Report (${{ matrix.device }})
        if: always()
        uses: actions/upload-artifact@330a01c490aca151604b8cf639adc76d48f6c5d4  # v4
        with:
          name: e2e-report-${{ matrix.device }}-${{ github.sha }}
          path: packages/e2e/reports/e2e/
          retention-days: 3
          if-no-files-found: ignore

      - name: Upload E2E Device Screenshots (${{ matrix.device }})
        if: always()
        uses: actions/upload-artifact@330a01c490aca151604b8cf639adc76d48f6c5d4  # v4
        with:
          name: e2e-screenshots-${{ matrix.device }}-${{ github.sha }}
          path: packages/e2e/screenshots/
          retention-days: 3
          if-no-files-found: ignore

      - name: Upload E2E Device Results (${{ matrix.device }})
        if: always()
        uses: actions/upload-artifact@330a01c490aca151604b8cf639adc76d48f6c5d4  # v4
        with:
          name: e2e-results-${{ matrix.device }}-${{ github.sha }}
          path: packages/e2e/test-results/
          retention-days: 3
          if-no-files-found: ignore

      - name: Cancel workflow on failure
        if: failure()
        uses: ./.github/actions/cancel-on-failure

  build-web:
    name: 🏗️ Build - Web
    runs-on: ubuntu-slim
    needs: [quality-versions, quality-lint, quality-format, quality-types, quality-security]

    strategy:
      matrix:
        build-type: [DEBUG, RELEASE]

    steps:
      - name: Checkout code
        uses: actions/checkout@8e8c483db84b4bee98b60c0593521ed34d9990e8  # v5

      - name: Setup Node.js
        uses: actions/setup-node@395ad3262231945c25e8478fd5baf05154b1d79f  # v6
        with:
          node-version: '22'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build shared libraries
        run: npm run build:packages

      - name: Build web application (${{ matrix.build-type }})
        env:
          REDIACC_BUILD_TYPE: ${{ matrix.build-type }}
          VITE_APP_VERSION: ${{ github.sha }}
          VITE_BASE_PATH: /
          NODE_ENV: production
        run: npm run build:web

      - name: Check build output
        run: |
          if [ ! -d "packages/web/dist" ]; then
            echo "Error: dist directory not created"
            exit 1
          fi
          if [ ! -f "packages/web/dist/index.html" ]; then
            echo "Error: index.html not found in dist"
            exit 1
          fi
          echo "Build successful!"
          ls -lah packages/web/dist/

      - name: Upload build artifacts
        uses: actions/upload-artifact@330a01c490aca151604b8cf639adc76d48f6c5d4  # v4
        with:
          name: build-${{ matrix.build-type }}-${{ github.sha }}
          path: packages/web/dist/
          retention-days: 90

      # Bundle Size Analysis (RELEASE only)
      - name: Analyze bundle size
        if: matrix.build-type == 'RELEASE'
        run: |
          echo "## 📦 Bundle Size Report" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### Main Bundles" >> $GITHUB_STEP_SUMMARY
          echo "\`\`\`" >> $GITHUB_STEP_SUMMARY
          du -h packages/web/dist/assets/js/*.js | sort -h >> $GITHUB_STEP_SUMMARY
          echo "\`\`\`" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### Total Size" >> $GITHUB_STEP_SUMMARY
          echo "\`\`\`" >> $GITHUB_STEP_SUMMARY
          du -sh packages/web/dist/ >> $GITHUB_STEP_SUMMARY
          echo "\`\`\`" >> $GITHUB_STEP_SUMMARY

      - name: Check bundle size limits
        if: matrix.build-type == 'RELEASE'
        run: |
          TOTAL_SIZE=$(du -sb packages/web/dist/ | cut -f1)
          MAX_SIZE=$((10 * 1024 * 1024))  # 10MB limit

          if [ $TOTAL_SIZE -gt $MAX_SIZE ]; then
            echo "::warning::Bundle size ($TOTAL_SIZE bytes) exceeds 10MB limit"
          else
            echo "Bundle size OK: $TOTAL_SIZE bytes"
          fi

      - name: Cancel workflow on failure
        if: failure()
        uses: ./.github/actions/cancel-on-failure

  build-desktop:
    name: 🖥️ Build - Desktop (${{ matrix.os }})
    needs: [quality-versions, quality-lint, quality-format, quality-types, quality-security]
    timeout-minutes: 30
    strategy:
      fail-fast: true
      matrix:
        # windows-latest works now that native module rebuild is skipped (beforeBuild hook)
        os: [ubuntu-latest, macos-latest, windows-latest]
    runs-on: ${{ matrix.os }}

    steps:
      - name: Checkout code
        uses: actions/checkout@8e8c483db84b4bee98b60c0593521ed34d9990e8  # v5

      - name: Setup Node.js
        uses: actions/setup-node@395ad3262231945c25e8478fd5baf05154b1d79f  # v6
        with:
          node-version: '22'  # Required for @electron/rebuild 4.x
          cache: 'npm'

      - name: Install dependencies
        if: runner.os != 'Windows'
        run: npm ci

      - name: Install dependencies (Windows)
        if: runner.os == 'Windows'
        # Skip postinstall scripts to avoid electron-builder install-app-deps timeout.
        # Native module rebuild is skipped entirely via beforeBuild hook (scripts/before-build.js).
        run: npm ci --ignore-scripts

      - name: Upload npm logs on failure (Windows)
        if: (failure() || cancelled()) && runner.os == 'Windows'
        uses: actions/upload-artifact@330a01c490aca151604b8cf639adc76d48f6c5d4  # v4
        with:
          name: npm-debug-logs-windows-${{ github.sha }}
          path: C:\npm\cache\_logs\
          retention-days: 7
          if-no-files-found: ignore

      - name: Build shared libraries
        run: npm run build:packages

      - name: Build web application
        env:
          REDIACC_BUILD_TYPE: RELEASE
          VITE_APP_VERSION: ${{ github.sha }}
          NODE_ENV: production
        run: npm run build:web

      - name: Build Electron main/preload
        working-directory: packages/desktop
        run: npm run build

      - name: Check desktop build output (Unix)
        if: runner.os != 'Windows'
        run: |
          echo "Checking Electron build output..."
          if [ ! -d "packages/desktop/out" ]; then
            echo "Error: desktop out directory not created"
            exit 1
          fi
          if [ ! -f "packages/desktop/out/main/index.js" ]; then
            echo "Error: main process bundle not found"
            exit 1
          fi
          if [ ! -f "packages/desktop/out/preload/index.js" ]; then
            echo "Error: preload script bundle not found"
            exit 1
          fi
          echo "Desktop build successful!"
          echo "## Desktop Build Output (${{ matrix.os }})" >> $GITHUB_STEP_SUMMARY
          echo "\`\`\`" >> $GITHUB_STEP_SUMMARY
          ls -lah packages/desktop/out/ >> $GITHUB_STEP_SUMMARY
          ls -lah packages/desktop/out/main/ >> $GITHUB_STEP_SUMMARY
          ls -lah packages/desktop/out/preload/ >> $GITHUB_STEP_SUMMARY
          echo "\`\`\`" >> $GITHUB_STEP_SUMMARY

      - name: Check desktop build output (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          Write-Host "Checking Electron build output..."
          if (-not (Test-Path "packages/desktop/out")) {
            Write-Error "Error: desktop out directory not created"
            exit 1
          }
          if (-not (Test-Path "packages/desktop/out/main/index.js")) {
            Write-Error "Error: main process bundle not found"
            exit 1
          }
          if (-not (Test-Path "packages/desktop/out/preload/index.js")) {
            Write-Error "Error: preload script bundle not found"
            exit 1
          }
          Write-Host "Desktop build successful!"
          "## Desktop Build Output (${{ matrix.os }})" | Out-File -Append -FilePath $env:GITHUB_STEP_SUMMARY
          "``````" | Out-File -Append -FilePath $env:GITHUB_STEP_SUMMARY
          Get-ChildItem packages/desktop/out/ | Out-File -Append -FilePath $env:GITHUB_STEP_SUMMARY
          Get-ChildItem packages/desktop/out/main/ | Out-File -Append -FilePath $env:GITHUB_STEP_SUMMARY
          Get-ChildItem packages/desktop/out/preload/ | Out-File -Append -FilePath $env:GITHUB_STEP_SUMMARY
          "``````" | Out-File -Append -FilePath $env:GITHUB_STEP_SUMMARY

      # Create distribution packages for each platform
      - name: Create distribution packages (Linux)
        if: runner.os == 'Linux'
        working-directory: packages/desktop
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: npm run dist:linux

      - name: Create distribution packages (macOS)
        if: runner.os == 'macOS'
        working-directory: packages/desktop
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          CSC_IDENTITY_AUTO_DISCOVERY: false
        run: npm run dist:mac

      - name: Bundle MSYS2 for Windows
        if: runner.os == 'Windows'
        working-directory: packages/desktop
        shell: bash
        run: npm run bundle:msys2

      - name: Create distribution packages (Windows)
        if: runner.os == 'Windows'
        working-directory: packages/desktop
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: npm run dist:win

      - name: Upload desktop artifacts
        uses: actions/upload-artifact@330a01c490aca151604b8cf639adc76d48f6c5d4  # v4
        with:
          name: desktop-${{ matrix.os }}-${{ github.sha }}
          path: dist/desktop/
          retention-days: 30
          if-no-files-found: error

      - name: Cancel workflow on failure
        if: failure()
        uses: ./.github/actions/cancel-on-failure

  # ============================================================================
  # TIER 5: CI SUMMARY (Depends on all)
  # ============================================================================
  summary:
    name: ✅ Summary
    runs-on: ubuntu-slim
    needs: [quality-versions, quality-lint, quality-format, quality-types, quality-security, test-unit, build-cli, test-vscode, infra-backend, test-cli-linux, test-cli-windows, test-cli-macos, test-e2e, test-e2e-devices, build-web, build-desktop]
    if: always()

    steps:
      - name: Check job results
        run: |
          echo "## CI Pipeline Summary" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### Tier 1: Quality & Security" >> $GITHUB_STEP_SUMMARY
          echo "- Lint: ${{ needs.quality-lint.result }}" >> $GITHUB_STEP_SUMMARY
          echo "- Format: ${{ needs.quality-format.result }}" >> $GITHUB_STEP_SUMMARY
          echo "- TypeScript: ${{ needs.quality-types.result }}" >> $GITHUB_STEP_SUMMARY
          echo "- Security: ${{ needs.quality-security.result }}" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### Tier 3: Build & Unit Tests" >> $GITHUB_STEP_SUMMARY
          echo "- Unit Tests: ${{ needs.test-unit.result }}" >> $GITHUB_STEP_SUMMARY
          echo "- CLI Build: ${{ needs.build-cli.result }}" >> $GITHUB_STEP_SUMMARY
          echo "- Web Build: ${{ needs.build-web.result }}" >> $GITHUB_STEP_SUMMARY
          echo "- Desktop Build: ${{ needs.build-desktop.result }}" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### Tier 4: Integration Tests" >> $GITHUB_STEP_SUMMARY
          echo "- VS Code: ${{ needs.test-vscode.result }}" >> $GITHUB_STEP_SUMMARY
          echo "- Backend Infra: ${{ needs.infra-backend.result }}" >> $GITHUB_STEP_SUMMARY
          echo "- CLI (Linux): ${{ needs.test-cli-linux.result }}" >> $GITHUB_STEP_SUMMARY
          echo "- CLI (Windows): ${{ needs.test-cli-windows.result }}" >> $GITHUB_STEP_SUMMARY
          echo "- CLI (macOS): ${{ needs.test-cli-macos.result }}" >> $GITHUB_STEP_SUMMARY
          echo "- E2E Browsers: ${{ needs.test-e2e.result }}" >> $GITHUB_STEP_SUMMARY
          echo "- E2E Devices: ${{ needs.test-e2e-devices.result }}" >> $GITHUB_STEP_SUMMARY

          if [ "${{ needs.quality-lint.result }}" != "success" ] || \
             [ "${{ needs.quality-format.result }}" != "success" ] || \
             [ "${{ needs.quality-types.result }}" != "success" ] || \
             [ "${{ needs.test-unit.result }}" != "success" ] || \
             [ "${{ needs.build-cli.result }}" != "success" ] || \
             [ "${{ needs.test-vscode.result }}" != "success" ] || \
             [ "${{ needs.test-cli-linux.result }}" != "success" ] || \
             [ "${{ needs.test-cli-windows.result }}" != "success" ] || \
             [ "${{ needs.test-cli-macos.result }}" != "success" ] || \
             [ "${{ needs.test-e2e.result }}" != "success" ] || \
             [ "${{ needs.test-e2e-devices.result }}" != "success" ] || \
             [ "${{ needs.build-web.result }}" != "success" ] || \
             [ "${{ needs.build-desktop.result }}" != "success" ]; then
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "❌ **CI Pipeline Failed**" >> $GITHUB_STEP_SUMMARY
            exit 1
          else
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "✅ **All CI Checks Passed**" >> $GITHUB_STEP_SUMMARY
          fi

<!-- generated by worklist.py --publish; edit the worklist, not this file -->
# Work in 0914-1

### Tooling transformation: registry spine, gate ports, hook ports, region cutover

`PR-TASK: e87fa3ce`

- [x] `#043041d3` W10 P3-P5: tts docker context and R2 primitive relocation with exec shims for the cross-repo c ... LATEST: W10 P3-P5 landed and IT CORRECTED MY OWN FIX. I widened four media absence assertions to also 
- [x] `#043f24e2` INVESTIGATED: reported exec-bit outlier in .claude/hooks/pre-edit ... LATEST: NOT A DEFECT, dismissed with evidence after sweeping the class rather than the instance. A sub
- [x] `#06182358` DOOR: operator-only. check:ci-secret-reachability is now RED on purpose and cannot be cleared  ... LATEST: DEFAULT EXECUTED, a deliberate no-action: the gate stays RED, exit 1. door:operator-only. Evid
- [x] `#0d972047` Four wl_reggate coverage controls red in test-worklist-v5.sh, a regression from my own drain o ... LATEST: FIXED at .claude/hooks/stop/worklist-cases/06-regression-gate.sh:197 (and three sibling fixtur
- [x] `#15d4845a` GAP: organize .json files (8 root, 8 in .ci/config, 20 in scripts/data) with a predicate and a ... LATEST: closed at docs/ci-overhaul/08-driver-contract.md:253 by replacing an unfalsifiable requirement
- [x] `#1c30cc7d` W12 agent/ history: attested compaction records keyed on blob ids, check:ci-plan-record, why-o ... LATEST: W12 P1 landed and its registrations are IN. Spot-checked by running the gates rather than the 
- [x] `#1d57d620` FINDING+FIX: git pathspec .ci/**/*.sh silently skipped every script directly under .ci/, so tw ... LATEST: both gates fixed and re-run green: check-toolchain-pins.sh exit 0 over 495 files, check-go-too
- [x] `#1e5ce54d` FINDING from W3, in a single-writer file so it came to me: scripts/check-ci-parity.ts:178 reso ... LATEST: FIXED at scripts/check-ci-parity.ts:177 and the fix immediately paid for itself. Workspace res
- [x] `#26eb1933` GAP: package.json key budget, 307 script keys today and every registration box appends; shrink ... LATEST: landed check:ci-package-key-budget at scripts/check-package-key-budget.ts:1 with its baseline 
- [x] `#2e642173` FIXED IN THE SAME BREATH, recorded because the cause was mine: `git add -N` stages an intent-t ... LATEST: Fixed at the moment of finding, evidence above: git ls-files -s .claude/rediacc_hooks/*.py now
- [x] `#3838ff40` W5 .claude hooks to Python: shellscan transliteration, hook.py dispatcher, 47 guards as module ... LATEST: W5 P0+P1 landed. shellscan.py 30577 bytes and proc.py 12787 under .claude/rediacc_hooks/, nine
- [x] `#38c7201c` TWO LIVE GUARD BYPASSES found by the W5 port and fixed. ... LATEST: BOTH FIXED at .claude/hooks/pre-bash/block-long-sleep.sh:39 and .claude/hooks/pre-bash/block-r
- [x] `#3c6d14de` macOS toolchain finish: constants.sh needs SHFMT_SHA256_DARWIN_{AMD64,ARM64} and SHELLCHECK_SH ... LATEST: four darwin checksums recorded in .ci/config/constants.sh:255 and :270, finishing the macOS fi

### Secret namespace migration and the move to Bitwarden

`PR-TASK: 24c98380`

_no tracked items yet_

### Not in any epic

- [>] `#6d928fdf` MAIN-PLAN CAMPAIGN (f4da5c2e). Operator is away and asked for the rest of agent/PLAN-tooling-t ... LATEST: Judge correctly caught that isolation:worktree sub-agents don't conflict with the babysitter o

## SESSION f4da5c2e 2026-09-14T15:20:48Z

LEAD posture over `/pr-babysit bg` on branch `0914-1`, PR OPEN: https://github.com/rediacc/console/pull/589 (draft, head dad606dbd). Babysitter idle, watch armed (bg `bwymrpr02`), not touching the tree. Submodule PRs: rediacc/renet#111, rediacc/account#87. Per skill rule, I do not touch `gh`/CI myself until the babysitter reports green.

Pre-push lane at push: 380/383, 24/27 original reds fixed, 3 carried with verified-real reasons (secret-reachability door:operator-only on corrected evidence; gate-manifest local-timing artifact, deliberately not "fixed" on bad data; embed-asset-freshness genuinely blocked on amd64 CDN 504s). Babysitter expects `check:ci-embed-asset-freshness` and `check:ci-secret-reachability` to stay red in CI, the submodule-init path to be a genuine unknown (never exercised before this push), and `check:ci-gate-manifest` to go GREEN in CI (its duration cache is gitignored).

Worklist `#5baa9c88` (16 branch-sha citations -> blob ids): STARTED investigating, then STOPPED before editing. Found the premise doesn't fit: every one of the 16 is a commit-identity claim ("COMMITTED as X", "HEAD stayed X", "measured at X"), and `wl_planrec.py`'s resolver has no `tree` kind, only `blob`/`commit` -- a blob id cannot preserve what these sentences assert without rewording each one individually. Also found `agent/PLAN-tooling-transformation.md:3618`'s `aea2bc733552` isn't a commit sha at all (a code-dup fingerprint, unrelated to the 16). This is a real per-site rewrite, bigger than a quiet-window task; not touching it further without a dedicated pass. No edits landed from this thread.

## Next action
1. Nothing driver-only is currently actionable (PR is open, CI in flight, `#5baa9c88` correctly re-scoped as too large for now) -- if idle at the next stop, re-survey the campaign's other deferred driver-only boxes (B2 D1-D5 design-only, B4's fail-open-scoping half, C2, D4) for whether the babysitter's tree is quiet enough to start one, rather than just waiting.
2. CI run on PR #589 is in flight, watch armed on `bwymrpr02`. On the babysitter's next report (green, or an unlocal-visible red like the submodule-init path), verify claims in code before ruling. On green: one-time `gh` verification per the skill's rule, confirm debugging aids off and the 3 sanctioned-untouched paths untouched, report PR links to the operator.

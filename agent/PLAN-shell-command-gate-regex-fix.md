# PLAN: Fix the two dead-branch regex bugs in `check:ci-shell-commands` (bash twin + Python port + tests + ledger) (moved)
Status: moved
Moved-To: agent/plans/PLAN-shell-command-gate-regex-fix.md

This plan moved to `agent/plans/PLAN-shell-command-gate-regex-fix.md`. The stub keeps every citation of the old path resolving; a plan moves exactly once, at close. See agent/README.md for the layout.

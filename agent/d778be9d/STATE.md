## SESSION d778be9d 2026-09-22T04:46:02Z

# d778be9d STATE (branch 0914-1, PR #589; local only, no push, PR-TASK: e87fa3ce trailer on every commit)

## Where things stand
HEAD fce51e202 (103 plans moved into agent/plans/). Uncommitted, staged: writers N (7 twins+2 libs deleted, 2 wiring namers flipped, 9 workflow sparse-checkout cones fixed) and O (run-legacy.sh verb census: 5 live/7 dead/3 uncertain, ruling-7 widened to scripts/ops+drills+dev, 17 files/7244 lines frozen as debt), plus my own citation-repair work: fixed check_plan_citations.py's carried_lines() (unions the origin's own git history, not just branch-merge-base) closing worklist #94b1841e (337 findings to 0), added test_gate_plan_citations.py (4 cases). Then found and fixed a SECOND instance of the same defect class: check_plan_boxes.py's _added_plans() had the identical git-rename-blindness (a plan-folder move is an ADD+MODIFY pair, never a rename, at any -M threshold, because check_plan_folders.py --move leaves a stub rather than deleting). Factored moved_from() out of check_plan_citations.py into .ci/rediacc_ci/quality/plan_lifecycle.py as a shared helper; both gates now use it; added a regression test to test_gate_plan_boxes_never_delete.py. Final gate sweep running in scratchpad/gates13.txt (28 gates + 2 pytest files).

## Next action
1. Read scratchpad/gates13.txt (should show ALLDONE); fix any non-rc=0 row, then commit everything as ONE commit (writers N+O + both citation/plan-boxes fixes) with PR-TASK trailer and a shape_cluster_diff quote. Tick #94b1841e with the commit sha. Then run `check_plan_folders.py --move agent/plans/PLAN-ci-stale-plan-citations-repair.md` (now committed, so the move is allowed) in a small follow-up commit, since its Status: done means it belongs in agent/plans/_done/.
2. Then dispatch the next bash-endgame wave per operator answers: legacy-router port for the 5 live run-legacy.sh verbs (dev/worktree/quality are UNCERTAIN, investigate before porting), the scripts/ops+drills+dev port wave (17 files, 7244 lines, now frozen debt), .ci/docker/run-in-{web,render}.sh (needs a port from scratch), the 8 unported bash gate tests (port to pytest per operator default), then the 39 real-run rehearsal (needs operator/credentialed session, batched checklist first), then W1P6 deletes the language-policy baseline when every path is deleted or exempt.

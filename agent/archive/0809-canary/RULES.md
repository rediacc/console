# RULES: branch 0809-canary

**This is a THROWAWAY CANARY branch**, not a wave. It exists to give the
newly-armed autopilot its first live red (a planted shfmt+SC2034 defect in
check-npmrc.sh, PR #562). The AUTOPILOT owns this branch's pushes from here:
no human/session commits on it while the canary runs — a second pusher would
contaminate the experiment. Evaluate, then close the PR and delete the
branch. Do not sharpen this file; it dies with the branch.

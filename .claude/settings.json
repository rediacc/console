{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "bash -c 'CMD=$(jq -r \".tool_input.command\" 2>/dev/null); if echo \"$CMD\" | grep -qE \"^git commit|&& git commit|; git commit\"; then bash /home/muhammed/monorepo/console/scripts/pre-commit-check.sh; else exit 0; fi'"
          }
        ]
      }
    ]
  }
}

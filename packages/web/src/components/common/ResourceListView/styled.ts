import { Flex } from 'antd';
import { styled } from 'goober';

export const ResourceListContainer = styled(Flex)`
  width: 100%;
`;

export const ResourceListControlsRow = styled(Flex)`
  width: 100%;
`;

export const ResourceListControlsLeft = styled(Flex)`
  flex: 1;
  min-width: 0;
`;

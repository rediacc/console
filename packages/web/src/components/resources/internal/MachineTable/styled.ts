import { Flex, Space } from 'antd';
import { styled } from 'goober';

export const MachineTableRoot = styled(Space)`
  width: 100%;
`;

export const MachineTableContent = styled(Flex)`
  width: 100%;
  flex: 1;
  overflow: hidden;
`;

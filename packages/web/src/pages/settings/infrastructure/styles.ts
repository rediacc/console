import styled from 'styled-components';

export const StyledRediaccEmpty = styled.div`
  padding-top: ${({ theme }) => theme.spacing.XL * 2.5}px;
  padding-bottom: ${({ theme }) => theme.spacing.XL * 2.5}px;
`;

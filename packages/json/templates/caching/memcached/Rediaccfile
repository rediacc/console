#!/bin/bash

up() {
    if [ -f .env ]; then
        source .env
    fi

    echo "Starting Memcached server..."
    renet compose -- up -d
}

down() {
    echo "Stopping Memcached server..."
    renet compose -- down -v
}

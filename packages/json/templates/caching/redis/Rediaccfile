#!/bin/bash

up() {
    if [ -f .env ]; then
        source .env
    fi

    echo "Starting Redis server..."
    renet compose -- up -d
}

down() {
    echo "Stopping Redis server..."
    renet compose -- down -v
}

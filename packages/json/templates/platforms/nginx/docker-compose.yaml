services:
  nginx:
    image: nginx:latest
    container_name: ${CONTAINER_NAME}
    restart: always
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://$${SERVICE_IP:-localhost}:80 || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s

# Test outputs
reports/
test-results/

# Accidental tsc emit into the source tree (sources are .ts only)
src/**/*.js
src/**/*.d.ts
src/**/*.map
tests/**/*.js
tests/**/*.d.ts
tests/**/*.map

# Environment: the .env `rediacc_ci.env.create_e2e_env` generates (machine-local VM topology)
.env
.env.*

# Dependencies
node_modules/

---
import ResourceBriefPage from '../../../components/resources/ResourceBriefPage.astro';
import { getLanguagePaths } from '../../../i18n/language-utils';
import type { Language } from '../../../i18n/types';

export const getStaticPaths = getLanguagePaths;

const { lang } = Astro.params as { lang: Language };
---

<ResourceBriefPage lang={lang} category="ransomware" deckKey="ransomwareSurvival" />

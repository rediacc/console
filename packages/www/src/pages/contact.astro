---
// Redirect root /contact to /en/contact for clean language-aware architecture
import { DEFAULT_LANGUAGE } from '../i18n/language-utils';

return Astro.redirect(`/${DEFAULT_LANGUAGE}/contact`);
---

---
title: "إضافة خادمك الأول"
description: "سجّل خادمك الأول في rdc، وجهّزه، وافهم بنية rdc + renet."
category: "Tutorials"
subcategory: essentials
order: 3
language: ar
sourceHash: "09d6cde1ac066c85"
sourceCommit: "43aec6b89a55f69f994476d3a124e749d4d2223f"
---

import TutorialStep from '../../../components/tutorial/TutorialStep.astro';

# إضافة خادمك الأول

قبل إضافة خادم، من المفيد أن تفهم كيف تعمل `rdc`. تعتمد Rediacc على بنية من أداتين: `rdc` على حاسوبك، و`renet` على الخادم.

## شاهد الدرس التعليمي

![Tutorial: Adding your first server](/assets/tutorials/tutorial-add-server.cast)

## لماذا أداتان؟

![rdc on laptop, renet on server, SSH between](/img/tutorials/tutorial-add-server/slide-1.svg)

- **`rdc`** هي أداة CLI على حاسوبك. هنا تكتب أوامرك.
- **`renet`** هو المنسق على الخادم. يدير التشفير وDocker والعزل.

عندما تشغّل أمرًا محليًا، تتصل `rdc` عبر SSH وتُنفّذ `renet` على الخادم. لن تحتاج أبدًا للاتصال بخوادمك يدويًا عبر SSH؛ `rdc` تفعل ذلك نيابةً عنك.

## الخطوة 1: تسجيل الخادم

<TutorialStep number={1} />

لا حاجة لـ `rdc config init`. يُنشأ ملف الإعداد تلقائيًا عند الاستخدام الأول، وتستخدم `rdc` مفتاح `~/.ssh` العادي كخيار احتياطي.

## الخطوة 2: التجهيز

<TutorialStep number={2} />

خادمك جاهز الآن لاستضافة المستودعات.

## مكان حفظ الإعداد

تحتفظ أداة CLI بكل شيء في `~/.config/rediacc/rediacc.json`، ملف JSON نصي واحد بسيط:

<TutorialStep number={3} />

يحتوي هذا الملف الواحد على أجهزتك ومفتاح SSH وبيانات اعتماد التشفير. انسخه إلى حاسوب آخر وستكون مستعدًا للعمل من ذلك الجهاز أيضًا.

## إعدادات متعددة

يمكنك الاحتفاظ بعدة إعدادات، واحدة لكل بيئة (إنتاج، اختبار، تطوير)، والتنقل بينها باستخدام العلامة `--config` مع أي أمر. انظر [إعداد إعدادات متعددة](/ar/docs/setup#multiple-configs).

---

التالي: [إنشاء مستودعك الأول](/ar/docs/tutorial-create-repo).

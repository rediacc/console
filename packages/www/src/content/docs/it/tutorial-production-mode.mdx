---
title: "Modalità di Produzione"
description: "Esegui la tua app scollegata dal tuo laptop e sopravvivi ai riavvii del server con l'avvio automatico."
category: "Tutorials"
subcategory: advanced
order: 10
language: it
sourceHash: "6921482ea1810fb3"
sourceCommit: "43aec6b89a55f69f994476d3a124e749d4d2223f"
---

import TutorialStep from '../../../components/tutorial/TutorialStep.astro';

# Modalità di Produzione

Finora hai eseguito l'app con `renet dev up` dall'interno del repo. Ottimo per lo sviluppo. Per la produzione, gestisci tutto dal tuo laptop con `rdc`. Chiudi il laptop e l'app continua a girare.

## Guarda il tutorial

![Tutorial: Modalità di produzione](/assets/tutorials/tutorial-production-mode.cast)

## Sviluppo vs produzione

La differenza è semplice:

- `renet dev up` gira **all'interno del repo**. Devi essere connesso.
- `rdc repo up` gira **dal tuo laptop**. Nessuna connessione necessaria dopo.

Tre azioni ti portano da dev a prod:

![Ferma, avvia, avvio automatico](/img/tutorials/tutorial-production-mode/slide-1.svg)

Se hai ancora una sessione `renet dev up` in esecuzione dentro il repo, chiudila prima con `rdc vscode connect -m my-server -r my-app` seguito da `renet dev down`. Da quel momento in poi, tutto il resto viene gestito interamente dal tuo laptop.

## Passo 1: Avvia in modalita' di produzione

<TutorialStep number={1} />

`rdc repo up` chiama la stessa funzione `up` che chiamava `renet dev up`. Stesso `Rediaccfile`, modo diverso di invocarlo, solo che ora l'app continua a girare dopo che ti disconnetti.

## Passo 2: Abilita l'avvio automatico

<TutorialStep number={2} />

Se il server si riavvia, questo repo torna automaticamente.

## Passo 3: Elenca i repo con avvio automatico

<TutorialStep number={3} />

Una rapida verifica che la tua app sia nella lista di avvio.

## Passo 4: Ferma dal tuo laptop

<TutorialStep number={4} />

Un comando per avviare, uno per fermare. Tutto dal tuo laptop.

---

Successivo: [Backup e Ripristino](/it/docs/tutorial-backup-restore).

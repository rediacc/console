---
title: "Продакшен-режим"
description: "Запустите приложение независимо от ноутбука и настройте автозапуск после перезагрузки сервера."
category: "Tutorials"
tags:
  - operations
  - containers
subcategory: advanced
order: 13
language: ru
sourceHash: "10af6bb3d8138b86"
sourceCommit: "4c8e5f6dcaf6b6317e8e0f5cf3715f1a547e0c4e"
---

import TutorialStep from '../../../components/tutorial/TutorialStep.astro';

![Tutorial: Production mode](/assets/tutorials/tutorial-production-mode.cast)

# Продакшен-режим

До сих пор вы запускали приложение командой `renet dev up` изнутри репозитория. Для разработки это отлично. Для продакшена вы управляете всем с ноутбука через `rdc`. Закройте ноутбук: приложение продолжит работать.

## Dev против prod

Разница проста:

- `renet dev up` работает **внутри репозитория**. Нужно оставаться подключённым.
- `rdc repo up` работает **с ноутбука**. После этого соединение не нужно.

Три действия переводят вас из dev в prod:

![Stop, start, autostart](/img/tutorials/tutorial-production-mode/slide-1.svg)

Если у вас ещё запущена сессия `renet dev up` внутри репозитория, сначала завершите её командой `rdc vscode connect my-app`, затем `renet dev down`. После этого всё остальное управляется исключительно с ноутбука.

## Шаг 1: Запуск в продакшен-режиме

<TutorialStep number={1} />

`rdc repo up` вызывает ту же функцию `up`, что и `renet dev up`. Тот же `Rediaccfile`, другой способ вызова, но теперь приложение продолжает работать после отключения.

## Шаг 2: Включение автозапуска

<TutorialStep number={2} />

Если сервер перезагрузится, этот репозиторий поднимется автоматически.

## Шаг 3: Список репозиториев с автозапуском

<TutorialStep number={3} />

Быстрая проверка того, что ваше приложение есть в списке загрузки.

## Шаг 4: Остановка с ноутбука

<TutorialStep number={4} />

Одна команда для запуска, одна для остановки. Всё с ноутбука.

---

Далее: [Мониторинг](/ru/docs/tutorial-monitoring).

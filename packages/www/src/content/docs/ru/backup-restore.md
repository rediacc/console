---
title: "Резервное копирование и восстановление"
description: "Резервное копирование зашифрованных репозиториев двумя способами: адресуемое по содержимому чанковое хранилище, загружающее только измененные ячейки, или полная отправка в любое совместимое с rclone хранилище. Восстановление на любой машине и автоматизация с помощью именованных стратегий и таймеров systemd."
category: Guides
order: 7
language: ru
sourceHash: "df8a9d53f6991817"
sourceCommit: "522dceadb04b6a3e7f4ea60ac1e47308f6a1a600"
---

# Резервное копирование и восстановление

Rediacc создает резервные копии зашифрованных репозиториев во внешние хранилища и восстанавливает их на той же или другой машине. Резервные копии зашифрованы; для восстановления требуются учетные данные LUKS репозитория.

## Два пути резервного копирования

У Rediacc есть два независимых пути резервного копирования, и это руководство описывает оба. Они используют разное хранилище и разные команды, поэтому репозиторий, скопированный одним путем, не считается скопированным другим.

**Чанковое хранилище** (`rdc backup snapshot`) загружает образ репозитория ячейками фиксированного размера, адресуемыми по их содержимому. Первый запуск загружает весь ненулевой инвентарь; каждый последующий запуск загружает только измененные ячейки, определяемые по метаданным распределения файловой системы, а не чтением всего образа. Идентичные ячейки хранятся один раз для всех снимков и для всего семейства форков, а использование учитывается в счет вашей квоты хранилища (`rdc backup usage`).

**Отправка в хранилище отменена.** Раньше `rdc repo push --to <storage>` копировала весь файл резервной копии в совместимого с rclone провайдера, который вы регистрировали самостоятельно. Ветка rclone полностью удалена, и push, pull, list и restore теперь отклоняют указание хранилища и отсылают вас сюда. Передача между машинами не затронута: она никогда не шла через rclone.

Восстановление из чанкового хранилища работает: `rdc backup restore <repo> --at <snapshot-id>` материализует сохраненный снимок, и `--at` также принимает метку времени RFC 3339, которая разрешается относительно инвентаря снимков. Добавьте `--as <name>` для восстановления под другим именем и `--up` для последующей активации репозитория. Чанковое хранилище также дает вам загрузку (`rdc backup snapshot`), проверку (`rdc backup verify`, и `--deep` для повторного хеширования каждой ячейки вместо выборки), инвентарь снимков (`rdc backup manifests`) и квоту-бухгалтерию (`rdc backup usage`).

### Команды чанкового хранилища

```bash
# Загрузить снимок. Первый запуск создает начальные данные, последующие отправляют только измененные ячейки.
rdc backup snapshot my-app

# Спланировать без загрузки: сообщает, что переместилось бы.
rdc backup snapshot my-app --dry-run

# Не доверять локальному якорю и повторно загрузить весь инвентарь.
# Это заново загружает всё и повторно списывает квоту; используйте
# только когда якорь заведомо неисправен.
rdc backup snapshot my-app --reseed

# Проверить сохраненный инвентарь и вашу квоту.
rdc backup verify my-app
rdc backup manifests my-app
rdc backup usage
```

## Холодные снимки (`--cold`)

Холодный снимок останавливает репозиторий перед заморозкой, поэтому сохранённый образ согласован на уровне приложения, а не только при сбое. Команда выполняется на самой машине:

```bash
# Все репозитории в datastore по умолчанию.
sudo renet backup snapshot --cold

# Только названные репозитории. --repo принимает GUID репозитория и повторяется.
sudo renet backup snapshot --cold --repo <guid> --repo <guid>
```

`--cold` нельзя сочетать с `--dry-run`. Пробный прогон, который останавливает контейнеры, уже не пробный, а тот, что их не останавливает, не холодный, поэтому renet отклоняет такую пару вместо того, чтобы выбирать смысл за вас.

### Что делает холодный прогон

Для каждого выбранного репозитория, в таком порядке:

1. Остановить его контейнеры.
2. Сбросить на диск точку монтирования репозитория и datastore.
3. Убедиться, что контейнеры действительно остановились.
4. Сделать reflink образа репозитория по принципу копирования при записи.
5. Снова запустить контейнеры.

И только после этого начинается загрузка, когда все репозитории уже подняты.

Простой приходится на заморозку, а не на передачу. Reflink состоит только из метаданных, поэтому занимает одинаковое время, хранит ли репозиторий 1 GB или 100 GB. С загрузкой всё иначе: она растёт вместе с числом изменившихся байтов, а первый снимок отправляет весь ненулевой инвентарь. Если держать контейнеры выключенными до конца загрузки, простой окажется привязан к объёму данных, а при первой копии это часы вместо миллисекунд.

Все выбранные репозитории останавливаются в одном общем окне, а не по очереди. Это стоит чуть большего простоя на каждый репозиторий и взамен даёт единую точку согласованности для всего набора.

Репозиторий без запущенных контейнеров и так спокоен. Его снимок делается вообще без простоя, и это нормальный результат, а не сбой.

### Во что обходится простой

На реальной машине весь простой составил **222 мс**:

| Этап | Измерено | Что происходит |
|------|----------|----------------|
| `cold_down` | 64 мс | Контейнеры останавливаются |
| `cold_sync` | 26 мс | Точки монтирования репозиториев и datastore сбрасываются на диск |
| `cold_verify` | 31 мс | Подтверждается, что контейнеры остановлены |
| `cold_stage` | 0 мс | Reflink образа репозитория |
| `cold_up` | 99 мс | Контейнеры запускаются снова |

Основное время уходит на перезапуск контейнеров, а подготовка практически бесплатна: reflink не заметен даже с точностью до миллисекунды. Но читайте этот ноль вместе с записями по каждому репозиторию, а не отдельно. Прогон, отклонивший все репозитории, тоже сообщает `cold_stage=0ms`, и только записи говорят, какой из двух случаев перед вами.

Эта разбивка нужна как доказательство, а не как украшение. Ни один из пяти этапов не читает и не отправляет данные репозитория, поэтому ни один из них не растёт вместе с резервной копией. Растёт только загрузка, и она идёт уже после того, как простой закончился.

renet печатает те же цифры в конце прогона, чтобы вы измеряли свои машины, а не верили нашим:

```text
Cold backup: <n> repositories quiesced, outage 222ms (cold_down=64ms cold_sync=26ms cold_verify=31ms cold_stage=0ms cold_up=99ms)
```

JSON-запись каждого репозитория несёт тот же простой и те же этапы, так что позже холодный снимок отличается от горячего без догадок по времени.

### Когда выбирать холодный режим

Горячий режим используется по умолчанию и подходит большинству репозиториев. Горячий снимок согласован при сбое, то есть соответствует состоянию репозитория после отключения питания, и не стоит ни секунды простоя. Большинство баз данных и очередей восстанавливаются из такого состояния сами.

Холодный режим выбирают для данных, которые нельзя безопасно снять во время записи. Очевидный случай: база данных со своим журналом упреждающей записи и состоянием в памяти. Вы меняете короткий и измеренный простой на снимок, который приложение открывает без предварительного восстановления.

### От чего холодный прогон отказывается

Отказ здесь и есть функция. Резервная копия с ярлыком «холодная», которая на деле ничего не остановила, лжёт вам ровно до момента восстановления, поэтому renet никогда молча не понижает холодный прогон до горячего:

- **Контейнеры, которые не остановились.** После остановки renet спрашивает у Docker-сокета самого репозитория, работает ли там ещё что-нибудь. Если да, такой репозиторий отклоняется, а не снимается. Проверка решает в безопасную сторону: если до сокета не достучаться или список контейнеров не читается, остановка считается неподтверждённой, а неподтверждённая означает отказ.
- **Лицензия, которую не удаётся прочитать.** Лицензии проверяются до простоя, а не после, потому что репозиторий с нечитаемой лицензией всё равно ничего бы не загрузил. Такой репозиторий пропускают, не останавливая. Если ни у одного из выбранных репозиториев нет читаемой лицензии, весь прогон отклоняется ещё до того, как выключится хоть один контейнер.
- **Второй холодный прогон на том же datastore.** Блокировка охватывает весь datastore, и занятая блокировка отклоняется сразу, не остановив ничего. Два пересекающихся прогона останавливали бы контейнеры, которые каждый из них считает своими, а второй запускал бы репозитории, которые первый ещё замораживает. Пропустить прогон и дождаться следующего лучше.

Если прогон прервали, пока контейнеры выключены, командой `systemctl stop` или перезагрузкой, renet запускает их обратно перед выходом. Восстановление на машине служит подстраховкой: оно замечает холодную копию, чей владелец исчез, и поднимает эти репозитории.

## Настройка хранилища

Перед отправкой резервных копий зарегистрируйте провайдера хранилища. Rediacc поддерживает любое совместимое с rclone хранилище: S3, B2, Google Drive и многие другие.

### Импорт из rclone

Если у вас уже настроен удаленный ресурс rclone:

```bash
rdc storage import rclone.conf
```

Эта команда импортирует конфигурации хранилища из файла конфигурации rclone в текущую конфигурацию. Поддерживаемые типы: S3, B2, Google Drive, OneDrive, Mega, Dropbox, Box, Azure Blob и Swift.

### Просмотр хранилищ

```bash
rdc storage list
```

## Отправка резервной копии на другую машину

Скопируйте репозиторий на вторую машину по SSH:

```bash
rdc repo push my-app --to-machine server-1
```

Зашифрованный образ копируется с ТЕМ ЖЕ GUID, так что это резервная копия или миграция, а не форк. Чтобы получить независимую копию, сначала выполните `rdc repo fork`, а затем отправьте форк.

Для резервной копии на конкретный момент времени используйте вместо этого чанковое хранилище: `rdc backup snapshot my-app` загружает только изменённые ячейки, а `rdc backup restore my-app --at <snapshot>` возвращает любую из них.

| Опция | Описание |
|-------|----------|
| `--to-machine <machine>` | Целевая машина для копирования между машинами |
| `--dest <filename>` | Пользовательское имя файла назначения |
| `--checkpoint` | Создать контрольную точку CRIU перед отправкой (для контейнеров с меткой `rediacc.checkpoint=true`). Целевая машина автоматически восстанавливает при `repo up` |
| `--force` | Перезаписать существующую резервную копию |
| `--bwlimit <limit>` | Ограничение пропускной способности для rsync-передачи (например, `10M`, `500K`) |
| `--tag <tag>` | Пометить резервную копию тегом |
| `-w, --watch` | Отслеживать прогресс операции |
| `--debug` | Включить подробный вывод |
| `--skip-router-restart` | Пропустить перезапуск сервера маршрутов после операции |

## Получение резервной копии с другой машины

Верните репозиторий с машины, на которой он хранится:

```bash
rdc repo pull my-app --from-machine server-1
```

Чтобы вместо этого восстановить из чанкового хранилища, используйте
`rdc backup restore my-app --at <snapshot-id>`.

Команда pull отказывается перезаписывать репозиторий, который в данный момент **смонтирован**. Сначала отмонтируйте его, выполните pull, а затем снова поднимите командой `rdc repo up`. Исключение составляют репозитории на основе каталогов: они синхронизируются на месте даже в смонтированном состоянии.

| Опция | Описание |
|-------|----------|
| `--from-machine <machine>` | Исходная машина для восстановления между машинами |
| `--force` | Перезаписать существующую локальную резервную копию |
| `--bwlimit <limit>` | Ограничение пропускной способности для rsync-передачи (например, `10M`, `500K`) |
| `-w, --watch` | Отслеживать прогресс операции |
| `--debug` | Включить подробный вывод |
| `--skip-router-restart` | Пропустить перезапуск сервера маршрутов после операции |

## Список резервных копий

Выведите список снимков в чанковом хранилище:

```bash
rdc backup manifests my-app
```

Чтобы увидеть резервные копии, лежащие на машине:

```bash
rdc backup list -m server-1
```

Вывод перечисляет снимки, которые чанковое хранилище хранит для этого репозитория:

| Колонка | Значение |
|---|---|
| `Mode` | `hot` или `cold`. В какой папке запланированных резервных копий находится эта запись |
| `Name` | Имя репозитория, разрешённое из вашей локальной конфигурации (для репозиториев не из конфигурации используется GUID) |
| `GUID` | GUID репозитория на диске |
| `Size` | Удобочитаемый размер файла резервной копии |
| `Modified` | Метка времени UTC от хранилища |

Список хранилища отменён вместе с веткой rclone; команда отказывает и называет эти две замены.

### Что на самом деле означают hot и cold

`--mode hot` и `--mode cold` описывают, как обращаются с репозиторием во время создания резервной копии, а не то, куда попадают данные.

**Hot** снимает копию работающего репозитория. Контейнеры продолжают обслуживать запросы, а образ захватывается посреди записи, поэтому резервная копия согласована при сбое: именно то состояние, которое вы получили бы, если бы у машины в этот момент пропало питание. Это подходит для всего, что восстанавливается из собственного журнала, то есть для большинства баз данных.

**Cold** сначала останавливает контейнеры, сбрасывает данные на диск, убеждается, что они остановлены, замораживает образ и только потом запускает их снова. Это стоит реального простоя, но этот простой представляет собой замораживание постоянной длительности, а не передачу, и результат согласован на уровне приложения.

Оба режима пишут в одно и то же чанковое хранилище. Ячейки адресуются по содержимому, поэтому репозиторий, для которого делаются и почасовые hot-снимки, и еженедельные cold-снимки, хранит общие блоки один раз, а не дважды, и семейство форков тоже разделяет их между собой. Использование учитывается в счёт вашей квоты командой `rdc backup usage`.

## Синхронизация по одному репозиторию за раз

Push и pull работают с одним репозиторием, заданным через ref (`name`, `name:tag` или `name@machine`). Формы "все репозитории сразу" нет: выполняйте команду для каждого репозитория отдельно.

### Отправка на другую машину

```bash
rdc repo push shop@server-1 --to-machine server-2
```

### Получение с другой машины

```bash
rdc repo pull shop@server-1 --from-machine server-2
```

| Опция | Описание |
|--------|-------------|
| `--to-machine <machine>` | Целевая машина для отправки между машинами |
| `--from-machine <machine>` | Исходная машина для получения между машинами |
| `--force` | Перезаписать существующую резервную копию или репозиторий |
| `--checkpoint` | Создать контрольную точку CRIU перед отправкой (только отправка) |
| `--up` | Смонтировать и развернуть репозиторий после получения (только получение) |
| `--bwlimit <limit>` | Ограничение пропускной способности для rsync-передачи (например, `10M`) |
| `--delta-base <guid>` | Передавать только изменённые блоки относительно неизменяемого базового GUID |
| `--debug` | Включить подробный вывод |
| `--skip-router-restart` | Пропустить перезапуск сервера маршрутов после операции |

## Запланированное резервное копирование

Rediacc использует именованные стратегии резервного копирования. Каждая стратегия определяет расписание, режим резервного копирования, необязательное ограничение пропускной способности и фильтры файлов. Машины привязываются к стратегиям по имени, чтобы определить, какие резервные копии на них выполняются.

### Режимы резервного копирования

| Режим | Поведение | Простой |
|-------|-----------|---------|
| `hot` | Снимок BTRFS создается во время работы сервисов (согласованность при сбое) | Отсутствует |
| `cold` | Сервисы остановлены, снимок сделан, сервисы перезапущены, снимок загружен (согласованность на уровне приложения) | Окно остановки+запуска для каждого репозитория, распараллеленное между репозиториями. См. "Оценка простоя при холодном резервном копировании" ниже. |

Используйте `hot` для сервисов, допускающих снимки с согласованностью при сбое. Используйте `cold`, когда нужна гарантированная согласованность и допустим кратковременный перезапуск.

### Семантика холодного резервного копирования

Холодное резервное копирование выполняется в трех фазах для каждого включенного репозитория: **остановка, снимок, запуск**. Понимание границ гарантий помогает операторам своевременно обнаруживать частичные сбои.

**Что гарантирует холодное резервное копирование:**

- Перед снимком каждый работающий контейнер в каждом включенном репозитории корректно останавливается через хук `down()` Rediaccfile, а Docker daemon репозитория переводится в состояние покоя. Таким образом, снимок согласован на уровне приложения, а не только при сбое.
- Набор ID контейнеров, работавших до снимка, сохраняется в файл-sidecar по пути `/var/run/rediacc/cold-backup-<guid>.running.json`. Это источник истины о том, какие контейнеры должны быть запущены после завершения.
- После снимка вызывается хук `up()` Rediaccfile репозитория для восстановления полного compose-стека.
- Файл-sidecar статуса выполнения по пути `/var/run/rediacc/cold-backup-<guid>.status.json` фиксирует фазу, результат и ошибки каждой попытки.

**Что холодное резервное копирование НЕ гарантирует:**

- `up()` выполняется по принципу "наилучших усилий". Он может завершиться неудачей по причинам, не зависящим от холодного резервного копирования (условие `depends_on: service_healthy` ещё ожидает, синтаксическая ошибка в файле compose, временный сбой сети при загрузке образа). При сбое холодное резервное копирование логирует ошибку на уровне error, записывает файл-sidecar статуса и переходит к следующему репозиторию.
- Если `up()` завершается неудачей, активируется **прямой запасной перезапуск**: читается running-sidecar, и каждый записанный ID контейнера перезапускается напрямую через Docker API (без compose). Это возвращает сервисы в рабочее состояние даже при проблемах с compose-потоком, хотя без повторного выполнения хуков Rediaccfile.
- Если даже запасной вариант не срабатывает для некоторых ID контейнеров (например, сам Docker daemon недоступен), sidecar **остается на месте**, чтобы watchdog маршрутизатора мог продолжать попытки при каждом тике.

**Восстановление через watchdog:** при каждом тике watchdog проверяет наличие running-sidecar. Любой ID контейнера из списка, который в данный момент остановлен, перезапускается *независимо от сохраненного `restart_policy` контейнера*. Это означает, что сервисы с `restart: on-failure` (которые Docker НЕ перезапустит после чистой остановки) всё равно восстанавливаются после холодного резервного копирования. Как только все перечисленные контейнеры запущены, sidecar удаляется.

**Как операторы обнаруживают сбои:**

- `rdc machine status <machine> --containers` показывает состояние выполнения. Сравните с ожидаемым набором.
- `/var/run/rediacc/cold-backup-<guid>.status.json` на машине. Проверьте через `rdc term connect <repo> -c "cat /var/run/rediacc/cold-backup-$GUID.status.json"`. `success: false` с устаревшим `startedAt` означает, что последнее резервное копирование не завершилось корректно.
- Логи запуска резервного копирования renet (`journalctl -u renet-*` или прямой вызов `rdc backup schedule`) содержат итоговую строку вида `Cold backup: post-snapshot restart summary total=N compose_ok=N fallback_ok=N failed=N failed_repos=[...]`. Непустой `failed_repos` является целевым шаблоном для grep.

### Оценка простоя при холодном резервном копировании

Каждый репозиторий остаётся недоступен только на время собственного окна `down()` + `up()`. На прогретом хосте эти значения обычно такие:

| Тип репозитория | Типичное время остановки+запуска |
|-----------------|----------------------------------|
| Малый (1-2 контейнера, без БД) | 5-15 с |
| Средний (веб-приложение + кэш) | 20-45 с |
| Тяжёлый (БД + очереди + почта) | 60-120 с |

Шаг снимка (`btrfs subvolume snapshot -r`) выполняется за O(1) независимо от размера репозитория: 0,1-1 с. Репозиторий не остаётся остановленным во время снятия снимков других репозиториев. Загрузчик затем работает с доступным только для чтения снимком, пока все репозитории уже снова запущены.

**Общее время выполнения всего прогона** определяется тем, сколько репозиториев перезапускается параллельно. renet выводит это значение из параметров хоста:

```text
concurrency = min(repoCount, max(2, NumCPU/2), 8)
```

Примеры:

| Хост | Репозитории | Параллелизм | Время перезапуска |
|------|-------------|-------------|-------------------|
| ВМ с 4 CPU | 5 репозиториев, в среднем 30 с каждый | 2 | ~75 с |
| Сервер с 16 CPU | 10 репозиториев, в среднем 40 с каждый | 8 | ~80 с |
| Узел флота с 64 CPU | 50 репозиториев, в среднем 40 с каждый | 8 | ~4 мин |

**Переопределение через переменную окружения:** установите `REDIACC_COLD_BACKUP_CONCURRENCY=N` в окружении сервиса резервного копирования (обычно через drop-in systemd), чтобы зафиксировать конкретное значение. Значение `=1` принудительно включает строго последовательные перезапуски, что полезно при отладке цикла крашей в хуке `up()` одного из репозиториев.

Если у вас есть репозиторий, чувствительный к задержкам (публичное веб-приложение, почта), его простой ограничен собственным окном остановки+запуска (обычно 30-90 с), а не общей длительностью прогона. Репозитории распределяются по слотам параллелизма в порядке их обнаружения; очереди приоритетов нет. Разделите тяжёлые репозитории в отдельные стратегии с ограничением через `--exclude`, если нужна более тонкая настройка расписания.

### Длительные резервные копии и перекрывающиеся расписания

Холодное резервное копирование, длящееся дольше собственного интервала расписания (например, первичный сид репозитория на 500 ГБ через умеренный канал может легитимно потребовать более 24 ч, в течение которых ночной таймер срабатывает снова), не ставит в очередь и не запускает второй прогон. Юнит systemd `Type=oneshot` является единственным экземпляром: когда таймер срабатывает, а сервис уже `activating`, systemd объединяет запуск с существующей задачей. Новый процесс не запускается, прогон не ставится в очередь на потом.

Конкретно, прогон, начатый в понедельник в 03:00 UTC и завершившийся в четверг в полдень:

| День | Срабатывание в 03:00 UTC | Результат |
|------|--------------------------|-----------|
| Понедельник | Первое срабатывание | Прогон начинается |
| Вторник | Второе срабатывание | Молча отброшено (предыдущий прогон всё ещё активен) |
| Среда | Третье срабатывание | Молча отброшено (предыдущий прогон всё ещё активен) |
| Четверг | Прогон завершается в полдень | Нет догона; следующий прогон в пятницу 03:00 UTC |

Директива `Persistent=true` таймера **не** спасает эти срабатывания. `Persistent=true` воспроизводит срабатывания, пропущенные потому, что сам таймер был неактивен (система выключена, таймер отключен). Срабатывания, отброшенные потому, что сервис был занят, теряются.

Это поведение по умолчанию преднамеренно. Запуск двух холодных резервных копий параллельно против одного и того же datastore будет конкурировать за путь снимка BTRFS, удалённое хранилище rclone и файлы-sidecars репозиториев в `/var/run/rediacc/cold-backup-<guid>.status.json`. Сериализация за длительным прогоном является безопасным исходом.

**Следствия для мониторинга.** Зависший бэкап (например, rclone, заклинивший на сетевой чёрной дыре) молча отбрасывает каждое последующее срабатывание таймера. Планировщик не выдаёт тревог. Следите за `systemctl show <unit> -p ActiveEnterTimestamp`: если сервис находится в состоянии `activating` дольше ожидаемой длительности прогона (например, более 48 ч на ночном таймере), проведите расследование.

**Если вам нужно, чтобы каждое запланированное срабатывание выполнилось**, переключите таймер с `OnCalendar=<cron>` на `OnUnitInactiveSec=<интервал>`. Это срабатывает через N часов после завершения предыдущего прогона вместо фиксированного расписания по настенным часам, так что длительные прогоны не вызывают потерь. Они просто смещают следующий прогон. Компромисс заключается в дрейфе расписания: ваш ночной 03:00 становится "24 ч после завершения последнего".

### Снимки, прерывания и пространство пула

Каждая операция push работает с моментальным снимком хранилища, поэтому загружаемые данные согласованы, даже пока репозитории продолжают выполнять запись. Пока идёт резервное копирование, этот снимок продолжает ссылаться на каждый блок, разделяемый с живыми репозиториями: удаления и [операции trim](/ru/docs/repositories#освобождение-пространства-trim) освобождают меньше пространства в пуле до завершения цикла и удаления снимка. В [отчёте о состоянии хранилища](/ru/docs/monitoring#состояние-хранилища) отображается, сколько пространства в данный момент удерживают снимки резервных копий.

Прерывания безопасны. Остановка службы (или перезагрузка машины) заставляет резервное копирование прервать передачу и удалить снимок перед завершением; следующий плановый запуск продолжит с того места, где остановился, поскольку неизменённые файлы пропускаются по контрольной сумме. Если процесс завершён слишком жёстко для выполнения очистки (отключение питания), осиротевший снимок обнаруживается и автоматически удаляется обслуживающей службой хранилища в течение нескольких минут.

### Определение стратегии

Стандартная конфигурация по умолчанию разделяет на две стратегии: быстрый почасовой hot-поток, охватывающий каждый репозиторий, и более медленный еженедельный cold-поток, который приостанавливает контейнеры ради согласованных на уровне приложения снимков. Оба пишут в одно и то же чанковое хранилище, и общие блоки хранятся один раз, а не отдельно для каждого потока.

```bash
rdc backup strategy set hourly-hot \
  --destination rediacc \
  --cron "0 * * * *" \
  --mode hot \
  --bwlimit 20M \
  --enable
```

```bash
rdc backup strategy set weekly-cold \
  --destination rediacc \
  --cron "15 3 * * 0" \
  --mode cold \
  --exclude very-large-repo \
  --enable
```

Фильтр `--exclude` в cold-стратегии - рекомендуемый запасной вариант для очень больших репозиториев, не помещающихся в ваше еженедельное окно обслуживания. Почасовая hot-стратегия по-прежнему их охватывает; cold их просто пропускает. Имена репозиториев в `--exclude` совпадают с именем репозитория в локальной конфигурации (без `:tag`).

| Опция | Описание |
|-------|----------|
| `<strategy>` (позиционный) | Имя стратегии (используется для привязки к машине) |
| `--destination <storage>` | Провайдер хранилища для загрузки |
| `--cron <expression>` | Cron-выражение (например, `"0 2 * * *"` для ежедневного запуска в 2 часа ночи) |
| `--mode <hot\|cold>` | Режим резервного копирования |
| `--bwlimit <limit>` | Ограничение пропускной способности для загрузок (например, `10M`) |
| `--include <pattern>` | Фильтр включения (можно указывать несколько раз) |
| `--exclude <pattern>` | Фильтр исключения (можно указывать несколько раз) |
| `--enable` / `--disable` | Включить или отключить стратегию |

### Просмотр стратегий

```bash
rdc backup strategy list
rdc backup strategy show weekly-cold
```

### Удаление стратегии

```bash
rdc backup strategy remove weekly-cold
```

### Привязка стратегий к машине

В конфигурации привяжите одно или несколько имен стратегий к машине:

```json
{
  "machines": {
    "hostinger": {
      "backupStrategies": ["hourly-hot", "weekly-cold"]
    }
  }
}
```

> **Привязка действует только в локальной конфигурации.** Определение стратегии и её привязка к машине не вносят изменений на саму машину. Выполните `rdc backup schedule -m <machine>` (см. [Развертывание расписания на машине](#развертывание-расписания-на-машине)), чтобы развернуть таймеры systemd, и повторите команду после любого изменения стратегии или привязки.

## Выбор горячего или холодного режима и фильтрация по репозиториям

### Горячий и холодный: кратко

| | Горячий | Холодный |
|---|---------|---------|
| **Согласованность** | При сбое (снимок BTRFS во время работы) | На уровне приложения (остановка, снимок, запуск) |
| **Простой** | Отсутствует | Окно остановки+запуска для каждого репозитория (обычно 5-120 с) |
| **Подходящая частота** | Высокая (например, почасово) | Низкая (например, ежедневно или еженедельно) |
| **Типичное использование** | Частая страховочная сетка | Запланированное резервное копирование с гарантированной согласованностью |

**Горячий** - правильный выбор по умолчанию для высокочастотных прогонов. Сервисы продолжают работать во время снимка, поэтому окно резервного копирования не прерывает пользователей. Снимок согласован при сбое: он эквивалентен тому, что вы получите после некорректного выключения. Для большинства современных баз данных и очередей сообщений это приемлемо.

**Холодный** уместен, когда вам нужен гарантированный согласованный на уровне приложения снимок и вы можете принять кратковременный перезапуск репозитория. Сервисы останавливаются перед снимком и перезапускаются до начала загрузки, поэтому медленная или неудавшаяся загрузка никогда не продлевает окно простоя. Полную модель гарантий см. в разделе [Семантика холодного резервного копирования](#семантика-холодного-резервного-копирования).

### Фильтрация репозиториев по стратегии

Каждая стратегия может содержать фильтры `--include` и `--exclude`. Репозитории, имена которых совпадают с шаблоном `--exclude`, пропускаются для этой стратегии; `--include` ограничивает прогон только этими именами. Фильтры совпадают с именем репозитория в локальной конфигурации (без `:tag`).

```bash
# Горячая стратегия: резервное копирование всего ежечасно
rdc backup strategy set hourly-hot \
  --destination rediacc \
  --cron "0 * * * *" \
  --mode hot \
  --bwlimit 6M \
  --enable

# Холодная стратегия: резервное копирование всего еженедельно, исключая большой производный набор данных
rdc backup strategy set weekly-cold \
  --destination rediacc \
  --cron "15 3 * * 0" \
  --mode cold \
  --exclude analytics-demo \
  --enable
```

### Когда исключать репозиторий из частой горячей стратегии

Исключите репозиторий из высокочастотного прогона, если:

- Репозиторий большой и **полностью воспроизводим** из исходных данных, уже находящихся на томе, поэтому каждая почасовая резервная копия расходует значительную полосу пропускания без ощутимой ценности для восстановления.
- Прогон резервного копирования превысит собственный интервал расписания при доступной скорости загрузки.

**Пример.** Репозиторий `analytics-demo` содержит около 114 ГБ производных таблиц Postgres, которые можно полностью пересоздать из необработанных CSV-файлов, уже хранящихся на том же томе. При ограничении загрузки 6 МБ/с одна горячая резервная копия этого репозитория занимает более 5 часов. Ежечасный запуск означает, что каждый прогон всё ещё выполняется, когда запускается следующий, что приводит к молчаливому сбросу каждого последующего прогона (см. [Длительные резервные копии и перекрывающиеся расписания](#длительные-резервные-копии-и-перекрывающиеся-расписания)). Исключение из `hourly-hot` и сохранение в `weekly-cold` означает, что резервное копирование выполняется раз в неделю, а не никогда.

> **Если данные полностью воспроизводимы**, подумайте, нужно ли вообще делать их резервную копию. Альтернатива: создавать резервную копию только необработанных исходных данных (CSV-файлов в данном примере) и полностью пропустить производную копию. Еженедельная холодная резервная копия исходных данных намного меньше и полностью достаточна для восстановления.

Репозиторий, не исключённый ни одной из стратегий, охватывается обеими: у него есть почасовые снимки, согласованные при сбое, и еженедельный снимок, согласованный на уровне приложения. `rdc backup manifests <repo>` показывает их вместе, а общие для них блоки хранятся один раз.

## Операции резервного копирования

### Развертывание расписания на машине

Отправьте привязанные стратегии на машину в виде systemd-таймеров:

```bash
rdc backup schedule -m server-1
rdc backup schedule -m server-1 --dry-run
```

Развёртывание является реконсилятором состояния. Оно читает текущие юнит-файлы и состояние systemd на машине, сравнивает их с тем, что произвела бы конфигурация (SHA-256 на файл), и трогает только те юниты, содержимое которых действительно изменилось. Повторный запуск без изменений конфигурации не производит действий: нет записей, нет `daemon-reload`, нет колебаний таймеров.

`--dry-run` печатает план для каждой стратегии (`created`, `updated (service, timer, env)`, `unchanged`, `removed`), не трогая машину. В сочетании с `--debug` также печатаются сгенерированные тела юнитов; токены rclone редактируются.

Если для стратегии, которую вы собираетесь обновить или удалить, в настоящий момент выполняется резервное копирование, развёртывание завершается с ошибкой, предлагая отменить его или передать `--force`. С `--force` выполняющийся вызов сохраняет свой юнит в памяти, а новая конфигурация применяется при следующем срабатывании таймера, поэтому выполняющееся резервное копирование никогда не прерывается принудительно.

`--reset-failed` является опциональным флагом. Когда он передан, после успешного развёртывания сбрасывается состояние failed на затронутых сервисах. По умолчанию выключен, чтобы предыдущие сигналы об ошибках оставались видимыми для оповещений.

### Запуск резервного копирования сейчас

Запустить резервное копирование немедленно, не дожидаясь таймера. Работает даже без развернутых таймеров, используя `systemd-run` для разовых запусков:

```bash
rdc backup run -m server-1
rdc backup run weekly-cold -m server-1
```

### Просмотр статуса резервного копирования

Отображает текущий статус таймеров резервного копирования и результаты последних задач:

```bash
rdc backup status -m server-1
rdc backup status hourly-hot -m server-1
```

### Отмена выполняющегося резервного копирования

```bash
rdc backup cancel -m server-1
rdc backup cancel weekly-cold -m server-1
```

## Миграция репозитория

Перенос репозитория с одной машины на другую:

```bash
rdc repo migrate my-app@server-1 --to server-2
```

| Опция | Описание |
|-------|----------|
| `<ref>` (позиционный) | Ссылка на репозиторий для переноса; её часть `@machine` задаёт источник |
| `--to <place>` | Целевая машина или кластер |
| `--provision` | Подготовить репозиторий на целевой машине перед переносом |
| `--checkpoint` | Создать контрольную точку CRIU перед миграцией |
| `--skip-dns` | Пропустить обновление DNS-записей после миграции |
| `--bwlimit <limit>` | Ограничение пропускной способности для передачи (например, `50M`) |

Миграция передает зашифрованные данные репозитория через rsync. Исходный репозиторий остается нетронутым, пока вы явно его не удалите.

## Обзор хранилища

`rdc storage browse` и `rdc storage import` являются исключением из этой отмены: они запускают ваш собственный rclone из PATH, а не встроенную копию, и остаются способом прочитать архив, записанный до этого изменения.

```bash
rdc storage browse my-storage
```

Просмотр доступен только для чтения. Отправка в хранилище, получение из него и его список отменены; каждая из этих команд отказывает и называет заменяющую её команду чанкового хранилища.

## Лучшие практики

- Настройте ежедневное холодное резервное копирование для согласованных на уровне приложения снимков критически важных данных
- Используйте горячее резервное копирование для высокочастотных снимков с нулевым временем простоя
- Периодически проверяйте восстановление для подтверждения целостности резервных копий
- Используйте несколько провайдеров хранилищ для критически важных данных (например, S3 + B2)
- Храните учетные данные в безопасности; резервные копии зашифрованы, но для восстановления требуются учетные данные LUKS

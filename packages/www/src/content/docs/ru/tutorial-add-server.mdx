---
title: "Добавление первого сервера"
description: "Зарегистрируйте первый сервер в rdc, подготовьте его и разберитесь в архитектуре rdc + renet."
category: "Tutorials"
subcategory: essentials
order: 3
language: ru
sourceHash: "09d6cde1ac066c85"
sourceCommit: "43aec6b89a55f69f994476d3a124e749d4d2223f"
---

import TutorialStep from '../../../components/tutorial/TutorialStep.astro';

# Добавление первого сервера

Прежде чем добавить сервер, полезно понять, как работает `rdc`. Rediacc построен на двух инструментах: `rdc` на вашем ноутбуке и `renet` на сервере.

## Смотреть урок

![Tutorial: Adding your first server](/assets/tutorials/tutorial-add-server.cast)

## Зачем два инструмента?

![rdc on laptop, renet on server, SSH between](/img/tutorials/tutorial-add-server/slide-1.svg)

- **`rdc`**: CLI на вашем ноутбуке. Здесь вы вводите команды.
- **`renet`**: оркестратор на сервере. Он управляет шифрованием, Docker и изоляцией.

Когда вы выполняете команду локально, `rdc` подключается по SSH и запускает `renet` на сервере. Вам никогда не придётся вручную заходить на серверы по SSH: `rdc` сделает это за вас.

## Шаг 1: Регистрация сервера

<TutorialStep number={1} />

`rdc config init` не требуется. Файл конфигурации создаётся автоматически при первом использовании, а `rdc` использует ваш стандартный ключ `~/.ssh`.

## Шаг 2: Подготовка сервера

<TutorialStep number={2} />

Сервер готов к размещению репозиториев.

## Где хранится конфигурация

CLI хранит всё в `~/.config/rediacc/rediacc.json`: единый файл в формате обычного JSON.

<TutorialStep number={3} />

В этом одном файле хранится всё: машины, SSH-ключ и учётные данные шифрования. Скопируйте его на другой ноутбук, и вы сможете управлять теми же серверами с него.

## Несколько конфигураций

Вы можете хранить несколько конфигураций, по одной на каждую среду (production, staging, dev), и переключаться между ними с помощью флага `--config` в любой команде. См. [настройка нескольких конфигураций](/ru/docs/setup#multiple-configs).

---

Далее: [Создание первого репозитория](/ru/docs/tutorial-create-repo).

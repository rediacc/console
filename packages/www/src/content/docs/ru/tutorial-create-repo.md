---
title: "Создание первого репозитория"
description: "Создайте зашифрованный репозиторий на сервере и откройте его в VS Code."
category: "Tutorials"
subcategory: essentials
order: 4
language: ru
sourceHash: "1294b0494f20671b"
---

# Создание первого репозитория

Репозиторий Rediacc представляет собой один зашифрованный файл на сервере. После монтирования он становится папкой с собственным Docker-демоном и данными приложения: полностью изолированной и полностью переносимой.

Представьте его как USB-накопитель для продакшена: в покое это файл, а при запуске готовый сервер.

## Смотреть урок

![Tutorial: Creating your first repository](/assets/tutorials/tutorial-create-repo.cast)

## Файл на диске, среда после монтирования

![Encrypted file mounts as an isolated folder](/img/tutorials/tutorial-create-repo/slide-1.svg)

На диске репозиторий хранится как один зашифрованный образ. После монтирования вы получаете:

- Выделенный Docker-демон (отдельный от хостового)
- Данные приложения внутри зашифрованного тома
- Loopback IP-адреса, не конфликтующие ни с чем на сервере

Репозитории переносимы. Вы можете перемещать их между машинами, делать резервные копии или мгновенно форкать. Каждый репозиторий изолирован от всех остальных репозиториев на том же сервере.

## Создание репозитория

```bash
time rdc repo create --name my-app -m my-server --size 2G
```

Эта команда создаёт на `my-server` зашифрованный репозиторий объёмом 2 ГБ. Проверьте результат:

```bash
time rdc repo list -m my-server
```

## Открытие в VS Code

```bash
rdc vscode connect -m my-server -r my-app
```

VS Code откроется прямо внутри репозитория. Обратите внимание: рабочее пространство пустое. Это ваша изолированная среда. Всё, что вы создаёте здесь, хранится внутри зашифрованного тома и недоступно для других репозиториев на том же сервере.

---

Далее: [Развёртывание первого приложения](/en/docs/tutorial-deploy-app).

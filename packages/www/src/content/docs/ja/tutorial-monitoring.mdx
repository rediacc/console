---
title: "モニタリング"
description: "rdc machine コマンドでラップトップからサーバーのヘルス状態とリポジトリのステータスをリアルタイムに確認します。"
category: "Tutorials"
subcategory: advanced
order: 14
language: ja
sourceHash: "15d2bdf94f4077b0"
sourceCommit: "3fb35b9a33c7e8ec6753ecd56231f2018e8f4803"
---

import TutorialStep from '../../../components/tutorial/TutorialStep.astro';

# モニタリング

アプリがデプロイされ、バックアップも取られています。次はサーバーを健全に保つことが大切です。それが `rdc` の役割です。`rdc` ならラップトップから任意のサーバーの全体像（ヘルス、コンテナ、リポジトリ）を把握できます。

## チュートリアル動画

![チュートリアル: モニタリング](/assets/tutorials/tutorial-monitoring.cast)

## 確認できる3つのこと

![ヘルス、コンテナ、リポジトリ](/img/tutorials/tutorial-monitoring/slide-1.svg)

## ヘルス: システム情報

<TutorialStep number={1} />

システムの稼働時間、ディスク使用量、ストレージの状態が表示されます。問題があれば教えてくれます。

## コンテナ

<TutorialStep number={2} />

各コンテナの名前、状態、ヘルス、CPU、メモリ、さらにどのリポジトリが所有しているかが表示されます。

## リポジトリ

<TutorialStep number={3} />

各リポジトリのサイズ、マウント状態、Docker の状態、ディスク使用量が表示されます。

## すべてを一度に確認する

<TutorialStep number={4} />

フィルターなしで `query` コマンドを実行すると、全体像（システム情報、リポジトリ、コンテナ）が表示されます。`--system`、`--containers`、`--repositories`、`--services`、`--network`、`--block-devices` を追加して、特定のセクションに絞り込むことができます。

## ローカルの動作確認

`rdc doctor` は特定のサーバーとは無関係に、ローカルのセットアップ（Node、SSH キー、`renet`、Docker）を確認します。

<TutorialStep number={5} />

## 基礎が揃いました

インストール、設定、デプロイ、フォーク、本番公開、オートスタート、バックアップ、モニタリング。すべてターミナルから、すべて自分のサーバーで行えます。

次は応用トラックです。コードのようにリポジトリをバージョン管理します。

---

次: [ブランチ: コミットとロールバック](/ja/docs/tutorial-branching)

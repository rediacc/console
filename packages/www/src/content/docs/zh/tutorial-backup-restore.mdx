---
title: "备份与恢复"
description: "将仓库推送到外部存储，在需要时于新服务器上恢复。"
category: "Tutorials"
subcategory: advanced
order: 11
language: zh
sourceHash: "c13a0f880039d7e7"
sourceCommit: "3fb35b9a33c7e8ec6753ecd56231f2018e8f4803"
---

import TutorialStep from '../../../components/tutorial/TutorialStep.astro';

# 备份与恢复

你的应用已在生产环境运行。立即备份。`rdc` 可以将整个仓库（应用、数据库、文件、配置）推送到外部存储，随时拉取回来。应对勒索软件、硬件故障，万无一失。

## 观看教程

![Tutorial: Backup and restore](/assets/tutorials/tutorial-backup-restore.cast)

## 三个步骤

![配置、推送、恢复](/img/tutorials/tutorial-backup-restore/slide-1.svg)

1. **配置**存储提供商。
2. **推送**备份。
3. 需要时**恢复**。

## 第一步：配置存储

你需要一个 `rclone` 配置文件。如果你已在使用 rclone，可以直接导入：

<TutorialStep number={1} />

支持 S3、B2、Google Drive、Dropbox 等众多存储服务。验证已配置的内容：

<TutorialStep number={2} />

## 第二步：推送备份

<TutorialStep number={3} />

整个仓库（应用、数据库、文件，一切）都已备份。由于仓库本身是加密的，备份也是加密的。无需额外的密钥管理。

随时列出备份：

<TutorialStep number={4} />

## 为什么没有停机时间？

备份上传时应用保持运行。这是如何保证一致性的？

原理与 [fork](/zh/docs/tutorial-forking) 相同。`rdc` 先 fork，然后上传 fork。fork 捕获了那一刻的状态，你的线上应用继续运行。无停机，无不一致。

## 第三步：恢复（真正的演练）

从未实际恢复过的备份只是一种期望，不算真正的备份。先将仓库下线：

<TutorialStep number={5} />

直接从存储拉取备份：

<TutorialStep number={6} />

然后重新挂载，完全恢复：

<TutorialStep number={7} />

同样的 pull 命令在全新服务器上同样有效：设置好服务器，将其添加到 `rdc`，然后在那台机器上执行 pull：

```bash
rdc repo pull --name my-app -m new-server --from my-storage
rdc repo up --name my-app -m new-server
```

相同的数据，相同的容器，不同的机器。

## 更快的备份：机器到机器

你也可以直接在机器之间推送，无需经过云存储：

```bash
rdc repo push --name my-app -m my-server --to-machine backup-server
```

> **进阶提示。** 机器到机器推送在首次之后只发送变更的数据块。[增量传输教程](/zh/docs/tutorial-delta-transfer)有现场演示。

---

下一篇：[网络与域名](/zh/docs/tutorial-networking)。

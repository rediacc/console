---
import type { Language } from '../../i18n/types';
import { createTranslator } from '../../i18n/utils';
import AccountLink from '../AccountLink.astro';
import Sentences from '../Sentences.astro';

interface Props {
  lang: Language;
}

const { lang } = Astro.props as Props;
const { t } = createTranslator(lang);
---

<section class="closing-cta">
  <div class="container">
    <div class="closing-cta-inner reveal">
      <h2 class="closing-cta-title">{t('getStarted.title')}</h2>
      <p class="closing-cta-subtitle"><Sentences text={t('getStarted.subtitle')} lang={lang} /></p>
      <div class="closing-cta-actions">
        <AccountLink
          label={t('getStarted.cta.primary')}
          className="btn btn-primary"
          trackEvent="cta_click"
          trackLabel="home-bottom-cta-get-started"
          trackDest="account"
        />
        <a href={`/${lang}/contact`} class="btn btn-secondary">{t('getStarted.cta.secondary')}</a>
      </div>
    </div>
  </div>
</section>

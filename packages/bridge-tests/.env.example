# Bridge Test Environment Configuration
# Copy this file to .env and customize as needed.
#
# This file is configured for FULL infrastructure (6 VMs):
# - 1 Bridge VM (192.168.111.1)
# - 2 Worker VMs (192.168.111.11, 192.168.111.12)
# - 3 Ceph VMs (192.168.111.21, 192.168.111.22, 192.168.111.23)

# =============================================================================
# Renet Configuration
# =============================================================================

# Path to renet binary (set by CI, auto-detected in local development)
RENET_BINARY_PATH=

# Docker image to extract renet binary from (CI mode)
RENET_DOCKER_IMAGE=ghcr.io/rediacc/elite/bridge:latest

# =============================================================================
# VM Network Configuration
# =============================================================================

# Network prefix for VMs (first 3 octets)
VM_NET_BASE=192.168.111

# Offset added to VM ID when calculating IP
VM_NET_OFFSET=0

# Bridge VM ID (IP = 192.168.111.1)
VM_BRIDGE=1

# Worker VM IDs (space-separated) - execute repository, datastore, container ops
VM_WORKERS=11 12

# Ceph node VM IDs (space-separated) - execute ceph_pool, ceph_image, ceph_snapshot ops
VM_CEPH_NODES=21 22 23

# =============================================================================
# Timeouts
# =============================================================================

# Default bridge command timeout in milliseconds (60s for complex operations)
# In CI mode, this is typically set to 120000 (2 minutes) for stability
BRIDGE_TIMEOUT=60000

# =============================================================================
# Test Environment
# =============================================================================

NODE_ENV=test
CI=false
DEBUG=false

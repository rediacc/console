{
  "cli": {
    "description": "Manage machines, repositories, and deployments over SSH"
  },
  "timing": {
    "step": {
      "loading": "Loading config...",
      "configLoaded": "Config loaded ({{duration}})",
      "connecting": "Connecting...",
      "connected": "Connected ({{duration}})",
      "provisioning": "Provisioning renet...",
      "renetProvisioned": "Renet provisioned ({{duration}})",
      "verifying": "Verifying machine...",
      "machineVerified": "Machine verified ({{duration}})",
      "activating": "Activating license...",
      "licenseActivated": "License activated ({{duration}})"
    }
  },
  "warnings": {
    "licenseSignatureRefreshed": "Refreshed {{count}} repo license(s) with invalid signatures after renet update",
    "licenseSignatureRefreshFailed": "{{count}} repo license(s) have invalid signatures after renet update. Run: rdc subscription refresh -m <machine>"
  },
  "commands": {
    "cluster": {
      "descriptionShort": "Create and manage clusters (node pools on a private LAN)",
      "description": "Provision and manage clusters: named sets of node pools (ceph, k8s-server, k8s-agent, hyperconverged) on a private network. Pool members materialize into machines, so every -m command works on them.",
      "create": {
        "descriptionShort": "Provision a configured cluster",
        "description": "Declare and provision a cluster: machines, Ceph pools, and Kubernetes. Pass --provider and --pool to declare in one step; a bare create provisions a cluster already declared.",
        "sshUserOption": "SSH user for provisioned members",
        "baseDomainOption": "Base domain for cluster public DNS (else inherited from a sibling machine)",
        "controlDsSizeOption": "Anchor control datastore size (default 10G)",
        "controlDsBackendOption": "Anchor control datastore backend: local | ceph (default: ceph if the cluster has ceph, else local)",
        "controlDsPoolOption": "Ceph rbd pool for the anchor control datastore (ceph backend)",
        "cidrOption": "Private network CIDR (e.g. 10.0.0.0/24)",
        "controlIdOption": "KVM: VM id of the control and registry node (default: 1)",
        "controlNodeOption": "Explicit control-node machine (default: first k8s-server member)",
        "declared": "Declared cluster \"{{name}}\"",
        "declareOnlyOption": "Record the cluster in config without provisioning it",
        "dockerRegistryOption": "KVM: in-VM Docker registry endpoint for this cluster",
        "netBaseOption": "KVM: network prefix, the first three octets (e.g. 192.168.112)",
        "netNameOption": "KVM: libvirt network for this cluster (e.g. renet12)",
        "netOffsetOption": "KVM: offset added to each VM id when deriving its address",
        "poolOption": "Pool spec name:role:count[:size] (role: ceph|k8s-server|k8s-agent|hyperconverged)",
        "primitiveOption": "Network primitive (e.g. vlan, vpc, network)",
        "providerOption": "Provider: a cloudProviders key, or 'kvm'",
        "examples": {
          "basic": "Create a cluster on a provider.",
          "declareOnly": "Declare a cluster around an existing control node."
        }
      },
      "status": {
        "descriptionShort": "Show clusters and their pools",
        "description": "List all clusters, or pass a cluster name to show that cluster's full config.",
        "none": "No clusters configured. Add one with: rdc cluster create <cluster>"
      },
      "scale": {
        "descriptionShort": "Scale a cluster pool up or down",
        "description": "Change a pool's member count (adds/removes machines and joins/drains nodes). k8s-agent pools scale in place; ceph pool growth is done via cephadm directly.",
        "poolOption": "Pool name",
        "countOption": "Desired member count"
      },
      "destroy": {
        "descriptionShort": "Destroy a cluster and its members",
        "description": "Tear down the provisioned members and remove the cluster and its machines from config.",
        "forceOption": "Skip confirmation and continue past teardown errors",
        "confirm": "Destroy cluster \"{{name}}\" and all its members?",
        "aborted": "Aborted."
      },
      "kubeconfig": {
        "descriptionShort": "Fetch and cache a cluster kubeconfig",
        "description": "Fetch the kubeconfig from the cluster control node over SSH and cache it locally (0600) for kubectl.",
        "cached": "Kubeconfig cached at {{path}}"
      },
      "fork": {
        "descriptionShort": "Clone a whole cluster (with data)",
        "description": "Clone an entire cluster, including its repos' data, into a new cluster: coordinated copy-on-write of the cluster and PV images, then node-identity rewrite so the fork comes up on new addresses. The parent keeps running.",
        "tagOption": "Fork tag",
        "writesOption": "Fork write disposition: local (ephemeral dm-COW overlay, zero Ceph footprint) | ceph (durable clone). Default local",
        "upOption": "Bring the forked repos up and gate on cluster health after the fork boots",
        "toOption": "Destination cluster whose nodes host the fork (its control node and agents; a cluster cannot fork onto its own machines, since two k3s cannot share a host network namespace)",
        "examples": {
          "basic": "Fork a cluster into a new destination.",
          "andStart": "Fork a cluster and start it right away."
        }
      },
      "migrate": {
        "descriptionShort": "Move a whole cluster to another place",
        "description": "Move an entire cluster, including its repos' data, to another machine or datacenter with a short cutover (hot pre-copy, then stop-and-restart on the destination).",
        "toOption": "Destination"
      },
      "join": {
        "descriptionShort": "Add a machine to a cluster as an agent",
        "description": "Adopt an existing registered machine as a Kubernetes agent node of a cluster, using the same CA-derived join token as anchor and rejoin.",
        "clusterOption": "Cluster to join the machine to",
        "examples": {
          "basic": "Join a machine to a cluster."
        }
      },
      "evict": {
        "descriptionShort": "Remove a node from its cluster",
        "description": "Drain the node, delete its Node object, and clear its cluster membership. The cluster is derived from the machine; a machine that still mounts a datastore is refused.",
        "forceOption": "Skip the drain when the node is already dead"
      },
      "rehearse": {
        "descriptionShort": "Boot an ephemeral throwaway fork, health-gate it, then discard",
        "description": "Rehearse a release/upgrade: fork the cluster onto a destination as an ephemeral throwaway (writes=local, secretless role=rehearsal), bring it up, gate on health, then discard it. The parent is never touched.",
        "tagOption": "Optional tag for the rehearsal fork (default: timestamped)",
        "onOption": "Destination cluster to boot the throwaway rehearsal on"
      },
      "snapshot": {
        "descriptionShort": "Snapshot every datastore in the cluster at one instant",
        "description": "Point-in-time snapshots of the whole cluster. One crash-consistent instant across every rbd-backed datastore the cluster owns, taken through Ceph's group snapshot: nothing stops and the cluster never notices. Local-backend datastores have no block-level group primitive, so they fall outside the instant and are reported as such. The instant is crash-consistent, not application-consistent: if you need a specific write to be IN the snapshot, sync it first.",
        "create": {
          "descriptionShort": "Take a cluster-wide snapshot",
          "description": "Snapshot every rbd-backed datastore in the cluster at ONE instant. Nothing is stopped. Any local-backend datastore in the cluster is listed as outside the instant: it is not part of the snapshot. The instant is crash-consistent, not application-consistent: if you need a specific write to be IN the snapshot, sync it first.",
          "labelOption": "Snapshot label (default: a UTC timestamp)"
        },
        "list": {
          "descriptionShort": "List cluster snapshots",
          "description": "List the cluster's group snapshots."
        }
      }
    },
    "credits": {
      "descriptionShort": "Show third-party attributions and license compliance info",
      "description": "Print the third-party components bundled with rdc: the binaries renet embeds and conveys (CRIU, rsync, rclone), the embedded Node.js runtime, and the bundled npm dependencies. Shows versions, SPDX license identifiers, and the exact-version source archive URLs required for GPL compliance. Use --licenses to print the full THIRD_PARTY_LICENSES text bundled with release builds.",
      "header": "Rediacc is proprietary software distributed under the Rediacc license; the following third-party components are distributed alongside it under their own licenses.\n\nThird-party components bundled with rdc:",
      "embeddedTitle": "Embedded binaries (conveyed by renet):",
      "bundledTitle": "Runtime and bundled dependencies:",
      "gplHeader": "GPL source availability:",
      "licensesUnavailable": "The full THIRD_PARTY_LICENSES text is generated at build time and is only bundled with release builds. In dev mode, run the CLI build to generate it, or view the per-component attributions with: rdc credits"
    },
    "mcp": {
      "descriptionShort": "Model Context Protocol server for AI agents",
      "description": "Expose the CLI as a Model Context Protocol (MCP) server for integration with AI agents. The server wraps CLI commands as MCP tools with structured JSON input/output. Key subcommand: serve (start stdio-based MCP server).",
      "serve": {
        "description": "Start MCP server (stdio transport)",
        "configOption": "Config name to use for all commands",
        "timeoutOption": "Default command timeout in milliseconds"
      }
    },
    "doctor": {
      "descriptionShort": "Diagnose CLI environment and renet integration",
      "description": "Run diagnostic checks on the CLI environment: Node.js version, renet binary availability, config file validity, SSH key status, and network connectivity. Outputs a structured health report with pass/fail indicators.",
      "title": "Rediacc CLI Doctor",
      "sections": {
        "environment": "Environment",
        "renet": "Renet",
        "configuration": "Configuration",
        "subscription": "Subscription",
        "virtualization": "Virtualization"
      },
      "checks": {
        "nodeVersion": "Node.js",
        "cliVersion": "CLI version",
        "goInstalled": "Go",
        "dockerAvailable": "Docker",
        "renetBinary": "Binary",
        "renetVersion": "Version",
        "renetCriu": "Embedded CRIU",
        "renetRsync": "Embedded rsync",
        "activeConfig": "Active config",
        "machines": "Machines",
        "sshKey": "SSH key",
        "subscriptionToken": "Token",
        "subscriptionStatus": "Status",
        "subscriptionMachineSlots": "Machine slots",
        "subscriptionRepoLicenses": "Repo licenses",
        "opsPrereqs": "OPS prerequisites"
      },
      "allPassed": "All checks passed",
      "hasWarnings": "{{count}} warning(s)",
      "hasErrors": "{{count}} error(s)",
      "notInstalled": "not installed",
      "notConfigured": "not configured",
      "notLoggedIn": "not logged in",
      "subscriptionUnreachable": "unable to reach subscription server",
      "opsSkipped": "renet not available (skipped)"
    },
    "config": {
      "descriptionShort": "Manage config files",
      "description": "Manage configuration files: create, switch, edit, reconcile state, and cross-cutting facilities (fields, audit, SSH keys, remote sync).",
      "prune": {
        "descriptionShort": "Sweep stale leftovers from the local config file",
        "description": "Remove dead weight from the local config file at ~/.config/rediacc/<config>.json. Three buckets are cleaned, all pure-local (no SSH/renet calls): (1) ACME cert-cache entries whose anchor GUID/repo/machine is no longer in the active config; (2) archived repositories whose grace period has expired (default 7 days, see defaults.pruneGraceDays); (3) dangling cross-references (machine→strategy, strategy→repo). Resources still in use, credentials, storage tokens, and known-hosts are never touched. Default behavior is to apply changes; pass --dry-run to preview only.",
        "dryRunOption": "Preview what would be removed without modifying the config file. Mirrors the default-off semantics of the other prune commands.",
        "certsOnlyOption": "Restrict to the ACME cert-cache bucket. Skips archive purging and cross-reference cleanup. Mutually exclusive with --archives-only and --refs-only.",
        "archivesOnlyOption": "Restrict to expired-archive purging. Skips cert-cache and cross-reference cleanup. Mutually exclusive with --certs-only and --refs-only.",
        "refsOnlyOption": "Restrict to dangling cross-references (machine→strategy, strategy→repo excludes/includes). Skips cert-cache and archives. Mutually exclusive with --certs-only and --archives-only.",
        "purgeArchivedOption": "Drop ALL archived repositories regardless of age, not just those past grace. Equivalent to running 'rdc repo admin archive purge' for every archived repo. Use only when you're sure you don't need any of the stashed credentials for restore.",
        "orphanReposOption": "Remove repository entries that are placed on no machine. Each entry holds that repo's credential and SSH key, so this is unrecoverable — run 'rdc config reconcile' first so placement reflects the machines.",
        "foundOrphanRepos": "{{count}} repository entry/entries are placed on no machine:",
        "removedOrphanRepos": "{{count}} repository entry/entries {{verb}} removed (placed on no machine):",
        "orphanReposHint": "Run 'rdc config reconcile' to derive placement from your machines. Entries still listed after that are genuinely unplaced; remove them with 'rdc config prune --orphan-repos'.",
        "droppedStateRepos": "{{count}} orphaned state record(s) {{verb}} dropped (no matching repository entry):",
        "graceDaysOption": "Override the archive grace window (in days) for this invocation. Falls back to defaults.pruneGraceDays in the config, then to 7 if neither is set.",
        "tooManyOnlyFlags": "Pass at most one of --certs-only / --archives-only / --refs-only.",
        "scanning": "Scanning local config for stale leftovers...",
        "removedCerts": "{{count}} cert cache entry/entries {{verb}} removed:",
        "purgedArchives": "{{count}} expired archived repository/repositories {{verb}} removed:",
        "keptArchivesInGrace": "Kept archived repositories still in grace (recoverable via 'rdc repo admin archive restore'):",
        "droppedRefs": "{{count}} dangling cross-reference(s) {{verb}} removed:",
        "nothingToPrune": "Nothing to prune — config is already clean.",
        "dryRun": "Dry run: {{count}} change(s) would be applied. Re-run without --dry-run to commit.",
        "completed": "Pruned {{count}} stale entry/entries from local config."
      },
      "init": {
        "description": "Create a new named config file",
        "optionMasterPassword": "Encrypt resources with a master password",
        "alreadyExists": "Config \"{{name}}\" already exists",
        "success": "Config \"{{name}}\" initialized",
        "bareForm": "The default config is created automatically; use 'config init <name>' for a named config, or pass a flag (--ssh-key, --server, ...) to set an option on the default.",
        "serverKeyMissing": "Account server {{server}} did not advertise an encryption key. It will be discovered on the first successful request.",
        "serverKeyUnreachable": "Could not reach account server {{server}} to sync its encryption key ({{error}}). It will be discovered on the first successful request.",
        "examples": {
          "basic": "Create a named configuration profile.",
          "withServer": "Create a profile pointed at a specific account server."
        }
      },
      "list": {
        "description": "List all config files",
        "noConfigs": "No config files found. Create one with: rdc config init"
      },
      "show": {
        "description": "Show current config details",
        "noConfig": "No active config. Create one with: rdc config init",
        "optionReveal": "Show plaintext for sensitive values (interactive only)"
      },
      "current": {
        "description": "Show the active config: name, file, resolved server and channel, token state"
      },
      "delete": {
        "description": "Delete a config file",
        "success": "Config \"{{name}}\" deleted"
      },
      "set": {
        "description": "Set a default value (team, region, bridge)",
        "success": "Set \"{{key}}\" to \"{{value}}\""
      },
      "clear": {
        "description": "Clear defaults (all or specific key)",
        "keyCleared": "Cleared \"{{key}}\"",
        "allCleared": "Cleared all defaults"
      },
      "recover": {
        "description": "Restore config from backup (.bak) file",
        "noBackup": "No backup found for config \"{{name}}\"",
        "confirm": "Restore config \"{{name}}\" from backup?",
        "success": "Config \"{{name}}\" restored from backup (version {{version}})",
        "failed": "Failed to restore config \"{{name}}\" from backup"
      },
      "ssh": {
        "description": "Manage SSH key configuration",
        "set": {
          "description": "Set SSH key for the current config",
          "optionKey": "Path to SSH private key file",
          "optionEmbed": "Embed key content in config instead of storing path",
          "success": "SSH key configured",
          "invalidKey": "File does not contain a valid SSH private key",
          "keyNotFound": "SSH key file not found: {{path}}"
        },
        "show": {
          "description": "Show current SSH key configuration",
          "noKey": "No SSH key configured"
        },
        "remove": {
          "description": "Remove SSH key from the current config",
          "success": "SSH key removed",
          "noKey": "No SSH key configured"
        }
      },
      "remote": {
        "description": "Manage remote config storage connection",
        "tokenExpired": "Remote config token expired. Re-authenticate with: rdc config remote enable",
        "unreachable": "Config server {{server}} is unreachable",
        "offlineCacheWarning": "Config server {{server}} is unreachable; serving config \"{{config}}\" from the offline cache (server version {{version}}, cached {{age}} ago). Changes cannot be saved until the server is reachable.",
        "offlineNoCache": "Config server {{server}} is unreachable and config \"{{config}}\" has no offline cache yet. Run \"rdc config remote refresh\" once the server is reachable to seed the cache.",
        "writeFailedClosed": "Cannot save: config \"{{config}}\" is stored on {{server}}, which is unreachable. The change was NOT saved.",
        "conflictRetryExhausted": "Config \"{{config}}\" is still conflicting with another device after 3 attempts. Re-run the command.",
        "passkeySecretMissing": "Passkey secret not found in OS keyring. Re-authenticate with: rdc config remote enable",
        "staleSlot": "Your key slot is outdated (the config encryption key was rotated). Re-enroll with: rdc config remote enable",
        "undecryptableIdentity": "Config {{configId}} in store {{storeId}} was encrypted with a different key than your slot holds, so this device cannot read it. That happens when the store already holds a config from another enrollment. Pull that config on a device that is already enrolled and check which config you meant, or ask an admin to re-share the store's key with this device from the portal (Config Storage > key slots).",
        "undecryptableSession": "Config {{configId}} unlocked with your key but the server session layer would not open it ({{detail}}). Retry the command; if it keeps failing, re-enroll with: rdc config remote enable",
        "handoffUndecryptable": "The browser sent a handoff this device cannot open ({{error}}). This usually means an older 'config remote enable' tab is still open and posted to the wrong session. Close every rediacc portal tab, then run the command again.",
        "enable": {
          "description": "Link this config to remote encrypted storage",
          "optionHeadless": "Use device code flow (for headless servers)",
          "optionApiUrl": "Account server URL",
          "openBrowser": "Open this URL to complete setup:",
          "waiting": "Waiting for browser approval...",
          "received": "Handoff received",
          "polling": "Polling for browser approval...",
          "success": "Remote config enabled. Config \"{{name}}\" is now synced to {{apiUrl}}.",
          "alreadyEnabled": "Config \"{{name}}\" is already linked to remote storage. Disable first with: rdc config remote disable",
          "expired": "Device code expired. Try again.",
          "optionPassword": "Enroll headlessly with a pre-provisioned password slot (no browser)",
          "passwordPrompt": "Config master password:",
          "passwordNonInteractive": "A config master password is required. Set REDIACC_CONFIG_PASSWORD, or run without --password in an interactive terminal.",
          "passwordRequirePasskey": "This organization requires a passkey to unlock config storage; password enrollment is disabled. Enroll from a browser with: rdc config remote enable",
          "passwordIpBound": "The API token in REDIACC_TOKEN is bound to a different client IP address. A token binds to the address it is first used from, so a token created elsewhere cannot enroll from this machine. Issue a fresh token from the web portal and retry.",
          "passwordMissingScope": "The API token in REDIACC_TOKEN does not carry the config:enroll scope. Issue a token with that scope from the web portal and retry.",
          "passwordTeamGone": "The team this API token belongs to has been archived or removed. Issue a new token for an active team from the web portal and retry.",
          "passwordTokenNotEnrollable": "This API token cannot enroll a config slot because it is not tied to a user in an organization. Issue a personal token from your own account in the web portal and retry.",
          "passwordForbidden": "The account server refused the config enrollment: {{error}}",
          "passwordNoSlot": "No password slot is provisioned for your config storage. Add one from the portal (Config Storage > key slots), then retry.",
          "passwordWrong": "Could not unlock the config with that password. Check REDIACC_CONFIG_PASSWORD (or the password you entered) and try again.",
          "optionForce": "Replace differing local content with the server copy without confirmation",
          "seeded": "Store was empty; pushed the local config as version 1.",
          "overwriteConfirm": "Local config \"{{config}}\" has {{machines}} machine(s) and {{repos}} repository(ies) that will be replaced by the server copy (the previous file is kept at {{backupPath}}). Continue?",
          "overwriteAborted": "Enable aborted: local config \"{{config}}\" has content that differs from the server copy. Re-run with --force to replace it, or confirm in an interactive terminal."
        },
        "disable": {
          "description": "Disconnect from remote storage and save config locally",
          "success": "Remote config disabled. Config \"{{name}}\" saved locally.",
          "notEnabled": "Remote config is not enabled.",
          "serverRequired": "Cannot disable: config server {{server}} is unreachable, and disable must pull the latest server copy first (a disable from a stale cache could lose another device's writes). Retry once the server is reachable."
        },
        "status": {
          "description": "Show remote connection status"
        },
        "refresh": {
          "description": "Force re-fetch config from remote storage",
          "pulling": "Pulling config from remote...",
          "pulled": "Config pulled",
          "success": "Config refreshed from remote (version {{version}}).",
          "notEnabled": "Remote config is not enabled."
        }
      },
      "field": {
        "description": "Pointer-addressed config field operations (get/set/unset/rotate/list). JSON Pointers (RFC 6901) like /credentials/cfDnsApiToken.",
        "descriptionShort": "Pointer-addressed config field operations",
        "get": {
          "description": "Read a single config value by JSON Pointer. Sensitive fields redact unless --reveal (humans only).",
          "optionPointer": "JSON Pointer to the field (e.g. /credentials/cfDnsApiToken)",
          "optionReveal": "Show plaintext for sensitive values (interactive TTY only; audited)",
          "optionDigest": "Print the SHA-256 digest instead of the value (safe to share with agents)"
        },
        "set": {
          "description": "Write a config value at a JSON Pointer. Sensitive paths require --current (knowledge-gate).",
          "optionPointer": "JSON Pointer to the field (e.g. /credentials/cfDnsApiToken)",
          "optionNew": "New value (parsed as JSON if it looks like JSON: {, [, \", true/false/null/number)",
          "optionCurrent": "Current plaintext value — required for sensitive-path mutations (knowledge-gate proof)",
          "success": "Set {{pointer}}"
        },
        "unset": {
          "description": "Delete a config value at a JSON Pointer. Sensitive paths require --current.",
          "optionPointer": "JSON Pointer to the field (e.g. /credentials/cfDnsApiToken)",
          "optionCurrent": "Current plaintext value — required for sensitive-path deletions",
          "success": "Unset {{pointer}}"
        },
        "rotate": {
          "description": "Rotate a sensitive value without --current. Interactive TTY only; loudly audited.",
          "optionPointer": "JSON Pointer to the sensitive field (e.g. /credentials/cfDnsApiToken)",
          "optionNew": "New value",
          "success": "Rotated {{pointer}}"
        },
        "list": {
          "description": "List every registered sensitivity pointer template with its kind and commit/encrypt policy.",
          "optionSensitive": "Show only sensitive (non-public) templates",
          "optionOutput": "Output format (json|table)"
        }
      },
      "edit": {
        "description": "Open the active config in $EDITOR as a redacted JSONC projection. Humans only; agents refused.",
        "optionReveal": "Show plaintext for sensitive values (interactive TTY only; audited)",
        "optionDump": "Print current config as JSONC to stdout (read-only; safe for agents when redacted)",
        "optionApply": "Apply an edited JSONC file (skips $EDITOR launch)",
        "optionCurrentSecrets": "JSON file mapping pointer→old plaintext for knowledge-gate on --apply",
        "optionEditor": "Editor command override (follows git precedence: flag > $GIT_EDITOR > git config core.editor > $VISUAL > $EDITOR)",
        "bannerTitle": "Editing config \"{{name}}\" (v{{version}})",
        "bannerStrip": "Lines beginning with // are stripped on save.",
        "bannerReveal": "--reveal active: SENSITIVE VALUES VISIBLE. Audited.",
        "bannerRedacted": "Sensitive fields shown as <redacted:KIND>:DIGEST stubs that round-trip safely.",
        "bannerRotateLine1": "To rotate a secret: replace its stub with the new plaintext value — you'll",
        "bannerRotateLine2": "be asked to confirm each rotation after you close the editor.",
        "noChanges": "No changes saved; aborting.",
        "saved": "Saved config \"{{name}}\"",
        "applied": "Applied {{path}}",
        "rotatePromptHeader": "You are about to rotate {{count}} sensitive field(s):",
        "rotatePromptBullet": "  • {{pointer}}",
        "rotatePromptFooter": "Type 'rotate' to confirm (anything else aborts): ",
        "rotateDeclined": "Aborted; draft preserved in editor tempfile.",
        "abortAfterRetries": "Aborting after {{count}} failed attempts. Draft preserved at: {{path}}"
      },
      "audit": {
        "description": "Inspect the config audit log (hash-chained JSONL at ~/.config/rediacc/audit.log.jsonl)",
        "log": {
          "description": "Print recent audit entries as JSON",
          "optionSince": "Only show entries newer than (e.g., '24h', '7d', ISO timestamp)",
          "optionPath": "Filter by JSON Pointer glob (e.g., /credentials/*)",
          "optionActor": "Filter by actor kind (human|agent)"
        },
        "tail": {
          "description": "Stream new audit entries as they are written (Ctrl+C to stop)"
        },
        "verify": {
          "description": "Verify the integrity of the SHA-256 hash chain across all audit entries",
          "chainOk": "Chain integrity verified across {{count}} entries.",
          "chainBroken": "Chain broken at line {{line}} — file has been tampered with or corrupted."
        }
      },
      "reconcile": {
        "description": "Rebuild runtime state from machine truth. Fixes stale attach and routing data.",
        "optionMachine": "Limit the reconcile to these machines",
        "optionAcceptObserved": "Rewrite a declared placement to match where the image actually is, but only when that is unambiguous (observed on exactly one machine). Duplicates stay conflicts.",
        "acceptObservedUnfiltered": "--accept-observed cannot be combined with --machine: a partial scan cannot prove a placement is unambiguous, so the rewrite would be a guess. Run 'rdc config reconcile --accept-observed' unfiltered."
      },
      "certCache": {
        "noBaseDomain": "Machine has no baseDomain configured",
        "noCacheForDomain": "No cert cache found for {{domain}}",
        "pull": {
          "cached": "Cached {{count}} certificate(s) ({{size}})",
          "downloading": "Downloading certificates from {{machine}}...",
          "merged": "Kept {{count}} certificate(s) from local cache with longer expiry",
          "noAcmeJson": "No acme.json found on {{machine}}",
          "pruned": "Pruned {{count}} stale certificate(s)",
          "prunedExpired": "Pruned {{count}} expired certificate(s)"
        },
        "push": {
          "merged": "Merged {{count}} certificate(s) with longer expiry into {{machine}}",
          "noCache": "No certificate cache found. Run 'rdc machine infra cert pull <machine>' first",
          "upToDate": "Certificates on {{machine}} are up to date",
          "uploaded": "Uploaded {{count}} certificate(s) to {{machine}}",
          "uploading": "Uploading cached certificates to {{machine}}..."
        }
      },
      "infra": {
        "push": {
          "certCacheUploaded": "Uploaded cached certificates to remote",
          "dnsCreated": "Created {{type}} record {{name}} -> {{content}}",
          "dnsError": "DNS warning: {{message}} (records may need manual setup)",
          "dnsResolvingZone": "Resolving Cloudflare zone for {{domain}}...",
          "dnsSkipLocal": "Skipping DNS: .local domain does not use Cloudflare",
          "dnsSkipNoToken": "Skipping DNS: no Cloudflare API token. Set with: rdc machine infra set <machine> --cf-dns-token <token>",
          "dnsUnchanged": "{{type}} record {{name}} already up to date",
          "dnsUpdated": "Updated {{type}} record {{name}} -> {{content}}"
        }
      },
      "machine": {
        "add": {
          "sshConfigFailed": "Failed to write SSH alias (non-fatal): {{error}}",
          "sshConfigWritten": "Wrote SSH alias rediacc--{{name}} to {{path}}"
        },
        "remove": {
          "sshConfigCleared": "Removed SSH alias rediacc--{{name}}"
        }
      },
      "rotateCek": {
        "description": "Rotate the organization config-encryption key",
        "optionApiUrl": "Account server URL",
        "notEnabled": "Remote config is not enabled for \"{{name}}\".",
        "warning": "This rotates the key the whole organization config is encrypted under. Every other device must re-link afterwards.",
        "confirm": "Rotate the config-encryption key now?",
        "cancelled": "Rotation cancelled. Nothing changed.",
        "openWizard": "Complete the rotation in your browser:",
        "confirmDone": "Did the browser report the rotation as complete?",
        "resync": "The rotation revoked this device. Re-link it in your browser:",
        "waiting": "Waiting for browser approval...",
        "received": "Handoff received",
        "verifying": "Verifying the new key decrypts the config...",
        "verified": "New key verified",
        "success": "Config \"{{name}}\" is now encrypted under the new key (version {{version}})."
      }
    },
    "machine": {
      "descriptionShort": "Manage remote machines (status, health, list, add, remove, setup, prune)",
      "description": "Inspect remote machine state: system resources, running containers, systemd services, deployed repositories, and health diagnostics. Read-only queries; to modify repos use `repo`. Key subcommands: status (full state, narrowed with --system, --containers, --repositories, --services), health, list, add, remove, setup, prune, provision, deprovision.",
      "list": {
        "description": "List machines",
        "noMachines": "No machines configured. Add one with: rdc machine add <name> --ip <ip> --user <user>",
        "examples": {
          "basic": "List every registered machine.",
          "sorted": "List machines sorted by name."
        }
      },
      "health": {
        "description": "Check machine health for CI/CD pipelines",
        "fetching": "Fetching machine health...",
        "fetched": "Health data fetched",
        "header": "\nHealth Check for {{name}}:",
        "statusHealthy": "✓ Status: HEALTHY",
        "statusWarning": "⚠ Status: WARNING",
        "statusError": "✗ Status: ERROR",
        "statusCritical": "✗ Status: CRITICAL",
        "noData": "No status data available for machine",
        "systemSection": "\nSystem:",
        "systemUptime": "  Uptime: {{uptime}}",
        "systemMemory": "  Memory: {{percent}}",
        "systemDisk": "  Disk: {{percent}}",
        "systemDatastore": "  Datastore: {{percent}}",
        "containersSection": "\nContainers:",
        "containersSummary": "  {{running}}/{{total}} running ({{healthy}} healthy, {{unhealthy}} unhealthy)",
        "servicesSection": "\nServices:",
        "servicesSummary": "  {{active}}/{{total}} active ({{failed}} failed)",
        "storageSection": "\nStorage:",
        "storageSmart": "  SMART: {{healthy}} healthy, {{failing}} failing",
        "storageTemp": "  Max Temperature: {{temp}}°C",
        "repositoriesSection": "\nRepositories:",
        "repositoriesSummary": "  {{mounted}}/{{total}} mounted ({{docker}} with Docker)",
        "licensesSection": "\nLicenses:",
        "licensesSummary": "  {{valid}}/{{total}} valid, {{expired}} expired, {{mismatch}} machine mismatch, {{missing}} missing",
        "issuesSection": "\nIssues:",
        "exitCode": "\nExit Code: {{code}}"
      },
      "provision": {
        "description": "Provision a new machine on a cloud provider using OpenTofu",
        "optionProvider": "Cloud provider name (from machine provider add)",
        "optionRegion": "Override default region",
        "optionType": "Override default instance type",
        "optionImage": "Override default OS image",
        "optionSshUser": "SSH username for the new VM (default: root)",
        "optionBaseDomain": "Base domain for infrastructure (e.g., example.com). Implies --infra",
        "optionNoInfra": "Skip infrastructure configuration (proxy + DNS)",
        "configuringInfra": "Configuring infrastructure (proxy + DNS) for {{name}}...",
        "infraInherited": "Auto-detected base domain from sibling machine \"{{source}}\"",
        "noInfraHint": "No base domain configured. Run 'rdc machine infra set <machine> --base-domain <domain>' then 'rdc machine infra push <machine>' to enable DNS and proxy.",
        "providerRequired": "Cloud provider \"{{name}}\" not found. Add with: rdc machine provider add <name>",
        "provisioning": "Provisioning {{name}} on {{provider}} ({{region}})...",
        "waitingSSH": "Waiting for SSH connectivity...",
        "sshReady": "SSH connection established",
        "settingUp": "Running machine setup...",
        "completed": "Machine {{name}} provisioned and ready ({{ip}})",
        "examples": {
          "basic": "Provision a new cloud machine from a provider.",
          "sized": "Provision a machine in a region with a chosen size."
        }
      },
      "deprovision": {
        "description": "Destroy a cloud-provisioned machine and remove from config",
        "optionForce": "Skip confirmation prompt",
        "confirm": "This will permanently destroy machine \"{{name}}\" and all its data. Continue?",
        "destroying": "Destroying {{name}}...",
        "completed": "Machine {{name}} destroyed and removed from config",
        "noState": "No OpenTofu state found for \"{{name}}\". Use 'config remove-machine' for manually added machines.",
        "destroyFailed": "Infrastructure destroy failed for {{name}}: {{error}}. Continuing with config removal (--force).",
        "dnsDeleted": "Deleted DNS record: {{type}} {{name}}",
        "dnsCleanedUp": "Removed {{count}} DNS record(s)"
      },
      "prune": {
        "description": "Remove orphaned datastore resources and stale snapshots from a machine. The base run cleans renet-internal datastore artifacts (BTRFS subvolumes, lock files, tmpfiles). The optional flags below enable progressively narrower repo cleanups: --orphaned-repos uses the local CLI config as the only signal, while --prune-unknown additionally consults the renet .interim/state mirror so legitimate forks created by other tools survive even when missing from your local config. Both deletion paths run a mount-safety preflight; pass --force-delete-mounted to override.",
        "descriptionShort": "Prune orphaned resources and stale snapshots",
        "dryRunOption": "Show what would be removed without making changes",
        "startingDatastore": "Scanning for orphaned datastore resources on {{machine}}...",
        "datastoreCompleted": "Datastore prune completed",
        "datastoreFailed": "Datastore prune failed",
        "startingUnits": "Scanning for orphaned systemd units on {{machine}}...",
        "unitsCompleted": "Unit prune completed",
        "unitsFailed": "Unit prune failed",
        "orphanedReposOption": "Delete every repo image on the machine that is not in your local CLI config. Coarse — also removes forks created by other tools that have no local config entry, even when their renet mirror correctly identifies them as forks. Use --prune-unknown for the narrower behavior that respects the mirror.",
        "orphanedReposStarting": "Scanning for orphaned repositories on {{machine}}...",
        "orphanedReposCompleted": "Pruned {{count}} orphaned repositories",
        "noReposFound": "No repositories found on machine",
        "pruneUnknownOption": "Delete only repos the renet .interim/state mirror cannot classify (not in local config AND no fork-marked mirror). Strictly narrower than --orphaned-repos: forks-without-config are preserved when the mirror identifies them. Pre-mirror legacy orphans and stale grands whose config entry was deleted both fall in this bucket.",
        "forceDeleteMountedOption": "Override the mount-safety preflight and delete repos even if they are currently mounted or have running Docker containers. Distinct from --force (which only overrides the archive grace period). Applies to both --orphaned-repos and --prune-unknown.",
        "unknownStarting": "Scanning for unknown repositories on {{machine}} (not in local config + no fork-marked mirror)...",
        "unknownFound": "Found {{count}} unknown repository/repositories ({{deletable}} deletable after mount-safety check)",
        "unknownCompleted": "Pruned {{count}} unknown repositories",
        "noUnknownRepos": "No unknown repositories found — every machine repo is either in local config or has a renet mirror",
        "completed": "Prune completed",
        "previewEmpty": "No orphaned datastore resources found",
        "dryRunHint": "{{count}} resource(s) would be removed. Rerun without --dry-run to remove them.",
        "datastorePrunedSummary": "Pruned {{count}} orphaned datastore resource(s)"
      },
      "add": {
        "description": "Register an existing machine you can reach over SSH.",
        "descriptionShort": "Add a machine to the current config",
        "success": "Machine \"{{name}}\" added ({{user}}@{{ip}})",
        "examples": {
          "basic": "Register a machine by its address and login user.",
          "customPort": "Register a machine that uses a non-default SSH port."
        }
      },
      "infra": {
        "cert": {
          "clear": {
            "cleared": "Certificate cache cleared",
            "description": "Remove the certificate cache",
            "noCache": "No certificate cache to clear"
          },
          "description": "Manage cached TLS certificates",
          "pull": {
            "description": "Download and cache TLS certificates from a machine",
            "optionNoPrune": "Skip pruning stale network-ID certificates"
          },
          "push": {
            "description": "Upload cached TLS certificates to a machine"
          },
          "status": {
            "certCount": "{{count}} certificate(s), {{size}} compressed",
            "description": "Show cached certificate inventory",
            "header": "Certificate cache for {{baseDomain}}",
            "noCache": "No certificate cache stored",
            "updatedAt": "Last updated: {{date}} (from {{machine}})"
          }
        },
        "description": "Manage infrastructure configuration (proxy, DNS, certificates)",
        "push": {
          "description": "Push infrastructure config to machine (Traefik proxy, router, Cloudflare DNS). Run 'machine infra set <machine>' first",
          "descriptionShort": "Push infrastructure config to machine",
          "installingProxy": "Installing the router...",
          "success": "Infrastructure config pushed to \"{{name}}\""
        },
        "set": {
          "description": "Set infrastructure configuration for a machine (machine-specific: IPs, domain, ports; shared: cert email, CF DNS token)",
          "descriptionShort": "Set infrastructure configuration for a machine",
          "noOptions": "No options specified. Use --public-ipv4, --base-domain, etc.",
          "optionBaseDomain": "Base domain for applications (per-machine)",
          "optionCertEmail": "Email for TLS certificate notifications (shared across machines)",
          "optionCfDnsToken": "Cloudflare DNS API token for ACME DNS-01 challenge (shared across machines)",
          "optionPublicIPv4": "Public IPv4 address (per-machine)",
          "optionPublicIPv6": "Public IPv6 address (per-machine)",
          "optionTcpPorts": "TCP ports to forward (comma-separated, e.g., 25,143,465)",
          "optionUdpPorts": "UDP ports to forward (comma-separated, e.g., 53)",
          "success": "Infrastructure config updated for \"{{name}}\""
        },
        "show": {
          "description": "Show infrastructure configuration for a machine",
          "noInfra": "No infrastructure config set for \"{{name}}\""
        }
      },
      "provider": {
        "add": {
          "description": "Add a cloud provider",
          "optionImage": "Default OS image",
          "optionImageAttr": "Custom: attribute name for OS image",
          "optionInstanceType": "Default instance type/size",
          "optionIpv4Output": "Custom: output attribute for IPv4 address",
          "optionIpv6Output": "Custom: output attribute for IPv6 address",
          "optionLabelAttr": "Custom: attribute name for VM label",
          "optionProvider": "Known provider source (e.g., linode/linode, hetznercloud/hcloud)",
          "optionRegion": "Default region for new machines",
          "optionRegionAttr": "Custom: attribute name for region",
          "optionResource": "Custom: OpenTofu resource type for VM",
          "optionSizeAttr": "Custom: attribute name for instance type",
          "optionSource": "Custom OpenTofu provider source (e.g., vultr/vultr)",
          "optionSshKeyAttr": "Custom: attribute name for SSH keys",
          "optionSshKeyFormat": "Custom: SSH key format (inline_list or resource_id)",
          "optionSshKeyResource": "Custom: OpenTofu resource type for SSH keys",
          "optionSshUser": "SSH username for new VMs (default: root)",
          "optionToken": "API token for the cloud provider",
          "success": "Cloud provider \"{{name}}\" added ({{provider}})"
        },
        "description": "Manage cloud providers for automated machine provisioning",
        "list": {
          "description": "List configured cloud providers",
          "noProviders": "No cloud providers configured. Add with: rdc machine provider add <name>"
        },
        "remove": {
          "description": "Remove a cloud provider configuration",
          "success": "Cloud provider \"{{name}}\" removed"
        }
      },
      "remove": {
        "confirm": "Remove machine \"{{name}}\" from the config?",
        "description": "Remove a machine from the config. Does not touch the machine itself.",
        "forceDangling": "Removed machine \"{{name}}\" with {{count}} repository placement(s) still on it: {{repos}}. Those placements now dangle. Re-point them with \"rdc repo migrate\" or delete them with \"rdc repo delete\".",
        "forceOption": "Remove the machine even if repositories are still placed on it, leaving those placements dangling.",
        "nextDelete": "Delete each repository placed on this machine.",
        "nextForce": "Remove the machine anyway, leaving those repository placements dangling.",
        "nextMigrate": "Move each repository to another machine first.",
        "placementConflict": "Cannot remove machine \"{{name}}\": {{count}} repository placement(s) still reference it: {{repos}}.",
        "placementConflictNext": "Move or delete these repositories first, or force the removal.",
        "success": "Machine \"{{name}}\" removed"
      },
      "scanKeys": {
        "completed": "Scanned {{count}}/{{total}} machines",
        "description": "Scan SSH host keys for machines in the current config",
        "keysScanned": "Host keys scanned for \"{{name}}\"",
        "replaced": "Host keys for \"{{name}}\": {{count}} existing key(s) REPLACED - verify the fingerprints below out of band",
        "noKeys": "Could not scan host keys for \"{{name}}\""
      },
      "setup": {
        "completed": "Machine '{{machine}}' setup completed successfully",
        "datastoreOption": "Datastore path on remote machine",
        "datastoreSizeOption": "Datastore size (e.g., 95%, 100G)",
        "description": "Install renet and prepare a registered machine for repositories.",
        "descriptionShort": "Provision a remote machine for repositories",
        "failed": "Machine '{{machine}}' setup failed: {{error}}",
        "starting": "Setting up machine '{{machine}}'..."
      },
      "status": {
        "ambiguous": "More than one machine is registered ({{machines}}); name the one to show.",
        "connection": "Connection",
        "containersAllHealthy": "All containers are healthy",
        "containersUnhealthyFound": "{{count}} unhealthy container(s) found",
        "description": "Show a machine's system, repositories, containers, and services.",
        "descriptionShort": "Show machine status",
        "fetched": "Status fetched",
        "fetching": "Fetching status from {{machine}}...",
        "healthCheck": "Health check mode - exits with code 2 if any unhealthy",
        "infrastructure": "Infrastructure",
        "servicesAllStable": "All services are stable",
        "servicesUnstableFound": "{{count}} unstable service(s) found",
        "stabilityCheck": "Stability check mode - exits with code 2 if any failed/restarting",
        "storageHealthFragmentationNote": "Fragmentation counts extents in each repo's copy-on-write image, not the files inside it. High values are expected under random-write workloads (databases, container layers) and are informational. Defragmentation is intentionally unsupported here: it would unshare reflinked forks and backup snapshots and inflate datastore usage.",
        "storageHealthHint": "Tip: Run with --storage-health to see BTRFS fragmentation and reflink savings per repo",
        "storageHealthPoolSnapshots": "Backup snapshots pin {{pinned}} ({{active}} active, {{stale}} stale; stale ones are removed by 'rdc machine prune')",
        "storageHealthPoolSummary": "Pool: {{used}} used, {{free}} free ({{percent}}% full)",
        "storageHealthSummary": "Storage efficiency: {{summary}}",
        "strictDriftDetected": "--strict: {{count}} container(s) past drift threshold; exiting non-zero",
        "syncCertsFailed": "Cert cache sync failed: {{error}}",
        "examples": {
          "basic": "Show a machine's full status.",
          "containers": "Show only the running containers on a machine."
        },
        "licenseBackupBlocked": "\nBACKUPS BLOCKED: {{repositoryGuid}} has been refused by licensing since {{at}} ({{reason}}). {{message}}\nScheduled backups for this repository are not running. Reissue with: rdc subscription refresh"
      }
    },
    "ops": {
      "descriptionShort": "Local VM operations (provision, manage, destroy)",
      "description": "Local VM provisioning and management using KVM/QEMU/Hyper-V (platform auto-detected). Not for remote machines — use `machine` for those. Key subcommands: up (start VMs), down (stop), status (list running VMs), ssh (connect to VM), setup (install prerequisites), check (verify host readiness).",
      "up": {
        "description": "Provision VM cluster locally",
        "starting": "Starting VM cluster provisioning...",
        "completed": "VM cluster provisioned successfully",
        "failed": "VM cluster provisioning failed"
      },
      "down": {
        "description": "Destroy VM cluster",
        "starting": "Destroying VM cluster...",
        "completed": "VM cluster destroyed successfully",
        "failed": "VM cluster destruction failed"
      },
      "status": {
        "description": "Show VM cluster status",
        "noVMs": "No VMs found"
      },
      "ssh": {
        "description": "SSH into a VM",
        "connecting": "Connecting to VM {{id}}...",
        "vmNotFound": "VM with ID {{id}} not found or not running"
      },
      "setup": {
        "description": "Install virtualization prerequisites",
        "linux": "Installing KVM prerequisites via renet...",
        "macos": "Installing QEMU via Homebrew...",
        "windows": "Setting up Hyper-V prerequisites...",
        "unsupported": "Unsupported platform: {{platform}}",
        "completed": "Prerequisites installed successfully",
        "failed": "Installation failed",
        "brewFailed": "Homebrew installation failed"
      },
      "check": {
        "description": "Verify virtualization prerequisites",
        "title": "OPS Prerequisites Check",
        "allPassed": "All prerequisites satisfied",
        "hasMissing": "{{count}} prerequisite(s) missing",
        "localFallback": "(using local checks — renet not available)"
      }
    },
    "datastore": {
      "descriptionShort": "Named, movable storage pools",
      "description": "Named storage pools that hold repositories. A datastore is mobile (attach it to a machine, move it to another) and single-mounter (exactly one machine holds it at a time). The implicit default datastore on each machine is not managed here.",
      "resize": {
        "description": "Grow or shrink a datastore. This is an offline operation: the repositories inside it must be stopped.",
        "sizeOption": "New size (for example 200G)",
        "descriptionShort": "Grow or shrink a datastore",
        "resized": "Datastore \"{{ref}}\" resized to {{size}}."
      },
      "fork": {
        "description": "Fork a datastore copy-on-write. The fork is instant and its cost does not grow with the size of the pool. Needs the rbd backend: a local datastore has no block-level clone, so repositories inside it fork one at a time by reflink instead (rdc repo fork).",
        "descriptionShort": "Fork a datastore copy-on-write",
        "cowSizeOption": "Overlay size for --writes local",
        "tagOption": "Tag for the fork (the result is name:tag)",
        "attachToOption": "Attach the fork to this machine right away (needs --writes)",
        "writesOption": "Where the fork's writes go: local (instant, ephemeral) or ceph (durable clone)",
        "forked": "Datastore \"{{ref}}\" forked from \"{{name}}\" (detached).",
        "examples": {
          "basic": "Fork a datastore into a new tag.",
          "andAttach": "Fork a datastore and attach it to a machine."
        }
      },
      "status": {
        "description": "Show one datastore: its backend, attachment, usage, repositories and snapshots. A detached datastore still reports its record.",
        "descriptionShort": "Show one datastore"
      },
      "create": {
        "descriptionShort": "Create a named datastore on a machine",
        "description": "Create an additional named datastore on a machine. A local backend is a file-backed pool on that machine and stays there. An rbd backend lives in Ceph, so it can move to any machine that reaches the same cluster. Pass --cluster to make it a kubernetes-world datastore; that backref is fixed at creation.",
        "machineOption": "Machine to create the datastore on",
        "sizeOption": "Datastore size (for example 100G)",
        "backendOption": "Storage backend: local (file-backed, stays on this machine) or rbd (Ceph, movable)",
        "poolOption": "Ceph pool for the rbd backend (default: rbd)",
        "imageOption": "RBD image name for the rbd backend (default: the datastore name)",
        "clusterOption": "Kubernetes cluster this datastore belongs to. Set means kubernetes repositories only; unset means docker repositories only. Fixed at creation.",
        "created": "Datastore \"{{name}}\" created on {{machine}} (detached). Attach it with \"rdc datastore attach {{name}} --to {{machine}}\".",
        "examples": {
          "basic": "Create a datastore on a machine.",
          "ceph": "Create a Ceph-backed datastore."
        }
      },
      "list": {
        "descriptionShort": "List named datastores",
        "description": "List named datastores, where they are attached, and what they hold.",
        "placeArgument": "Narrow to one cluster or one machine",
        "examples": {
          "basic": "List every datastore.",
          "byPlace": "List the datastores at a machine or cluster."
        }
      },
      "attach": {
        "descriptionShort": "Attach a datastore to a machine",
        "description": "Attach a datastore to a machine. Exactly one machine holds a datastore at a time, so attaching it somewhere else moves it: the old holder gives it up first, and a failed hand-off leaves the original attachment intact. A fork must say where its writes go.",
        "toOption": "Machine to attach the datastore to",
        "writesOption": "Where a fork's writes go: local (instant, ephemeral, lost on detach) or ceph (durable clone in the pool). Required for a fork.",
        "cowSizeOption": "Overlay size for --writes local",
        "noAutoOption": "Do not re-attach this datastore automatically on boot",
        "forceOption": "Fence a stale holder that did not give the datastore up cleanly",
        "attached": "Datastore \"{{ref}}\" is attached to {{machine}}.",
        "noop": "Datastore \"{{ref}}\" is already attached to {{machine}} the same way. Nothing to do.",
        "relocating": "Moving \"{{ref}}\" from {{from}} to {{to}} (it detaches from {{from}} first)...",
        "examples": {
          "basic": "Attach a datastore to a machine.",
          "cephWrites": "Attach a datastore with Ceph-backed writes."
        }
      },
      "detach": {
        "descriptionShort": "Detach a datastore from its machine",
        "description": "Detach a datastore from its machine. Repositories inside it stop first. A fork attached with --writes local has nowhere to write its overlay back to, so detaching it throws the overlay away and needs --discard.",
        "discardOption": "Throw away a local-writes fork and its overlay. The data is not recoverable.",
        "confirmDiscard": "Discard fork \"{{ref}}\" and everything written to it?",
        "aborted": "Nothing was discarded.",
        "detached": "Datastore \"{{ref}}\" detached from {{machine}}.",
        "discarded": "Fork \"{{ref}}\" discarded; its overlay is gone.",
        "noop": "Datastore \"{{ref}}\" is not attached to anything. Nothing to do."
      },
      "snapshot": {
        "descriptionShort": "Point-in-time snapshots of a datastore",
        "description": "Take and list point-in-time snapshots of a datastore. A snapshot costs nothing at rest and is what a fork clones from.",
        "create": {
          "descriptionShort": "Snapshot a datastore",
          "description": "Take a point-in-time snapshot of a datastore. Nothing stops. The instant is crash-consistent, not application-consistent: if you need a specific write to be IN the snapshot, sync it first.",
          "labelOption": "Snapshot label (default: a UTC timestamp)",
          "created": "Datastore \"{{ref}}\" snapshotted as \"{{label}}\"."
        },
        "list": {
          "descriptionShort": "List datastore snapshots",
          "description": "List a datastore's snapshots."
        }
      },
      "delete": {
        "descriptionShort": "Destroy a datastore and everything in it",
        "description": "Destroy a datastore and everything in it. It detaches first; if it will not detach cleanly, the delete fails rather than orphaning a mounted pool.",
        "forceOption": "Delete even though repositories still point at it. Their data goes with it.",
        "confirm": "Destroy datastore \"{{ref}}\" and everything in it?",
        "aborted": "Nothing was deleted.",
        "deleted": "Datastore \"{{ref}}\" deleted."
      }
    },
    "repo": {
      "description": "Deploy and manage encrypted repositories on machines. Key subcommands: create, up (deploy), down (stop), fork (instant CoW clone), delete, push/pull (backup), sync (file transfer). Lifecycle: repo create → repo up → repo down. See Key Concepts in rdc --help for architecture, routing, and environment variable details.",
      "descriptionShort": "Deploy and manage encrypted repositories on machines",
      "machineOption": "Target machine name",
      "yesOption": "Skip confirmation for batch operations",
      "batchConfirm": "{{action}} all {{count}} repos on \"{{machine}}\"? [y/N]",
      "noCredential": "Warning: Repository \"{{name}}\" has no credential set. Operations on encrypted repos may fail.",
      "certSync": {
        "updated": "ACME cert cache updated from {{machine}} ({{count}} certs)"
      },
      "up": {
        "description": "Deploy or update a repository (mount, run Rediaccfile up which calls renet compose). The machine is derived from the ref placement. Proxy routes take ~3s to become active after deploy. Prints the URL pattern for HTTP-exposed services (rediacc.service_port label) on completion. First deploy and forks are mounted automatically. CRIU checkpoint restore is auto-detected; use --skip-checkpoint to force fresh start. Use --all --machine <m> to deploy every repository on a machine.",
        "descriptionShort": "Deploy or update a repository",
        "skipCheckpointOption": "Skip CRIU checkpoint restore even if checkpoint data exists (force fresh start)",
        "grandOption": "Parent credential repository (auto-resolves name to GUID). Only for repos sharing secrets with a parent",
        "tlsOption": "Request dedicated TLS cert for this repo (forks use shared machine cert by default)",
        "starting": "Starting services for \"{{repository}}\" on \"{{machine}}\"...",
        "completed": "Services started",
        "failed": "Service start failed",
        "allOption": "Deploy every repository whose home is --machine (batch form)",
        "noStartOption": "Mount and prepare the repository without running its up() steps (folds the retired 'repo mount'; LUKS open and, for cluster repos, PV generation).",
        "noWaitOption": "Return once containers are started; health checks continue in the background",
        "examples": {
          "basic": "Deploy a repository on its machine.",
          "all": "Deploy every repository on a machine at once."
        }
      },
      "mount": {
        "starting": "Mounting \"{{repository}}\" on \"{{machine}}\"...",
        "completed": "Repository mounted",
        "failed": "Mount failed"
      },
      "upAll": {
        "includeForksOption": "Also mount/start forked repositories",
        "parallelOption": "Start repositories concurrently",
        "concurrencyOption": "Max concurrent repositories (default: 3)",
        "starting": "Starting all repositories on \"{{machine}}\"...",
        "completed": "All repositories started",
        "failed": "Up-all operation failed"
      },
      "down": {
        "description": "Stop repository Docker containers (runs Rediaccfile down via renet compose). The machine is derived from the ref placement. Does NOT unmount the encrypted volume -- the repo stays mounted and can be restarted with 'repo up'. Use --unmount to also close the LUKS container after stopping (folds the retired 'repo unmount'). Use --checkpoint to save CRIU process state before stopping (next 'repo up' auto-restores). Use --all --machine <m> to stop every repository on a machine.",
        "descriptionShort": "Stop repository Docker containers",
        "unmountOption": "Also unmount (close the LUKS container) after stopping. Required before 'repo resize' or to fully secure the volume",
        "checkpointOption": "Create CRIU checkpoint before stopping (save process memory state for later restore via 'repo up')",
        "starting": "Stopping services for \"{{repository}}\" on \"{{machine}}\"...",
        "completed": "Services stopped",
        "failed": "Service stop failed",
        "allStarting": "Stopping all repositories on \"{{machine}}\"...",
        "allCompleted": "All repositories stopped",
        "allFailed": "Down-all operation failed",
        "allOption": "Stop every repository whose home is --machine (batch form)",
        "examples": {
          "basic": "Stop a repository's containers.",
          "unmount": "Stop a repository and unmount its encrypted volume."
        }
      },
      "status": {
        "description": "Get repository status (mount state, Docker daemon running, container count, disk usage)",
        "starting": "Getting status of \"{{repository}}\" on \"{{machine}}\"...",
        "completed": "Status retrieved",
        "failed": "Status check failed"
      },
      "cat": {
        "description": "Read a bounded window of a file in a repository to stdout (diagnostics go to stderr).",
        "remoteFileOption": "File path to read, relative to the repository mount root",
        "maxBytesOption": "Maximum bytes to read and print (default 1 MiB, hard ceiling 50 MiB)",
        "offsetOption": "Byte offset to start reading from",
        "headOption": "Print only the first N lines (cannot combine with byte range)",
        "tailOption": "Print only the last N lines (cannot combine with byte range)",
        "statOption": "Print only size, type, and modification time; read no content",
        "forceBinaryOption": "Allow reading binary (NUL-containing) content",
        "starting": "Reading \"{{path}}\" from \"{{repository}}\" on \"{{machine}}\"...",
        "failed": "Read failed",
        "examples": {
          "basic": "Print a file from inside a repository."
        }
      },
      "diff": {
        "description": "Git-style file-level diff between two copy-on-write forked repositories. Reports Added, Modified, Deleted, and Renamed files. Diffs the repository given by the positional <ref> (the target / new side) against its parent, resolved from local config, or against an explicit --base repository (the base / old side). Metadata-only and size-independent: it diffs the encrypted LUKS images at the block level without decrypting them, so a 1 GB repo and a 100 GB repo diff in the same milliseconds.",
        "descriptionShort": "Git-style file-level diff between two forked repositories (A/M/D/R)",
        "baseOption": "Repository to diff against (the base / old side); defaults to the parent of <ref>",
        "nameOnlyOption": "Print only changed file paths, one per line (no status letters)",
        "statOption": "Show per-file change magnitude (byte and block deltas) and totals",
        "contentOption": "Show a unified text diff for a single file (requires a file path)",
        "fastOption": "Trust the block filter; skip content-hash confirmation (may over-report Modified)",
        "starting": "Diffing \"{{base}}\" against \"{{target}}\" on \"{{machine}}\"...",
        "failed": "Diff failed",
        "truncated": "Diff truncated: file exceeds the content-diff size cap.",
        "notFound": "Repository \"{{name}}\" not found in context",
        "notAFork": "\"{{name}}\" is not a fork. Pass an explicit base with --base.",
        "parentMissing": "Parent of \"{{name}}\" is not in this context. Pass it explicitly with --base.",
        "sameRepo": "Cannot diff a repository against itself.",
        "conflictingFormat": "Choose only one of --name-only, --stat, --content. For JSON, use -o json.",
        "contentNeedsPath": "--content requires a file path, for example: --content app/config.toml"
      },
      "list": {
        "description": "List repositories on a machine",
        "starting": "Listing repositories on \"{{machine}}\"...",
        "completed": "List completed",
        "failed": "List failed: {{error}}",
        "serverNameLegend": "* name read from the machine (not in your local config)",
        "datastoreOption": "List the repositories in this datastore (resolved to whichever machine currently holds it)",
        "examples": {
          "basic": "List the repositories on a machine.",
          "byDatastore": "List the repositories that live in a datastore."
        }
      },
      "create": {
        "description": "Create a new repository. State its home once: a machine or a datastore.",
        "sizeOption": "Repository size (e.g., 10G, 100G, 1T)",
        "noDockerOption": "Skip starting Docker daemon after creation",
        "starting": "Creating repository \"{{repository}}\" ({{size}}) on \"{{machine}}\"...",
        "registered": "Registered \"{{repository}}\" ({{guid}}, network {{networkId}})",
        "completed": "Repository created",
        "failed": "Repository creation failed",
        "rollback": "Rolled back config for \"{{repository}}\"",
        "alreadyExists": "Repository \"{{name}}\" already exists in config",
        "datastoreNotAttached": "datastore \"{{datastore}}\" is not attached to any machine. Attach it: rdc datastore attach {{datastore}} --to <machine>. If it is already attached but the config does not know, run rdc config reconcile.",
        "datastoreNotFound": "datastore \"{{datastore}}\" is not in the datastore registry.",
        "datastoreOption": "Named datastore that holds the repo (docker tiering, or the cluster form)",
        "machineInCluster": "{{machine}} is a member of cluster {{cluster}}. A repo on a cluster machine needs a datastore: rdc datastore list {{cluster}}. --machine here would create a docker repo on that node.",
        "placementRequired": "repo create takes exactly one placement flag: --machine (docker, default datastore) or --datastore (named datastore; required for cluster repos).",
        "sizeOnK8s": "kubernetes repos size their volumes from the PVC declarations; --size applies to docker repos.",
        "sizeRequiredDocker": "--size is required for a docker repository (--machine, or a docker-world --datastore).",
        "startingDatastore": "Creating repository \"{{repository}}\" on datastore \"{{datastore}}\" ({{machine}})...",
        "examples": {
          "basic": "Create a new repository on a machine.",
          "sized": "Create a repository with a fixed volume size."
        }
      },
      "delete": {
        "archiveHint": "To archive credentials for recovery: rerun with --archive-config",
        "archiveOption": "Move config entry to deletedRepositories for later recovery",
        "archived": "Credential for \"{{repository}}\" archived",
        "cloudBackupHint": "Cloud backups (if any) persist after delete. Remove orphaned cloud backups with: rdc storage prune <storage> -m {{machine}}",
        "completed": "Repository deleted",
        "configRetained": "Config entry for \"{{repository}}\" retained (the repo may exist on other machines). To drop it, re-run with --archive-config, then \"rdc repo admin archive purge {{repository}}\".",
        "confirm": "Delete repository \"{{repository}}\" on \"{{machine}}\"? This destroys its containers, volumes, and encrypted image.",
        "description": "Delete a repository and its data. Config entry is preserved; use --archive-config to move credentials to deletedRepositories for recovery via 'repo admin archive restore'. Takes a positional <ref>; a bare name resolves to the grand line and is refused when several repos share the base name, so pass name:tag to target a fork.",
        "descriptionShort": "Delete a repository and its data",
        "failed": "Repository deletion failed",
        "restoreHint": "Restore with: rdc repo admin archive restore {{guid}}",
        "starting": "Deleting \"{{repository}}\" on \"{{machine}}\"...",
        "examples": {
          "basic": "Delete a repository.",
          "fork": "Delete a single fork by its tag."
        }
      },
      "commit": {
        "descriptionShort": "Freeze a working fork into a new immutable commit",
        "description": "Freeze the current state of a mounted working fork into a new immutable commit (git-like). The commit records its message, author, timestamp, and parent in-volume (so it travels on push) and is marked read-only — it refuses to mount. The working fork continues unchanged, like git leaving the working tree intact. Check a commit out with 'rdc repo checkout' to get a writable copy.",
        "messageOption": "Commit message",
        "authorOption": "Commit author",
        "completed": "Committed {{commit}}: {{message}}",
        "failed": "Commit failed"
      },
      "branch": {
        "descriptionShort": "Create a branch ref pointing at the current commit",
        "description": "Create a named branch ref pointing at the working fork's current commit (its tip). Branch refs live in the CLI config (machine = object store, config = ref store).",
        "branchOption": "Name of the new branch",
        "completed": "Branch \"{{branch}}\" -> {{commit}}"
      },
      "checkout": {
        "descriptionShort": "Reflink-clone a commit/branch into a writable working fork",
        "description": "Reflink-clone an immutable commit (or a branch tip) into a fresh writable working fork and point HEAD at it. Near-instant and constant-time (BTRFS reflink).",
        "tagOption": "Name for the new writable working fork",
        "fromOption": "Resolve the positional <commit-or-branch-ref> as a branch name on this working fork"
      },
      "log": {
        "descriptionShort": "Walk the commit history of a fork or commit",
        "description": "Print the commit history reachable from a working fork's current commit (or a commit reference), walking the parent chain recorded by 'rdc repo commit'. Reads the out-of-volume mirror, so no commit is unlocked.",
        "failed": "Log failed"
      },
      "merge": {
        "descriptionShort": "Lifecycle-safe merge of a commit/fork into a working fork",
        "description": "Merge a source commit or fork into a target working fork. The live target is never mutated in place: the result is built in a reflink clone and atomically swapped in. A mounted or running target is refused unless --force, which cleanly quiesces it first. Without --resolve it is a whole-image take-theirs (the target becomes the source); with --resolve ours|theirs it is a per-file three-way merge against the common ancestor, taking each side's unique changes and resolving two-sided conflicts per the flag.",
        "fromOption": "Source commit or fork to merge from",
        "forceOption": "Quiesce a mounted/running target first, then merge (never mutates a live mount)",
        "completed": "Merged {{source}} into {{target}}",
        "failed": "Merge failed",
        "resolveOption": "Per-file conflict resolution for a three-way merge: 'ours' keeps the target's version, 'theirs' takes the source's. Omit for whole-image fast-forward (take-theirs).",
        "baseOption": "Common-ancestor commit GUID for a three-way merge (used with --resolve). Defaults to the source commit's parent or the target's current commit."
      },
      "gc": {
        "descriptionShort": "Garbage-collect unreachable immutable commits on a machine",
        "description": "Delete immutable commit objects on a machine that no branch or HEAD reaches (reachability GC). The machine is the object store; the CLI config is the ref store. Dry-run by default: pass --apply to delete. Never touches a mounted object or a working fork.",
        "applyOption": "Actually delete the unreachable commits (default is a dry-run preview)",
        "nothing": "Nothing to collect on {{machine}}: every commit is reachable",
        "dryRunHeader": "{{count}} unreachable commit(s) would be deleted:",
        "dryRunHint": "Re-run with --apply to delete them.",
        "deleted": "Deleted commit {{commit}}",
        "deleteFailed": "Failed to delete commit {{commit}}",
        "applied": "Garbage collection complete: {{deleted}}/{{total}} commit(s) deleted"
      },
      "fork": {
        "description": "Create a CoW (Copy-on-Write) fork of a repository. FORK IS NEAR-INSTANT AND CONSTANT-TIME regardless of repo size, BTRFS reflink clones the underlying image so a 100 GB repo and a 1 GB repo fork in the same ~seconds. The fork gets a NEW GUID, networkId, IP range, and auto-route domain ({service}-fork-{tag}.{repo}.{machine}.{baseDomain}) and is a fully independent copy. Online forking is supported, the parent can remain running. Fork inherits the parent's encryption credentials automatically. Use --checkpoint to capture CRIU process state before forking, the fork will auto-restore on first 'repo up' (in-memory state preserved). CROSS-MACHINE FORK: fork locally first, then transfer: (1) rdc repo fork <parent-ref> --tag <name>, (2) rdc repo push <fork-ref> --to <target-machine>, (3) rdc backup restore <fork-ref> --as <fork-name> -m <target-machine> --up. WARNING: do NOT use \"repo push\" alone to fork, it creates a raw copy with the SAME GUID (not an independent fork). Always fork first to get a new identity. Auto-routes use the repo name, so each fork gets a unique domain automatically.",
        "descriptionShort": "Near-instant CoW fork of a repository (new GUID, networkId, IP range)",
        "tagOption": "Tag for the fork (creates name:tag)",
        "checkpointOption": "Create CRIU checkpoint on source before forking (capture process memory state for restore on fork)",
        "immutableOption": "Mark the fork read-only: it refuses to mount, keeping its image byte-stable forever (a frozen commit/base for cross-machine delta push)",
        "immutableUpConflict": "repo fork: --immutable and --up are mutually exclusive. An immutable fork refuses to mount, so it cannot be brought up. Drop --up, or drop --immutable to deploy a normal fork.",
        "upOption": "Mount and start services after forking (fork + mount + up in one command)",
        "starting": "Forking \"{{parent}}\" as \"{{repository}}\" on \"{{machine}}\"...",
        "registered": "Registered fork \"{{repository}}\" ({{guid}}, network {{networkId}})",
        "completed": "Fork created. Deploy it: repo up {{repository}}. To copy it to another machine: repo push {{repository}} --to <target>, then boot the artifact there with backup restore {{repository}} --as {{repository}} -m <target> --up",
        "failed": "Fork creation failed",
        "rollback": "Rolled back config for \"{{repository}}\"",
        "alreadyExists": "Repository \"{{name}}\" already exists in config",
        "tagReservedLatest": "repo fork: tag \"latest\" is reserved for the grand (production) repository — pick a different --tag (e.g. \"test\", \"staging\", \"<feature>\")",
        "detachOption": "With --up: return once containers are started; health checks continue in the background (ideal for throwaway forks)",
        "detachedHint": "Services are starting in the background. Check status with: rdc machine status {{machine}} --containers",
        "noWaitOption": "With --up: return once containers are started; health checks continue in the background (ideal for throwaway forks)",
        "examples": {
          "basic": "Fork a repository under a new tag.",
          "andStart": "Fork a repository and start the fork right away."
        }
      },
      "replicate": {
        "descriptionShort": "Instant read replicas from datastore forks",
        "description": "Create N read replicas of a repository in seconds, regardless of database size. Takes ONE datastore snapshot, then makes N constant-time fork-attaches (copy-on-write, throwaway local writes) spread across the cluster's nodes, and applies a generated overlay: one pinned PersistentVolume per replica, a StatefulSet, and two Services ({repo}-rw to the primary, {repo}-ro across the replicas). Replicas are POINT-IN-TIME copies: no replication stream, no read-your-writes; each replica runs one crash-recovery pass before serving. Refresh them with 'repo replicate refresh'. Replica sets are managed state: see 'repo replicate status' and remove with 'repo replicate remove'.",
        "replicasOption": "Number of read replicas to create",
        "imageOption": "Engine image the replicas run (same engine as the primary)",
        "portOption": "Port the engine serves on",
        "pvcOption": "Volume name the engine mounts as its data dir (default: data)",
        "primaryAppOption": "The primary pod's 'app' label value the -rw Service targets (default: the repo name)",
        "headlessOption": "Make the -ro Service headless: DNS returns every replica pod IP for driver-side balancing (long-lived DB connections skew a normal Service's per-connection balancing)",
        "refreshOption": "Intended refresh interval recorded on the set (e.g. 1h); run 'repo replicate refresh' to roll it",
        "status": {
          "descriptionShort": "Show managed replica sets",
          "description": "Show managed replica sets: replicas, hosting nodes, fork datastores, snapshot, and refresh timestamps."
        },
        "remove": {
          "descriptionShort": "Tear down a replica set",
          "description": "Remove a replica set: delete its generated k8s objects (label-scoped), discard its fork datastores, drop its snapshot, and forget the state. Infra steps are best-effort so removal converges even on a partially-gone cluster."
        },
        "refresh": {
          "descriptionShort": "Roll replicas onto a fresh snapshot",
          "description": "Roll every replica onto a fresh point-in-time snapshot, ONE at a time: bounce the replica pod, discard and re-fork its datastore under the same path, and let readiness re-admit it. N-1 replicas keep serving throughout."
        }
      },
      "canary": {
        "descriptionShort": "Canary release: weighted traffic split on shared data",
        "description": "Run a NEW image next to the stable one and split live traffic between them by percentage (release ladder rung 2). Creates one canary Deployment + Service on the SAME live data (no fork: canary users on forked data would read stale data and write into a doomed copy) and tells the Rediacc proxy to route the given percent of the stable hostname's traffic to it. Weight 0 keeps the canary dark; weight 100 is the blue/green flip. Before every canary change, an automatic group snapshot of the cluster's datastores is taken (release ladder rung 0) as the universal undo. Schema compatibility between the two versions (expand-contract) is the application's responsibility. For schema-BREAKING releases, use blue/green instead: fork the repo (instant, includes data), point the fork's Service here with weight 100, and roll back by restarting the untouched parent; writes made after the flip exist only in the new copy, so the rollback window is a policy decision.",
        "imageOption": "New image the canary Deployment runs (against shared live data)",
        "portOption": "Port the app serves on (must match the stable Service)",
        "weightOption": "Percent of traffic routed to the canary (integer 0-100; 0 = dark, 100 = full flip)",
        "serviceOption": "Stable Service to split traffic with (default: the repo name)",
        "replicasOption": "Canary Deployment replica count (default: 1)",
        "status": {
          "descriptionShort": "Show managed canary sets",
          "description": "Show managed canary sets: weight, images, stable service, and the latest release-undo snapshot."
        },
        "weight": {
          "descriptionShort": "Change a canary's traffic percentage",
          "description": "Change the percent of traffic routed to a canary. Takes a fresh release-undo group snapshot first, then re-applies the overlay; the proxy picks up the new split on its refresh tick. Weight 100 sends all traffic to the new version (the blue/green flip)."
        },
        "remove": {
          "descriptionShort": "Tear down a canary",
          "description": "Remove a canary's Deployment and Service (label-scoped); the stable Service serves 100% again. Release-undo snapshots are retained; prune them with the datastore snapshot commands."
        },
        "create": {
          "descriptionShort": "Start a canary: run a new image on a share of live traffic",
          "description": "Run a NEW image next to the stable one and send it a percentage of live traffic. The canary shares the repository's live data (no fork), so schema compatibility between the two versions is the application's responsibility. A group snapshot of the cluster's datastores is taken first, as the undo."
        }
      },
      "resize": {
        "description": "Resize a repository offline (supports both grow and shrink). Repo must be unmounted first ('repo down --unmount'). For zero-downtime growth without stopping, use 'repo expand' instead",
        "descriptionShort": "Resize a repository offline (grow or shrink)",
        "sizeOption": "New repository size (e.g., 10G, 100G, 1T)",
        "starting": "Resizing \"{{repository}}\" to {{size}} on \"{{machine}}\"...",
        "completed": "Resize completed",
        "failed": "Resize failed"
      },
      "expand": {
        "description": "Expand a mounted repository online (zero downtime, grow-only). Grows the LUKS container and filesystem while containers keep running. Cannot shrink -- use 'repo resize' for that (requires unmount)",
        "descriptionShort": "Expand a mounted repository online (zero downtime, grow-only)",
        "sizeOption": "New repository size (e.g., 10G, 100G, 1T)",
        "starting": "Expanding \"{{repository}}\" to {{size}} on \"{{machine}}\"...",
        "completed": "Expansion completed",
        "failed": "Expansion failed"
      },
      "trim": {
        "description": "Reclaim datastore pool space from mounted repositories (online, zero downtime). Frees blocks deleted inside repositories back to the pool via fstrim. Without a ref, trims every mounted repository on -m plus the datastore itself. fstrim is skipped for repositories under an active backup; --docker reclaim still runs",
        "descriptionShort": "Reclaim free space from repositories back to the pool",
        "dockerOption": "Reclaim Docker space (stopped containers, dangling images, build cache); runs even while a backup snapshot is active",
        "dockerVolumesOption": "Additionally prune unused Docker volumes",
        "reportOnlyOption": "Show discard state and reclaimable estimate without trimming",
        "repoNotFound": "Repository not found: {{name}}",
        "noMountedRepos": "No mounted repositories found",
        "starting": "Trimming repositories on \"{{machine}}\"...",
        "completed": "Trim completed: {{total}} reclaimed",
        "failed": "Trim failed"
      },
      "policy": {
        "description": "Manage the automatic size policy applied by the machine's storage maintainer: grow a repository's quota online when it fills up (never past max-quota, never when the pool is low) and run scheduled trims. There is no automatic shrink; use 'repo resize' for that. Without a ref, the machine-wide default policy on -m is addressed",
        "descriptionShort": "Manage automatic size policy (auto-grow, scheduled trim)",
        "repoNotFound": "Repository not found: {{name}}",
        "invalidBool": "{{flag}} must be 'true' or 'false', got: {{value}}",
        "failed": "Policy operation failed",
        "set": {
          "description": "Set size policy fields. Only the flags you pass are changed; other stored fields keep their values. Enabling auto-grow requires --max-quota: the ceiling is your explicit consent to over-provision the pool",
          "descriptionShort": "Set size policy fields (machine default, or one repo by ref)",
          "autoGrowOption": "Enable automatic online quota growth (true/false)",
          "maxQuotaOption": "Auto-grow ceiling (e.g. 200G); required for auto-grow",
          "growThresholdOption": "Filesystem used % that triggers a grow (default 85)",
          "growStepOption": "Growth per step: absolute (10G) or percent of quota (20%)",
          "autoTrimOption": "Enable scheduled trim (true/false)",
          "trimIntervalOption": "Minimum hours between automatic trims (default 24)"
        },
        "get": {
          "description": "Show the stored machine default, the repository override (with a ref), and the effective merged policy the maintainer acts on",
          "descriptionShort": "Show size policy (machine default, repo override, effective)"
        }
      },
      "push": {
        "description": "Push repository to another machine. Storage destinations are retired; use `rdc backup snapshot` for point-in-time backup. For machine-to-machine transfer, the encrypted repo image is copied with the SAME GUID — this is a backup/migration, not a fork. To create an independent fork, use 'repo fork' first, then push. A pushed copy lands as a backup ARTIFACT: boot it on the target with 'backup restore <ref> --as <name> -m <target> --up'",
        "descriptionShort": "Push repository to another machine",
        "optionTo": "Destination machine name (auto-detected from config)",
        "optionProvision": "Auto-provision target machine via cloud provider if it doesn't exist",
        "optionCheckpoint": "Create CRIU checkpoint before backup (captures process memory state for live migration)",
        "optionForce": "Force overwrite existing backup",
        "destRequired": "Specify --to <machine> (a machine name from config)",
        "pushing": "Pushing backup of \"{{repo}}\" to {{dest}}...",
        "deploying": "Deploying \"{{repo}}\" on {{machine}}...",
        "deployed": "Repository \"{{repo}}\" deployed on {{machine}}",
        "optionBwlimit": "Bandwidth limit for rsync transfer (e.g., \"6M\", \"10M\")",
        "optionDeltaBase": "Immutable base GUID present byte-identical on both machines; transfer only changed blocks (machine target). Omit for hands-free auto-base",
        "optionStrategy": "Block-delta strategy when using a delta base: auto, physical, or shared",
        "statsDelta": "Pushed \"{{repo}}\" to {{machine}} — {{transferred}} transferred in {{duration}} (delta vs base {{base}}, image {{image}})",
        "statsFull": "Pushed \"{{repo}}\" to {{machine}} — {{transferred}} transferred in {{duration}} (full)",
        "statsNoBytes": "Pushed \"{{repo}}\" to {{machine}} — completed in {{duration}}",
        "deployFailed": "Failed to deploy \"{{repo}}\" after push",
        "examples": {
          "toMachine": "Push a repository to another machine."
        },
        "storageRetired": "Pushing to a storage backend is retired: the rclone cloud arm has been removed. Point-in-time backup is `rdc backup snapshot` (upload) and `rdc backup restore <repo> --at <snapshot>` (download), both over the chunk store. Machine-to-machine transfer is unaffected: use --to-machine"
      },
      "pull": {
        "description": "Pull repository from another machine. Storage sources are retired; use `rdc backup restore` for point-in-time restore. The source type is auto-detected from config. Use --up to deploy after pull",
        "descriptionShort": "Pull repository from another machine",
        "optionFrom": "Source machine name (auto-detected from config)",
        "optionForce": "Force overwrite existing repository",
        "optionBwlimit": "Bandwidth limit for rsync transfer (e.g., \"6M\", \"10M\")",
        "optionUp": "After pull, mount and deploy repository on this machine",
        "optionDeltaBase": "Immutable base GUID present byte-identical on both machines; receive only changed blocks (machine source)",
        "optionStrategy": "Block-delta strategy when using a delta base: auto, physical, or shared",
        "sourceRequired": "Specify --from <machine> (a machine name from config)",
        "pulling": "Pulling backup of \"{{repo}}\"...",
        "deploying": "Deploying \"{{repo}}\" on {{machine}}...",
        "deployed": "Repository \"{{repo}}\" deployed on {{machine}}",
        "deployFailed": "Failed to deploy \"{{repo}}\" after pull",
        "examples": {
          "basic": "Pull a repository from another machine.",
          "andStart": "Pull a repository and start it right away."
        },
        "storageRetired": "Pulling from a storage backend is retired: the rclone cloud arm has been removed. Point-in-time restore is `rdc backup restore <repo> --at <snapshot>` over the chunk store. Machine-to-machine transfer is unaffected: use --from-machine"
      },
      "migrate": {
        "description": "Live-migrate a repository from one machine to another with minimal downtime. Two-phase rsync: bulk transfer while running, then brief stop for delta sync. Moves the whole repository (routing is repointed at the new home) and, once the move succeeds, deletes the source images (use --keep-source to retain them). Supports CRIU checkpoint for process memory migration and auto-provisioning of target machines",
        "descriptionShort": "Live-migrate a repository between machines",
        "optionTo": "Destination machine or cluster",
        "optionProvision": "Auto-provision target via cloud provider (e.g., hetzner, linode)",
        "optionBwlimit": "Bandwidth limit for rsync transfer (e.g., 10M)",
        "optionCheckpoint": "CRIU live migration: capture and restore process memory state",
        "optionDeltaBase": "Immutable base GUID for the cutover delta (advanced; defaults to the Phase-1 base)",
        "optionStrategy": "Block-delta strategy for the cutover: auto, physical, or shared",
        "optionSkipDns": "Skip DNS record switching after migration",
        "optionKeepSource": "Keep the source images after a successful move (by default they are deleted; leftover copies are not addressable by any config record and reconcile flags them as strays)",
        "provisioning": "Provisioning target machine {{machine}} via {{provider}}...",
        "phase1": "Phase 1: Transferring bulk data (source stays running)...",
        "phase1Done": "Bulk transfer complete",
        "phase1Stats": "Transferred {{transferred}} in {{duration}} — source stayed online",
        "phase2": "Phase 2: Cutover (brief downtime)...",
        "checkpointing": "Creating CRIU checkpoint...",
        "stoppingSource": "Stopping source services...",
        "sourceStopped": "Source services stopped",
        "deltaSync": "Syncing delta...",
        "deltaDone": "Delta sync complete",
        "phase2Stats": "Cutover (DOWNTIME): {{transferred}} in {{duration}}",
        "unmountingSource": "Unmounting source...",
        "sourceUnmounted": "Source unmounted",
        "phase3": "Phase 3: Starting on target...",
        "startingTarget": "Starting services on target...",
        "targetStarted": "Target services started",
        "switchingDns": "Switching DNS records...",
        "complete": "Migration complete: {{name}} moved from {{from}} to {{to}}",
        "downtime": "Downtime: {{duration}}",
        "noOpSameHome": "Repository {{name}} already lives on {{machine}}; nothing to migrate.",
        "placementUpdated": "Routing updated: {{name}} now lives on {{machine}}",
        "placementRetryHint": "Routing already points at {{machine}}, so 'rdc repo up {{name}}' will retry there. The source images were kept as recovery material.",
        "forkRefError": "migrate moves a repository and all its forks together, and {{name}}:{{tag}} is a fork. To put a copy of the fork elsewhere use 'rdc repo push {{name}}:{{tag}} --to <place>' (the copy is a backup artifact; 'rdc backup restore' makes it live), or promote it first with 'rdc repo promote {{name}}:{{tag}}'.",
        "familyHasForks": "migrate moves the whole family to one machine, but {{name}} still has forks: {{forks}}. Push each fork where you want it with 'rdc repo push {{name}}:<tag> --to <place>', or wait for whole-family migrate.",
        "deletingSource": "Deleting source images on {{machine}}...",
        "sourceDeleted": "Source images for {{name}} deleted from {{machine}}",
        "sourceRetained": "Kept source images for {{name}} on {{machine}}. They are not addressable by any config record; reconcile will flag them as strays.",
        "sourceDeleteFailed": "Could not delete the source images for {{name}} on {{machine}}: {{error}}. The move already succeeded; sweep the leftover with 'rdc machine prune {{machine}}'.",
        "examples": {
          "basic": "Move a repository to another machine.",
          "strategy": "Move a repository using the physical copy strategy."
        }
      },
      "sync": {
        "description": "Sync repositories and files (bulk push/pull, file transfer)",
        "upload": {
          "description": "Upload files to a repository via rsync over SSH (delta transfer). Use --mirror to delete extra remote files, --dry-run to preview, --exclude to skip patterns",
          "examples": {
            "dir": "Upload a local directory into a repository.",
            "file": "Upload a single file to a path in a repository."
          }
        },
        "download": {
          "description": "Download files from a repository via rsync over SSH (delta transfer). Use --mirror to delete extra local files, --dry-run to preview, --exclude to skip patterns",
          "examples": {
            "dir": "Download a repository directory to a local folder.",
            "file": "Download a single file from a repository."
          }
        },
        "status": {
          "description": "Dry-run comparison of local and remote files (shows what would be transferred without actually transferring)"
        }
      },
      "tunnel": {
        "description": "Create an SSH port-forward tunnel to a container's port on a remote machine. Auto-detects container and port when unambiguous. The tunnel stays open until you press Ctrl+C",
        "descriptionShort": "SSH tunnel to a container port",
        "portOption": "Remote container port to forward",
        "localOption": "Local port (defaults to same as remote port)",
        "containerOption": "Container name (auto-detected if only one running)",
        "fetching": "Fetching container info from \"{{machine}}\"...",
        "active": "Tunnel active: localhost:{{localPort}} → {{container}} ({{remoteIP}}:{{remotePort}})",
        "hint": "Press Ctrl+C to close the tunnel",
        "closed": "Tunnel closed",
        "noContainers": "No running containers found for repository \"{{repository}}\"",
        "multipleContainers": "Multiple running containers found. Specify one with --container (-c):\n{{list}}",
        "containerNotFound": "Container \"{{name}}\" not found in repository \"{{repository}}\"",
        "noPorts": "No exposed ports found on container \"{{container}}\". Specify --port",
        "multiplePorts": "Multiple ports exposed on \"{{container}}\". Specify --port:\n{{list}}",
        "noServiceIP": "Container \"{{container}}\" has no service IP assigned",
        "portInUse": "Local port {{port}} is already in use. Pick another with --local",
        "notReady": "Tunnel did not become ready on localhost:{{localPort}} within 15s",
        "urlOnlyOption": "Print only the local URL once the tunnel is ready (machine-readable)"
      },
      "secret": {
        "description": "Manage per-repo secrets injected at deploy time. Write-only: get returns digest only (never plaintext), set/unset require --current digest match or --rotate-secret. Two delivery modes: env (REDIACC_SECRET_<KEY>) and file (tmpfs at /var/run/rediacc/secrets/<networkId>/<KEY> via compose `secrets:`). Secrets never enter the encrypted LUKS image; forks inherit none.",
        "descriptionShort": "Manage per-repo deploy-time secrets (env / file)",
        "keyOption": "Secret key in UPPER_SNAKE_CASE (max 64 chars). Will be exposed as REDIACC_SECRET_<KEY> for env-mode or /run/secrets/<key> in containers for file-mode.",
        "valueOption": "Secret value. Pass `-` to read from stdin (avoids shell-history exposure).",
        "modeOption": "Delivery mode: 'env' (visible in container env, docker inspect) or 'file' (tmpfs file, never in env). Default: file.",
        "currentOption": "Previous plaintext value (passwd-style precondition). Required for overwrite/unset; mutually exclusive with --rotate-secret.",
        "rotateOption": "Acknowledge rotation; skip --current precondition (audited as rotation). Use when intentionally rotating without verifying the prior value.",
        "get": {
          "descriptionShort": "Show digest of a secret (value never returned)",
          "description": "Show the SHA-256 digest of a secret. The plaintext value is never returned by design (write-only). Use --current on a subsequent set/unset to verify a value you already know, or rotate via `set --rotate-secret`.",
          "notFound": "Secret \"{{key}}\" not found on repository \"{{repository}}\"",
          "examples": {
            "basic": "Read a repository secret."
          }
        },
        "list": {
          "descriptionShort": "List per-repo secret keys and modes",
          "description": "List secret keys and modes (never values, never digests).",
          "empty": "No secrets defined on \"{{repository}}\""
        },
        "set": {
          "descriptionShort": "Set or overwrite a per-repo secret",
          "description": "Set or overwrite a secret. Forks do not inherit; set on the fork explicitly. Under agent context, requires --current digest match (passwd-style).",
          "success": "Set secret \"{{key}}\" ({{mode}}) on \"{{repository}}\"",
          "examples": {
            "basic": "Store a secret for a repository.",
            "envMode": "Store a secret and expose it as an environment variable."
          }
        },
        "unset": {
          "descriptionShort": "Delete a per-repo secret",
          "description": "Delete a secret. Under agent context, requires --current digest match.",
          "success": "Removed secret \"{{key}}\" from \"{{repository}}\""
        }
      },
      "admin": {
        "archive": {
          "description": "Archived repository record bookkeeping.",
          "list": {
            "description": "List archived repository records.",
            "noArchived": "No archived repositories"
          },
          "purge": {
            "confirm": "Purge all expired archived records?",
            "description": "Permanently delete archived records.",
            "noArchived": "No archived repositories to purge",
            "success": "Purged {{count}} archived credential(s)"
          },
          "restore": {
            "description": "Restore an archived record into the config.",
            "success": "Restored \"{{name}}\" ({{guid}})"
          }
        },
        "description": "Repository administration and maintenance verbs.",
        "validate": {
          "description": "Validate repository integrity (LUKS container, filesystem consistency, configuration). Use after unexpected shutdowns or to verify backup health",
          "descriptionShort": "Validate repository integrity",
          "starting": "Validating \"{{repository}}\" on \"{{machine}}\"...",
          "completed": "Validation completed",
          "failed": "Validation failed"
        },
        "fsck": {
          "descriptionShort": "Check config refs against machine objects",
          "description": "Validate the CLI config refs (branches, HEAD) against the objects actually present on a machine. Reports dangling refs (a ref pointing at a missing object) and orphan commits (an immutable commit no ref reaches). Read-only.",
          "clean": "No drift on {{machine}}: every ref resolves and no orphan commits found",
          "danglingHeader": "{{count}} dangling ref(s) (point at a missing object):",
          "orphanHeader": "{{count}} orphan commit(s) (no ref reaches them):",
          "orphanHint": "Reclaim orphans with: rdc repo gc --apply -m <machine>"
        },
        "ownership": {
          "description": "Change repository directory ownership UID on the mounted volume (default: 7111). Use when containers need a specific UID to access repo files",
          "descriptionShort": "Change repository directory ownership UID",
          "uidOption": "Owner UID (default: 7111)",
          "starting": "Setting ownership of \"{{repository}}\" on \"{{machine}}\"...",
          "completed": "Ownership updated",
          "failed": "Ownership update failed"
        },
        "autostart": {
          "description": "Manage automatic repository startup on boot",
          "enable": {
            "description": "Enable autostart for a repository (omit name to enable all)",
            "starting": "Enabling autostart for \"{{repository}}\" on \"{{machine}}\"...",
            "completed": "Autostart enabled",
            "failed": "Failed to enable autostart",
            "startingAll": "Enabling autostart for all repositories on \"{{machine}}\"...",
            "completedAll": "Autostart enabled for all repositories",
            "failedAll": "Failed to enable autostart for all repositories"
          },
          "disable": {
            "description": "Disable autostart for a repository (omit name to disable all)",
            "starting": "Disabling autostart for \"{{repository}}\" on \"{{machine}}\"...",
            "completed": "Autostart disabled",
            "failed": "Failed to disable autostart",
            "startingAll": "Disabling autostart for all repositories on \"{{machine}}\"...",
            "completedAll": "Autostart disabled for all repositories",
            "failedAll": "Failed to disable autostart for all repositories"
          },
          "list": {
            "description": "List repositories with autostart enabled",
            "starting": "Listing autostart repositories on \"{{machine}}\"...",
            "failed": "Failed to list autostart repositories",
            "empty": "No autostart-enabled repositories on \"{{machine}}\"",
            "service": "Autostart service: installed={{installed}}, enabled={{enabled}}"
          }
        },
        "template": {
          "description": "Manage and apply deployment templates (writes docker-compose.yml, Rediaccfile, and additional files). Rediaccfile must use 'renet compose' (not 'docker compose'). Restart policies are safe (auto-managed by renet watchdog). Renet auto-injects network_mode:host, CRIU capabilities, and rediacc labels. Dangerous settings (privileged, pid:host) are blocked. Use 'list' to see available built-in templates, 'apply' to deploy one to a repository",
          "descriptionShort": "Manage and apply deployment templates",
          "list": {
            "description": "List all embedded deployment templates shipped with the CLI",
            "descriptionShort": "List available templates",
            "empty": "No templates available"
          },
          "apply": {
            "description": "Apply a template to a repository. Use a built-in template name (e.g. app-postgres) or --file for a custom JSON template. Rediaccfile lifecycle: up() starts containers (pull images, generate configs here), down() stops. Minimal Rediaccfile: up() { renet compose -- pull; renet compose -- up -d; } down() { renet compose -- down; }. IMPORTANT: Rediaccfile MUST use 'renet compose': 'docker compose' is rejected. ENV VARS: two levels: (a) Rediaccfile shell: ${SVCNAME_IP} (e.g. APP_IP), ${REDIACC_WORKING_DIR}, ${REDIACC_NETWORK_ID}. (b) Inside containers: renet auto-injects SERVICE_IP and REDIACC_NETWORK_ID env vars. eBPF bind rewriting handles IP isolation transparently, so apps can bind to 0.0.0.0 and the kernel rewrites it to the correct loopback IP. Health checks can use localhost. network_mode:host is injected and ports: are ignored. STORAGE: Both ${REDIACC_WORKING_DIR}/... bind mounts and Docker named volumes are safe: Docker data-root is inside the encrypted LUKS mount. RESTART POLICY: Restart policies are safe: renet auto-strips them for CRIU compatibility and the watchdog handles recovery. Compose: do NOT add network_mode or rediacc.* labels (renet injects them). Multi-project: place each sub-project in its own subdirectory with its own Rediaccfile: renet auto-discovers and runs them in order. HTTPS routing: (A) Auto-route (fork-friendly, recommended): do NOT add traefik.enable. Renet auto-generates https://{serviceName}.{repoName}.{machineName}.{baseDomain}. Add rediacc.service_port=<port> label for non-80 ports. Each fork gets a unique domain. (B) Traefik labels (custom domain, NOT fork-friendly): traefik.enable=true, traefik.http.routers.<n>.rule=Host(`domain`), traefik.http.routers.<n>.entrypoints=websecure,websecure-v6, traefik.http.routers.<n>.tls.certresolver=letsencrypt, traefik.http.services.<n>.loadbalancer.server.port=<port>. For TCP/UDP: rediacc.tcp_ports=3306 / rediacc.udp_ports=53",
            "descriptionShort": "Apply a template to a repository",
            "notFound": "Template \"{{name}}\" not found. Available: {{available}}",
            "templateOption": "Template to apply"
          },
          "fileOption": "Path to custom template JSON file ({\"version\":\"2\",\"files\":{\"Rediaccfile\":\"...\",\"docker-compose.yml\":\"...\"}}): overrides the built-in template name",
          "grandOption": "Parent credential repository (auto-resolves name to GUID). Only for repos sharing secrets with a parent",
          "fileNotFound": "Template file not found: {{path}}",
          "starting": "Applying template to \"{{repository}}\" on \"{{machine}}\"...",
          "completed": "Template applied to \"{{repository}}\" on \"{{machine}}\". Deploy with: rdc repo up {{repository}}",
          "failed": "Template application failed"
        }
      },
      "batchAllNeedsMachine": "--all needs --machine <m>: the machine whose repositories to run the batch against.",
      "batchMachineOption": "With --all: the machine whose repositories to run the batch against",
      "batchNeedRefOrAll": "Provide a repository ref (repo {{verb}} <ref>), or use the batch form: repo {{verb}} --all -m <machine>.",
      "batchRefConflict": "A repository ref derives its own machine. --all and --machine are only for the batch form: repo {{verb}} --all -m <machine>.",
      "promote": {
        "backupInfo": "Old production data preserved as: {{backup}}",
        "completed": "Promote complete. The production repo now has the fork's data.",
        "confirm": "Promote \"{{fork}}\" into production as \"{{grand}}\"? The current production data is preserved as a backup fork.",
        "description": "Make a validated fork the production repository under its parent name. The parent keeps its identity (GUID, networkId, domains, autostart, backup chain) and receives the fork's data; the old production data is preserved as a backup fork. Use it to test an upgrade on a fork, verify it, then promote. Pass an explicit <name>:<tag> for the fork; a bare ref resolves to the parent and is rejected with \"not a fork\". Promote never fetches bytes: use 'repo push' or 'backup restore' for that.",
        "descriptionShort": "Make a validated fork the production repository under its parent name",
        "failed": "Promote failed",
        "grandNotFound": "Grand repo not found for fork \"{{name}}\"",
        "notAFork": "promote swaps a fork into production; \"{{name}}\" is the production record",
        "revertHint": "To revert: rdc repo promote {{backup}}",
        "starting": "Promoting \"{{fork}}\" into \"{{grand}}\" on \"{{machine}}\"...",
        "examples": {
          "basic": "Promote a fork to be the main repository.",
          "noConfirm": "Promote a fork without the confirmation prompt."
        }
      },
      "refMachineConflict": "A repository ref derives its own machine. Use -m/--machine only for the machine-wide form: repo {{verb}} --all -m <machine>.",
      "logs": {
        "descriptionShort": "Show a repository's container logs",
        "description": "Show application logs from a repository's containers. Docker repositories read from the repository's own Docker daemon; kubernetes repositories read the pod logs in the repository's namespace. If the repository runs more than one container, name the one you want with --container.",
        "containerOption": "Container to read (only needed when the repository runs more than one)",
        "followOption": "Keep streaming new lines until interrupted",
        "linesOption": "How many lines of history to show (default: 100)",
        "timestampsOption": "Prefix each line with its timestamp",
        "examples": {
          "basic": "Show a repository's container logs.",
          "container": "Show the last 50 log lines for one container."
        }
      },
      "exec": {
        "descriptionShort": "Run a command inside a repository container",
        "description": "Run a command inside a repository container. The command's own exit code is passed straight through, so this works in scripts and conditionals. Docker repositories run it through the repository's Docker daemon; kubernetes repositories run it in the repository's namespace.",
        "cmdArgument": "Command to run: everything after -- is sent to the container verbatim, so CLI flags (-c, -i, -u, --debug) must come before --",
        "containerOption": "Container to run in (only needed when the repository runs more than one)",
        "interactiveOption": "Allocate a terminal (for a command that expects one)",
        "userOption": "User to run the command as",
        "examples": {
          "basic": "Run a command inside a repository container.",
          "interactive": "Open an interactive shell in a container.",
          "separator": "Pass flags to the remote command: put them after --, and keep the CLI's own flags before it."
        }
      }
    },
    "backup": {
      "browse": {
        "description": "List the files a repository contains. A local read-only walk of the repository image: no account server and no network. An encrypted repository must be mounted, or have its keyfile deployed, before it can be opened; browse says so rather than listing nothing. It cannot list a snapshot that exists only in chunk storage; restore it first, then browse the result.",
        "optionPath": "Limit the listing to this subtree",
        "optionDepth": "Limit descent to N levels (0 for unlimited)",
        "optionLimit": "Maximum entries to return (0 for unlimited)",
        "starting": "Listing {{name}}...",
        "completed": "Listed {{name}}",
        "failed": "Could not list {{name}}",
        "truncated": "Listing truncated at {{limit}} entries. Narrow it with --path, or raise --limit.",
        "empty": "{{name}} contains no files."
      },
      "cancel": {
        "cancelled": "Cancelled {{name}}",
        "cancelling": "Stopping {{name}}...",
        "description": "Cancel a running backup on a remote machine",
        "notRunning": "No backup running for strategy \"{{name}}\""
      },
      "description": "Manage backup strategies, their scheduled runs, and the artifacts they produce: declare a strategy for a repository, run or restore it on demand, and inspect the archives it wrote to a storage endpoint.",
      "descriptionShort": "Manage backups",
      "drNudge": "Config storage is not enrolled: your repo credentials (LUKS passphrases) live only on this host, so a bare-metal restore cannot recover them. Enroll with \"rdc config remote enable\" to sync them inside the encrypted config blob.",
      "list": {
        "description": "List backup artifacts on a machine.",
        "listing": "Listing backups...",
        "optionPath": "Subdirectory within the storage root. When omitted, hot/ and cold/ are listed and merged.",
        "optionStorage": "RETIRED: storage listing was removed with the rclone arm",
        "placementExclusive": "Pass exactly one of --machine or --storage, not both.",
        "placementRequired": "Specify --machine <m> or --storage <s> to say where to list from.",
        "examples": {
          "basic": "List the backups stored for a machine.",
          "byStorage": "List the backups in a specific storage backend."
        },
        "storageRetired": "Listing a storage backend is retired: the rclone cloud arm has been removed. Chunk-store snapshots are listed with `rdc backup manifests`; artifacts on a machine with `rdc backup list -m <machine>`"
      },
      "manifests": {
        "description": "List backup snapshot manifests recorded on the server",
        "fetching": "Fetching backup manifests...",
        "empty": "No backup manifests found."
      },
      "restore": {
        "atNoSnapshot": "No snapshot was taken at or before {{at}}, so there is no point in time to restore.",
        "atResolved": "Resolved {{at}} to snapshot {{snapshot}} (taken {{created}}).",
        "atUnparsable": "--at \"{{at}}\" is neither a snapshot id nor a time. Pass a snapshot id from `rdc backup manifests`, or an RFC3339 time such as 2026-08-14T12:00:00Z.",
        "datastoreUnattached": "Datastore \"{{datastore}}\" is not attached to any machine. Attach it first: rdc datastore attach {{datastore}} --to <machine>.",
        "deployFailed": "Deploy of restored {{name}} failed",
        "deployed": "Deployed {{name}} on {{machine}}",
        "deploying": "Deploying {{name}} on {{machine}}...",
        "description": "Turn a backup artifact into a live repository. Placement is stated here.",
        "examples": {
          "basic": "Restore a backup onto a machine.",
          "asNewRepo": "Restore a backup as a new repository and start it."
        },
        "failed": "Restore of {{name}} failed",
        "optionAs": "Name for the restored repository (defaults to the artifact name)",
        "optionAt": "Restore a point-in-time: a snapshot id or an RFC3339 time (chunk-store restore)",
        "optionDatastore": "Restore into this named datastore (its attached machine hosts it)",
        "optionUp": "Deploy the restored repository after the transfer",
        "placeRequired": "A restore artifact must name where it lives: use repo@place (for example shop:nightly@backup-1).",
        "placementExclusive": "Pass exactly one placement flag: --machine or --datastore.",
        "placementRequired": "Say where to restore: --machine <m> (docker) or --datastore <d> (named datastore).",
        "pulling": "Pulling {{name}} from {{place}}...",
        "restored": "Restored {{name}} onto {{machine}}",
        "restoringSnapshot": "Restoring {{name}} from snapshot {{snapshot}}...",
        "sourceUnknown": "No repository named \"{{name}}\" is known in this config, so its backup artifact cannot be located.",
        "targetExists": "\"{{name}}\" already exists. Restore under another name (--as) or delete the existing repository first.",
        "storageRetired": "Restoring from a storage backend is retired: the rclone cloud arm has been removed. Point-in-time restore is `rdc backup restore <repo> --at <snapshot>` over the chunk store"
      },
      "retention": {
        "clear": {
          "description": "Remove the policy, so every snapshot of this repository is kept."
        },
        "cleared": "Retention policy removed for {{name}}. Every snapshot is kept from now on.",
        "description": "Show the snapshot retention policy the server is enforcing.",
        "empty": "No retention policy is declared, so every snapshot is kept.",
        "knobNotAnInteger": "{{flag}} needs a whole number of snapshots to keep, not \"{{value}}\".",
        "noKnobs": "Give at least one rule, for example --keep-last 7. Setting none would clear the policy, which \"clear\" does explicitly.",
        "optionKeepDaily": "Keep the newest snapshot from each of this many days",
        "optionKeepHourly": "Keep the newest snapshot from each of this many hours",
        "optionKeepLast": "Keep this many of the most recent snapshots",
        "optionKeepMonthly": "Keep the newest snapshot from each of this many months",
        "optionKeepWeekly": "Keep the newest snapshot from each of this many weeks",
        "optionKeepYearly": "Keep the newest snapshot from each of this many years",
        "set": {
          "description": "Declare how many snapshots to keep. Every knob is replaced, not merged.",
          "success": "Retention policy set for {{name}} ({{count}} rule(s)). Snapshots outside it are deleted by the next sweep."
        }
      },
      "run": {
        "description": "Run a backup now.",
        "examples": {
          "basic": "Run a backup strategy now."
        }
      },
      "schedule": {
        "description": "Deploy backup schedule to a remote machine (systemd timers)",
        "optionDryRun": "Preview generated units without deploying",
        "optionForce": "Proceed even if a backup is currently running (new unit applies on next tick; running invocation keeps its old unit)",
        "optionResetFailed": "Clear failed state on touched services after a successful deploy (off by default, preserves failure signal)",
        "success": "Backup schedule deployed to {{machine}}"
      },
      "snapshot": {
        "description": "Upload a chunk-store snapshot: full inventory first, changed cells after",
        "optionReseed": "Distrust the local anchor and upload a full inventory (re-uploads everything and re-charges quota)",
        "optionCold": "Stop the containers, freeze, restart, then upload. Costs a short real outage and gives an application-consistent snapshot; cannot be combined with --dry-run",
        "optionDryRun": "Plan only: no upload. Reports what would move",
        "starting": "Snapshotting {{name}} to chunk storage...",
        "completed": "Snapshot stored for {{name}}",
        "failed": "Snapshot failed for {{name}}",
        "quotaRefused": "Snapshot refused for {{name}}: storage quota exceeded. Prune old snapshots or raise the quota"
      },
      "status": {
        "description": "Show backup status and timer state on a remote machine"
      },
      "strategy": {
        "description": "Manage named backup strategies (hot/cold modes, destinations, scheduling)",
        "set": {
          "description": "Create or update a backup strategy",
          "optionBwlimit": "Rclone bandwidth limit (e.g., \"6M\", \"10M:off\", \"08:00,3M;22:00,10M\")",
          "optionCron": "Cron schedule (e.g., \"0 * * * *\" for hourly)",
          "optionDestination": "Destination name within the strategy. Creates a Rediacc chunk-store destination unless --storage is given",
          "optionDisable": "Disable the strategy or destination",
          "optionEnable": "Enable the strategy or destination",
          "optionExclude": "Exclude these repos from backup (comma-separated names)",
          "optionFolder": "Subfolder under the storage bucket for this destination (e.g. hot, cold). Needs --storage",
          "optionInclude": "Only back up these repos (comma-separated names)",
          "optionMode": "Backup mode: \"hot\" snapshots while everything keeps running, \"cold\" stops the containers for the moment the snapshot is taken",
          "optionStorage": "Use this rclone storage config instead of the chunk store. Legacy: such a destination cannot be scheduled",
          "saved": "Backup strategy configuration saved",
          "storageRequired": "--storage <storage-name> is required for this destination. Without it you get a Rediacc chunk-store destination, which has no storage bucket and therefore no --folder",
          "examples": {
            "configure": "Configure a backup strategy's storage and mode.",
            "disable": "Turn a backup strategy off."
          }
        },
        "bind": {
          "description": "Bind a backup strategy to a machine so its schedule is deployed there",
          "bound": "Backup strategy \"{{strategy}}\" bound to machine \"{{machine}}\"",
          "alreadyBound": "Backup strategy \"{{strategy}}\" is already bound to machine \"{{machine}}\""
        },
        "unbind": {
          "description": "Unbind a backup strategy from a machine",
          "unbound": "Backup strategy \"{{strategy}}\" unbound from machine \"{{machine}}\"",
          "notBound": "Backup strategy \"{{strategy}}\" is not bound to machine \"{{machine}}\""
        },
        "remove": {
          "description": "Remove a backup strategy or destination",
          "optionDestination": "Remove only this destination (keeps other destinations)",
          "removed": "Backup strategy removed"
        },
        "list": {
          "description": "List all backup strategies"
        },
        "show": {
          "description": "Show backup strategy details",
          "destinationsHeader": "  Destinations:",
          "noDestinations": "  Destinations: (none)",
          "notConfigured": "No backup strategies configured",
          "notFound": "Backup strategy \"{{name}}\" not found"
        }
      },
      "usage": {
        "description": "Show backup storage quota, stored bytes, and per-repo usage",
        "fetching": "Fetching backup storage usage...",
        "empty": "No per-repo backup storage usage recorded yet."
      },
      "verify": {
        "description": "Verify a repository's backup anchor against the chunk store",
        "optionDeep": "Re-hash every recorded cell (full verification) instead of a sampled check",
        "starting": "Verifying backup for {{name}} ({{level}})...",
        "completed": "Backup verified for {{name}}",
        "failed": "Backup verification failed for {{name}}"
      }
    },
    "storage": {
      "descriptionShort": "Manage storage",
      "description": "Manage external backup storage endpoints (S3 and other rclone-compatible targets): register and remove them, list what is registered, browse the artifacts they hold, and prune orphaned backups.",
      "list": {
        "description": "List storage endpoints. Give a name for full detail.",
        "noStorages": "No storage endpoints configured. Register one with: rdc storage add <name> --vault <json>",
        "optionReveal": "Reveal the storage configuration in plaintext (interactive TTY only; audited)"
      },
      "browse": {
        "description": "Browse files in a storage system",
        "pathOption": "Subdirectory path to list",
        "listing": "Listing files in \"{{name}}\"...",
        "noFiles": "No files found"
      },
      "prune": {
        "description": "Delete orphaned backups from storage that are no longer in any config. Multi-config safe with grace period protection. The rclone calls run on --machine (the executor), not on your laptop, so clients don't need rclone installed locally; --machine is the executor, not the source of truth.",
        "descriptionShort": "Delete orphaned backups from storage",
        "machineOption": "Executor machine — runs the rclone list/delete calls against the storage. Required because clients aren't expected to have rclone installed locally; storage credentials still come from your local config.",
        "forceDeleteMountedOption": "Override the mount-safety check and delete cloud backups even if the source GUID is currently mounted or has a running container on the executor machine. Distinct from --force (which only overrides the grace period for archived repos).",
        "listing": "Listing backups in \"{{storage}}\"...",
        "noBackups": "No backups found in storage",
        "found": "Found {{count}} backups in storage",
        "completed": "Pruned {{count}} orphaned backups"
      },
      "add": {
        "added": "Registered storage \"{{name}}\"",
        "adding": "Registering storage \"{{name}}\"...",
        "description": "Register a storage endpoint.",
        "examples": {
          "basic": "Register a storage backend from a vault definition."
        }
      },
      "import": {
        "description": "Import a storage endpoint from a definition file.",
        "imported": "Imported storage \"{{name}}\" ({{type}})",
        "noConfigs": "No valid storage configurations found in file",
        "notFound": "Storage \"{{name}}\" not found in config file",
        "optionName": "Import only this named section",
        "summary": "Imported {{count}} storage(s)",
        "unsupported": "Skipping \"{{name}}\": unsupported provider type \"{{type}}\""
      },
      "remove": {
        "confirm": "Remove storage endpoint \"{{name}}\"?",
        "description": "Remove a storage endpoint from the config.",
        "removed": "Removed storage \"{{name}}\""
      }
    },
    "sync": {
      "description": "Transfer files between local and remote repositories via rsync",
      "fetchingDetails": "Fetching connection details...",
      "previewingChanges": "\nPreviewing changes...\n",
      "noChanges": "No changes to sync.",
      "cancelled": "Sync cancelled.",
      "dryRunHeader": "\nDry run - showing what would be transferred:\n",
      "totalSize": "Total size: {{size}}",
      "duration": "Duration: {{seconds}}s",
      "errors": "Errors:",
      "upload": {
        "description": "Upload files to a repository via rsync over SSH (delta transfer). Use --mirror to delete extra remote files, --dry-run to preview, --exclude to skip patterns",
        "descriptionShort": "Upload files to a repository via rsync",
        "starting": "Starting upload...",
        "progress": "Uploading: {{percentage}}% - {{file}} ({{speed}})",
        "completed": "Upload completed: {{count}} files transferred",
        "failed": "Upload failed"
      },
      "download": {
        "description": "Download files from a repository via rsync over SSH (delta transfer). Use --mirror to delete extra local files, --dry-run to preview, --exclude to skip patterns",
        "descriptionShort": "Download files from a repository via rsync",
        "starting": "Starting download...",
        "progress": "Downloading: {{percentage}}% - {{file}} ({{speed}})",
        "completed": "Download completed: {{count}} files transferred",
        "failed": "Download failed"
      },
      "status": {
        "description": "Dry-run comparison of local and remote files (shows what would be transferred without actually transferring)",
        "descriptionShort": "Compare local and remote files (dry-run)"
      }
    },
    "subscription": {
      "descriptionShort": "Manage machine licensing and subscription",
      "description": "Manage licensing and subscription tokens. Sign in with login, inspect with status, reissue licenses with refresh.",
      "login": {
        "description": "Authenticate via browser or API token",
        "validating": "Validating token...",
        "validated": "Token validated",
        "success": "Logged in successfully",
        "plan": "Plan: {{plan}}",
        "machines": "Machines: {{active}}/{{max}}",
        "openBrowser": "Visit this URL to authorize:",
        "waitingApproval": "Waiting for browser authorization...",
        "polling": "Waiting for approval (press Ctrl+C to cancel)...",
        "expired": "Authorization timed out. Please try again.",
        "discoveringRegions": "Discovering available regions...",
        "regionsDiscovered": "Regions loaded",
        "regionSelected": "Data region: {{region}} ({{domain}})",
        "regionPrompt": "Where should your data be stored?",
        "channelSynced": "Update channel set to {{channel}}.",
        "serverChanged": "Server changed. Previous session cleared.",
        "edgeSeparator": "Edge Channel",
        "machinesOverLimit": "  {{count}} machine slot(s) are over the limit."
      },
      "logout": {
        "description": "Clear stored subscription token",
        "success": "Subscription token removed"
      },
      "status": {
        "description": "Show subscription, and license state for a machine.",
        "parseFailed": "  Could not read remote machine status",
        "remote": "\nSubscription:",
        "remotePlan": "  Plan:     {{plan}}",
        "remoteStatus": "  Status:   {{status}}",
        "remoteMachine": "    - {{id}}... (last seen: {{lastSeen}})",
        "serverWouldUse": "Would use account server: {{server}}",
        "remoteMachineActivations": "  Machine slots: {{active}}/{{max}}",
        "remoteRepoLicenseIssuances": "  Monthly repo license issuances: {{used}}/{{limit}}",
        "remoteRepoLicenses": "  Repo licenses: {{totalTrackedRepos}} tracked, {{validCount}} valid, {{refreshRecommendedCount}} refresh recommended, {{hardExpiredCount}} hard expired",
        "issuanceLimitReached": "Monthly repo license issuance limit reached for the current UTC calendar month.",
        "issuanceUsageHigh95": "Monthly repo license issuances are above 95% of the current UTC limit.",
        "issuanceUsageHigh80": "Monthly repo license issuances are above 80% of the current UTC limit.",
        "remoteMachineOverLimit": "    - {{id}}... (last seen: {{lastSeen}}) OVER LIMIT",
        "overLimitSummary": "  {{count}} machine(s) are soft-claimed beyond the machine-slot limit. Renewals still succeed; NEW repositories cannot be licensed until you free a slot or raise the limit."
      },
      "activation": {
        "status": {
          "header": "\nMachine slot ({{machineName}})",
          "unavailable": "  Activation: could not query account state",
          "machineId": "  Machine ID: {{machineId}}",
          "activations": "  Activations: {{active}}/{{max}}",
          "active": "  Activation: active (last seen {{lastSeenAt}})",
          "inactive": "  Activation: machine is not currently active in account state"
        }
      },
      "repo": {
        "status": {
          "header": "\nRepo licenses: ({{machineName}})",
          "empty": "  No repo licenses installed",
          "entry": "  - {{repositoryGuid}}: {{freshness}}{{hardExpirySuffix}}",
          "hardExpirySuffix": " (hard expiry: {{effectiveHardExpiry}})",
          "datastoreSuffix": " [datastore {{datastoreId}}]",
          "blockedBackup": "  ! {{repositoryGuid}}: backups are BLOCKED by licensing since {{at}} ({{reason}}). {{message}}",
          "blockedBackupRemedy": "  Run 'rdc subscription refresh -m {{machineName}}' to reissue; the marker clears on the next successful renewal.",
          "lastRenewal": "    last renewal: {{outcome}}{{detail}}"
        }
      },
      "refresh": {
        "description": "Refresh licenses from the account server.",
        "success": "Repo licenses processed",
        "repos": {
          "refreshing": "Refreshing repo licenses...",
          "refreshed": "Repo licenses refreshed"
        },
        "repo": {
          "refreshing": "Refreshing repo license...",
          "notFound": "Repository not found: {{repoName}}",
          "failed": "Failed to refresh repo license",
          "refreshed": "Repo license refreshed",
          "success": "Repo license refreshed for \"{{repoName}}\""
        },
        "account": {
          "refreshing": "Refreshing account state...",
          "refreshed": "Account state refreshed",
          "failed": "Could not read account state from the account server."
        },
        "targetExclusive": "Pass at most one of -m or --repo. --repo derives its own machine from the ref."
      }
    },
    "shortcuts": {
      "run": {
        "completedLocal": "Completed in {{duration}}ms",
        "description": "WARNING: Low-level command for debugging and experimentation only. Not for production use. Use higher-level commands (repo up, repo down, etc.) instead. Runs a raw bridge function on a machine.",
        "descriptionShort": "Run a raw bridge function on a machine (debugging only)",
        "executingLocal": "Executing '{{function}}' on {{machine}}...",
        "failedLocal": "Failed: {{error}}"
      },
      "trace": {
        "description": "Trace a task (shortcut for: queue trace)"
      },
      "cancel": {
        "description": "Cancel a task (shortcut for: queue cancel)"
      },
      "retry": {
        "description": "Retry a failed task (shortcut for: queue retry)"
      }
    },
    "update": {
      "descriptionShort": "Check for and apply CLI updates",
      "description": "Check for new CLI versions and apply updates. Supports --check-only to check without installing, and --force to update even if already on the latest version.",
      "force": "Force update even if already up-to-date",
      "checkOnly": "Only check for updates without downloading",
      "errors": {
        "notSEA": "Cannot update: not running as a packaged binary. Build with SEA first.",
        "unsupportedPlatform": "This platform is not supported for self-update.",
        "lockFailed": "Another update is already in progress. Try again in a few seconds.",
        "noBinary": "No binary available for this platform in the latest release.",
        "noChecksum": "Release manifest is missing the SHA256 checksum.",
        "checksumMismatch": "Downloaded binary failed checksum verification.",
        "binaryBusy": "Current binary is in use; the update has been staged for the next launch.",
        "unknown": "Unknown error during update."
      },
      "available": "Update available: v{{version}} (current: v{{current}})",
      "releaseNotes": "Release notes: {{url}}",
      "upToDate": "Already up to date (v{{version}})",
      "checking": "Checking for updates...",
      "downloading": "Downloading v{{version}}...",
      "progress": "Downloading... {{percent}}%",
      "success": "Updated from v{{from}} to v{{to}}",
      "failed": "Update failed: {{error}}",
      "autoApplied": "Updated to v{{version}} (was v{{from}}). Changes take effect on next run.",
      "staged": "Update v{{version}} staged (will apply on next launch)",
      "stagedFallback": "Update v{{version}} downloaded and staged. It will be applied on next launch.",
      "rollback": "Rollback to the previous version",
      "rollbackSuccess": "Rolled back successfully. Restart rdc to use the previous version.",
      "rollbackNoBackup": "No previous version found. Rollback is only available after an update.",
      "rollbackFailed": "Rollback failed: {{error}}",
      "statusDescription": "Show auto-update status and diagnostics",
      "channelDescription": "Set release channel (stable or edge)",
      "channelSet": "Release channel set to {{channel}}",
      "configMissing": "Config \"{{name}}\" does not exist. Create it with: rdc config init {{name}}",
      "invalidChannel": "Invalid channel: {{channel}}. Use stable or edge.",
      "npmUpdateHint": "To update, run:\n  {{command}}",
      "rollbackRequiresSEA": "Rollback is only available for SEA binary installations."
    },
    "term": {
      "descriptionShort": "SSH terminal access to machines and repositories",
      "description": "Open an SSH terminal to a machine or a repository. The target is a machine name or a repo ref, and the repository context (DOCKER_HOST, working directory, environment) is set up automatically. Use `-c` to run a single command non-interactively. For container logs or a one-shot command in a container, use `repo logs` and `repo exec`. For VS Code, use `vscode`.",
      "connect": {
        "description": "Open a shell on a machine, or inside a repository with its Docker set up.",
        "examples": {
          "machine": "Open an SSH terminal to a machine.",
          "repo": "Open a terminal inside a repository.",
          "runCommand": "Run a single command on a machine over SSH."
        }
      },
      "connectingTo": "Connecting to {{title}}...",
      "fetchingDetails": "Fetching connection details...",
      "launchSuccess": "Terminal launched successfully.",
      "launchingTerminal": "Launching {{type}} terminal...",
      "testingConnectivity": "Testing connectivity to {{host}}:{{port}}..."
    },
    "vscode": {
      "descriptionShort": "Open VS Code Remote SSH to a machine or repository",
      "description": "Open VS Code with Remote SSH to a machine or repository with sandboxed per-repo environment. Same SSH connection as `term` but launches VS Code instead. Key subcommands: connect (or positional shorthand), list (show configured connections), cleanup (remove SSH config entries), check (verify VS Code installation).",
      "connect": {
        "description": "Open VS Code on a machine or inside a repository.",
        "detecting": "Detecting VS Code...",
        "extensionWarning": "Warning: Remote SSH extension may not be installed.\nInstall from: https://marketplace.visualstudio.com/items?itemName=ms-vscode-remote.remote-ssh\n",
        "fetchingDetails": "Fetching connection details...",
        "testingConnectivity": "Testing connectivity to {{host}}:{{port}}...",
        "persistingKey": "Persisting SSH key...",
        "configuringSSH": "Configuring SSH...",
        "configuringVSCode": "Configuring VS Code settings...",
        "settingsWarning": "Warning: Could not configure VS Code settings: {{error}}",
        "provisioningRenet": "Provisioning renet to remote...",
        "settingUpEnv": "Setting up remote environment...",
        "opening": "Opening VS Code: {{connection}} → {{path}}",
        "success": "VS Code launched successfully.",
        "installingServer": "Ensuring {{provider}} {{version}} is installed on the machine...",
        "startingServer": "Starting {{provider}} inside the repo sandbox...",
        "serverReused": "Reusing the already-running server",
        "tunnelReady": "Tunnel ready on localhost:{{port}}",
        "browserUrl": "Open VS Code in your browser: {{url}}",
        "passwordNote": "Login password: {{password}}",
        "openingBrowser": "Opening your browser...",
        "examples": {
          "basic": "Open a repository in VS Code over SSH.",
          "folder": "Open a specific folder of a repository in VS Code."
        }
      },
      "list": {
        "description": "List configured VS Code SSH connections",
        "noConnections": "No VS Code SSH connections configured.",
        "configFile": "\nConfig file: {{path}}",
        "header": "Configured VS Code SSH connections:\n",
        "keyPersisted": " (key persisted)",
        "total": "\nTotal: {{count}} connection(s)"
      },
      "cleanup": {
        "description": "Remove VS Code SSH configurations",
        "cleanedAll": "Cleaned up {{count}} VS Code SSH connection(s).",
        "removedKeys": "Removed all persisted SSH keys.",
        "cleaned": "Cleaned up: {{connection}}"
      },
      "check": {
        "description": "Check VS Code installation and configuration",
        "title": "VS Code Setup Check\n",
        "installation": "Installation:",
        "vscodeFound": "  VS Code: {{path}}",
        "version": "  Version: {{version}}",
        "variant": "  Variant: Insiders",
        "vscodeNotFound": "  VS Code: Not found",
        "remoteSSH": "  Remote SSH Extension: {{status}}",
        "installed": "Installed",
        "notDetected": "Not detected",
        "configuration": "\nConfiguration:",
        "settingsPath": "  Settings Path: {{path}}",
        "configured": "  Configured: {{status}}",
        "yes": "Yes",
        "no": "No",
        "missingSettings": "  Missing Settings:",
        "sshConfig": "\nSSH Config: {{path}}",
        "activeConnections": "Active Connections: {{count}}"
      },
      "serve": {
        "description": "Manage the in-sandbox browser VS Code server",
        "status": {
          "description": "Show whether the browser VS Code server is running",
          "running": "{{provider}} running (pid {{pid}}, port {{port}})",
          "stopped": "{{provider}} is not running"
        },
        "stop": {
          "description": "Stop the browser VS Code server",
          "stopped": "{{provider}} stopped",
          "notRunning": "{{provider}} was not running"
        }
      }
    },
    "serve": {
      "description": "Run this process as an executor so clients can run commands through it instead of connecting over SSH themselves",
      "optionPort": "Port to listen on",
      "optionHost": "Address to bind",
      "optionMode": "Placement: daemon (customer host) or container",
      "summary": "Run as an executor for remote clients",
      "listening": "Executor listening on http://{{host}}:{{port}} (mode: {{mode}})",
      "hint": "Point a client at it with: rdc --proxy <url> <command>",
      "draining": "Draining in-flight commands before shutdown.",
      "examples": {
        "basic": "Start the local executor server.",
        "customPort": "Start the executor server on a specific port.",
        "container": "Start the executor server in container mode."
      }
    },
    "job": {
      "description": "Manage detached jobs on a machine: list them, check status, replay or follow their event logs, cancel one, or garbage-collect finished ones. A detached job runs under systemd on the machine, so it survives a dropped connection and can be re-attached to later.",
      "summary": "Manage detached jobs on a machine",
      "machineOption": "Target machine name",
      "idOption": "Job ID",
      "list": {
        "description": "List detached jobs on a machine, newest first",
        "empty": "No jobs on {{machine}}.",
        "examples": {
          "basic": "List the jobs on a machine."
        }
      },
      "status": {
        "description": "Show a detached job's status",
        "examples": {
          "basic": "Show the status of one job."
        }
      },
      "logs": {
        "description": "Replay or follow a detached job's event log",
        "followOption": "Stream until the job finishes",
        "sinceLineOption": "Resume after this many lines (the count you already received)",
        "invalidSinceLine": "--since-line must be a line count (got \"{{value}}\")",
        "detached": "Detached from job {{jobId}}. It is still running on {{machine}} and was NOT cancelled. Re-attach with: rdc job logs {{jobId}} -m {{machine}} --follow",
        "examples": {
          "basic": "Show a job's logs.",
          "follow": "Stream a job's logs as they arrive."
        }
      },
      "cancel": {
        "description": "Cancel a running detached job",
        "confirm": "Cancel job {{jobId}} ({{function}}) on {{machine}}? The operation will be stopped.",
        "requested": "Cancel requested for job {{jobId}}.",
        "alreadyFinished": "Job {{jobId}} already {{state}}; nothing to cancel.",
        "examples": {
          "basic": "Cancel a running job."
        }
      },
      "gc": {
        "description": "Remove finished jobs and their logs",
        "olderThanOption": "Collect jobs that finished longer ago than this, in hours (default: 168)",
        "invalidOlderThan": "--older-than must be a number of hours, e.g. 168 or 168h (got \"{{value}}\")",
        "confirm": "Remove jobs on {{machine}} that finished more than {{hours}}h ago? Their logs are deleted.",
        "removed": "Removed {{count}} job(s)."
      },
      "finished": {
        "succeeded": "Job {{jobId}} succeeded.",
        "other": "Job {{jobId}} {{state}}. {{error}}"
      }
    }
  },
  "help": {
    "keyConcepts": "\nKey Concepts:\n\n  Architecture\n    • Each repository = isolated Docker environment (own daemon, loopback IP range, encrypted LUKS storage)\n    • Use renet compose (never raw docker compose) — auto-injects network_mode:host and rediacc.* labels\n    • Storage: bind-mount to ${REDIACC_WORKING_DIR}/... — never Docker named volumes (excluded from forks/backups)\n    • eBPF bind rewriting handles IP isolation: apps bind to 0.0.0.0 and the kernel rewrites to the correct loopback IP\n\n  Routing\n    • Auto-route (fork-friendly): https://{service}.{repo}.{machine}.{baseDomain}\n    • Custom domain: Traefik labels (traefik.enable=true) — not fork-friendly\n    • rediacc.service_port label for non-80 ports. Routes activate ~3s after deploy\n    • HTTPS: automatic Let's Encrypt wildcard certs via Cloudflare DNS-01, which requires --cf-dns-token configured via 'rdc machine infra set'. Without it, HTTP works but HTTPS certs are not provisioned\n    • Grand repos get *.{repo}.{machine}.{baseDomain} — provisioned automatically by Traefik on first repo up\n    • Forks reuse the parent repo's wildcard cert (already provisioned, zero rate limit impact)\n\n  Environment Variables\n    • Rediaccfile shell: ${SVCNAME_IP}, ${REDIACC_WORKING_DIR}, ${REDIACC_NETWORK_ID}\n    • Inside containers: SERVICE_IP, REDIACC_NETWORK_ID (auto-injected by renet)\n\n  Forking\n    • rdc repo fork <parent-ref> --tag <tag>: instant CoW clone with a new GUID, networkId, IP range, auto-route domain\n    • Parent stays running (online forking). Cross-machine: fork locally, then rdc repo push <ref> --to <machine>\n    • Config entry persists after repo delete (the repo may exist on other machines)",
    "agentMode": "\n  Agent Mode\n    • Fork-only: create a fork before making changes to any repository\n    • Grand (non-fork) repositories are read-only for agents\n    • Workflow: repo fork <repo> --tag <tag> -> repo up <repo>:<tag> -> term connect <repo>:<tag>\n    • A repository ref (repo[:tag]) derives its own machine, so -m is not needed\n    • JSON output is automatic: all commands return { success, data, errors, metrics }\n    • Always parse full JSON, never use tail/head/grep on JSON output\n    • Use filter flags (e.g. --repositories, --containers) to reduce response size\n\n",
    "examples": "Examples:",
    "cli": {
      "machineQuery": "Show machine status",
      "termRepo": "SSH into repository",
      "repoUp": "Deploy repository services",
      "syncUpload": "Upload files to repository",
      "syncDownload": "Download files from repository"
    },
    "machine": {
      "query": "Show full machine status",
      "containers": "Show container status only",
      "health": "Health check for CI/CD",
      "keyConcepts": "\nKey Concepts:\n\n  Query Filters\n    • rdc machine status <name> returns all sections by default\n    • Use --system, --repositories, --containers, --services, --network, --block-devices to filter\n    • JSON output enriches containers with: repository (resolved name), domain, autoRoute\n\n  Health & Monitoring\n    • machine health exits with: 0=healthy, 1=warning, 2=error — use for CI/CD gates\n    • machine status <name> --containers --health-check exits 2 if any container is unhealthy\n    • machine status <name> --services --stability-check exits 2 if any service is failed, restarting, or has >3 restarts\n    • machine prune removes orphaned loopback IPs and systemd units from deleted repos\n\n  Datastore Structure (/mnt/rediacc/)\n    • repositories/ — encrypted LUKS images (one per repo GUID)\n    • mounts/ — decrypted mount points (accessible when repo is mounted)\n    • immovable/ — per-repo Docker daemon data\n    • Datastore is a BTRFS pool file on the system disk. Effective free space = min(disk free, datastore free). Expand with: rdc datastore resize\n"
    },
    "repo": {
      "create": "Create encrypted repo",
      "keyConcepts": "\nKey Concepts:\n\n  Compose & Rediaccfile\n    • renet compose -- up -d (never raw docker compose). The -- separator is required\n    • rdc repo exec <ref> [CLI flags] -- <remote cmd>: everything after -- goes to the container verbatim, so put -c/-i/-u/--debug BEFORE --. A --debug written after -- is passed to the remote command and never enables CLI debug output\n    • Rediaccfile must define up() and down() functions. up() should: create dirs, generate configs, start containers\n    • compose ports: are ignored (network_mode:host). Use rediacc.service_port label for external access\n    • restart: always/unless-stopped are auto-stripped by renet and saved to .rediacc.json — the router watchdog auto-recovers stopped services\n    • Each service gets a loopback IP. eBPF bind rewriting handles isolation transparently, so apps can bind to 0.0.0.0\n    • To connect to sibling services use their service name (e.g. \"db\", \"redis\") — renet injects all service names as hostnames. Never embed ${DB_IP} in connection strings stored inside databases — it bakes the IP and breaks fork isolation (validation error)\n    • Health checks can use localhost. Example: [\"CMD\", \"curl\", \"-f\", \"http://localhost:8080/health\"]\n\n  Storage\n    • Bind-mount to ${REDIACC_WORKING_DIR}/data, ${REDIACC_WORKING_DIR}/config, etc.\n    • Example: volumes: ['${REDIACC_WORKING_DIR}/data:/var/lib/postgresql/data']\n    • Docker named volumes are excluded from forks and backups\n    • Config files bind-mounted into containers must be world-readable (644) if the container runs as a non-default UID\n\n  Routing\n    • Grand repo auto-route: https://{service}.{repo}.{machine}.{baseDomain} (wildcard cert *.{repo}.{machine}.{baseDomain} auto-provisioned by Traefik on first repo up)\n    • Fork auto-route: https://{service}-fork-{tag}.{repo}.{machine}.{baseDomain} (uses parent repo's wildcard cert, zero rate limit impact)\n    • Custom domain: traefik.enable=true + traefik.http.routers.{name}.rule=Host(`domain.com`)\n    • TCP/UDP forwarding: rediacc.tcp_ports=5432 / rediacc.udp_ports=53\n    • Custom domains (rediacc.domain label) are ignored on forks — the domain belongs to the grand repo\n\n  Lifecycle\n    • repo delete fully cleans up: storage, Docker daemon, loopback IPs, systemd units\n    • repo down stops containers but keeps Docker daemon and IPs (quick restart with repo up)\n    • depends_on with condition: service_healthy handles startup order but not runtime recovery\n    • If a database stops cleanly (exit 0), dependent services crash-loop. Fix: rdc repo up <repo-ref>\n    • Autostart: rdc repo admin autostart enable <repo-ref> — auto-mounts and starts repo on machine boot\n    • Scheduled backups skip fork repos automatically. Use rdc repo push <repo-ref> --to <remote> for manual fork backup\n    • rdc repo promote <fork-ref> — replace production data with fork data (preserves identity). Revert with: rdc repo promote <backup-fork-ref>\n\n  Naming & targeting\n    • Grand (production) repo: reference it as <repo>, equivalently <repo>:latest (latest is the default tag). A bare ref resolves to the exact config key, else falls back to <repo>:latest.\n    • Fork: always <repo>:<tag> with a non-latest tag (e.g. app:test). up/down/delete/term/sync all take the same <repo>:<tag>.\n    • So `app` targets the GRAND; `app:test` targets that FORK. Always include the tag when acting on a fork — the one ambiguous case is a fork mistakenly registered under the bare/latest name.\n    • Agents may only modify forks; grand repos are protected (override only via REDIACC_ALLOW_GRAND_REPO, which must be set by the operator before the session). Forks are the safe, disposable unit.\n",
      "up": "Deploy with auto-mount",
      "down": "Stop services",
      "fork": "CoW fork for testing"
    },
    "term": {
      "machine": "SSH into machine",
      "repo": "SSH into repo (DOCKER_HOST auto-set)",
      "command": "Run command without interactive shell",
      "cluster": "SSH into cluster control node (KUBECONFIG auto-set, kubectl namespace pinned)",
      "syncHint": "For file transfer, use: rdc repo sync upload/download",
      "fork": "SSH into a fork",
      "collisionHint": "If a name is both a repository and a machine, address the repository as <repo>@<machine>."
    },
    "vscode": {
      "machine": "Open machine in VS Code",
      "repo": "Open repository in VS Code",
      "cluster": "Open cluster control node in VS Code (KUBECONFIG auto-set)",
      "fork": "Open a fork in VS Code"
    },
    "config": {
      "init": "Create named config with SSH key",
      "addMachine": "Add machine to config",
      "setupMachine": "Run setup on remote machine"
    },
    "sync": {
      "upload": "Upload local files to repository",
      "download": "Download repository files locally"
    },
    "useHelpAll": "Use --help-all for extended descriptions"
  },
  "errors": {
    "agent": {
      "grandGuard": "\"{{name}}\" is a grand (production) repository. Agents cannot modify grand repositories directly.\n\nGrand repositories contain production data. Use a fork instead: forks are safe, isolated sandbox copies.\n\nWorkflow:\n  1. rdc repo fork {{name}} --tag <tag-name>\n  2. Operate on the fork (e.g. rdc repo up {{name}}:<tag-name>)\n  3. When done, the user can promote the fork with rdc repo promote {{name}}:<tag-name>\n\nUser: to allow direct access, set REDIACC_ALLOW_GRAND_REPO={{name}} (or * for all repos) in your terminal before starting the agent session.",
      "grandGuardOverride": "\"{{name}}\" is a grand (production) repository. Agent-initiated overrides are not accepted.\n\nDo not attempt to set REDIACC_ALLOW_GRAND_REPO — only the user can authorize this before the agent starts.\n\nUse a fork instead: forks are safe, isolated sandbox copies.\n  1. rdc repo fork {{name}} --tag <tag-name>\n  2. Operate on the fork\n  3. When done, the user can promote the fork with rdc repo promote {{name}}:<tag-name>",
      "mcpGrandGuard": "\"{{name}}\" is a grand (production) repository. Agents cannot modify grand repositories directly.\n\nUse repo_fork to create a safe, isolated sandbox copy, then operate on the fork.\nIf you need direct access, ask the user to export REDIACC_ALLOW_GRAND_REPO before starting the agent.",
      "mcpGrandGuardOverride": "\"{{name}}\" is a grand (production) repository. Agent-initiated overrides are not accepted.\n\nDo not attempt to set REDIACC_ALLOW_GRAND_REPO.\n\nUse repo_fork to create a safe, isolated sandbox copy, then operate on the fork.",
      "machineGuard": "Direct machine access to \"{{machine}}\" is not allowed in agent mode.\n\nAgents must connect through a repository context for sandboxed access. Fork a repository first, then connect to the fork.\n\nWorkflow:\n  1. rdc repo fork <repo> --tag <tag-name>\n  2. rdc term connect <repo>:<tag-name> -c \"<command>\"\n\nUser: machine-level access needs the wildcard — set REDIACC_ALLOW_GRAND_REPO=* in your terminal before starting the agent session. A specific repo name does not unlock direct machine access.",
      "machineGuardOverride": "Direct machine access to \"{{machine}}\" is not allowed. Agent-initiated overrides are not accepted.\n\nDo not attempt to set REDIACC_ALLOW_GRAND_REPO — only the user can authorize this before the agent starts.\n\nFork a repository first, then connect to the fork:\n  1. rdc repo fork <repo> --tag <tag-name>\n  2. rdc term connect <repo>:<tag-name> -c \"<command>\"",
      "forkBlocked": "Command \"{{command}}\" is not available on fork repositories.\n\nFork repos are interim sandbox environments. This operation only applies to grand (production) repositories. Ask the user to run this command directly.",
      "createGuard": "Cannot create a new grand repository \"{{name}}\" in agent mode.\n\nAgents operate in fork-only mode. Grand repositories are production environments that agents cannot access after creation.\n\nInstead, fork an existing repository:\n  rdc repo fork <parent-ref> --tag {{name}}\n\nUser: to allow repository creation, set REDIACC_ALLOW_GRAND_REPO=* (or a specific repo name) in your terminal before starting the agent session.",
      "createGuardOverride": "Cannot create a new grand repository \"{{name}}\". Agent-initiated overrides are not accepted.\n\nDo not attempt to set REDIACC_ALLOW_GRAND_REPO — only the user can authorize this before the agent starts.\n\nInstead, fork an existing repository:\n  rdc repo fork <parent-ref> --tag {{name}}",
      "grandGuardOverrideNonLinux": "\"{{name}}\" is a grand (production) repository. REDIACC_ALLOW_GRAND_REPO is set, but its origin could not be verified on {{platform}} (the ancestry verification helper is unavailable or failed), so the override is not accepted.\n\nUse the fork-first workflow:\n  1. rdc repo fork {{name}} --tag <tag-name>\n  2. Operate on the fork\n  3. When done, the user can promote the fork with rdc repo promote {{name}}:<tag-name>\n\nUser: verification works on Linux, macOS, and Windows with an intact rdc installation. Reinstall rdc if this persists, and set the variable in your terminal before starting the agent.",
      "machineGuardOverrideNonLinux": "Direct machine access to \"{{machine}}\" is overridden, but the override's origin could not be verified on {{platform}} (the ancestry verification helper is unavailable or failed), so the override is not accepted.\n\nFork a repository first, then connect to the fork:\n  1. rdc repo fork <repo> --tag <tag-name>\n  2. rdc term connect <repo>:<tag-name> -c \"<command>\"\n\nUser: verification works on Linux, macOS, and Windows with an intact rdc installation. Reinstall rdc if this persists, and set the variable in your terminal before starting the agent.",
      "createGuardOverrideNonLinux": "Cannot create a new grand repository \"{{name}}\". REDIACC_ALLOW_GRAND_REPO is set, but its origin could not be verified on {{platform}} (the ancestry verification helper is unavailable or failed).\n\nInstead, fork an existing repository:\n  rdc repo fork <parent-ref> --tag {{name}}\n\nUser: verification works on Linux, macOS, and Windows with an intact rdc installation. Reinstall rdc if this persists, and set the variable in your terminal before starting the agent.",
      "mcpGrandGuardOverrideNonLinux": "\"{{name}}\" is a grand (production) repository. The REDIACC_ALLOW_GRAND_REPO override could not be verified on {{platform}} (the ancestry verification helper is unavailable or failed) and is not accepted.\n\nUse repo_fork to create a safe, isolated sandbox copy, then operate on the fork.",
      "commandBlocked": "The \"{{command}}\" command is not available in agent mode.\n\nThis command executes arbitrary low-level functions without safety guards. Use typed CLI commands or MCP tools instead.\n\nAlternatives:\n  - MCP tools: repo_up, repo_down, repo_fork, repo_logs, repo_exec, machine_status\n  - CLI commands: rdc repo up, rdc repo down, rdc term connect\n  - Run \"rdc --help\" to see all available commands\n\nIf you need raw access, ask the user to run the command directly.",
      "clusterOpBlocked": "Cluster command \"{{command}}\" is blocked in agent mode. Cluster operations provision or tear down real infrastructure, so an agent cannot authorize them itself. User: to allow this, set REDIACC_ALLOW_CLUSTER_OPS=<cluster-name> (or * for all clusters) in your terminal BEFORE starting the agent session — an override set from inside the agent is not accepted.",
      "configEdit": "Interactive editor is blocked in agent environments.\n\nAlternatives:\n  • rdc config edit --dump > config.jsonc    (safe redacted inspection)\n  • rdc config field set --pointer <pointer> --current <old> --new <new>\n  • rdc config edit --apply config.jsonc\n\nTo override, set REDIACC_ALLOW_CONFIG_EDIT=<scope-glob> in the parent shell\nBEFORE starting your agent (it must appear in the agent's process ancestry).",
      "configEditAncestry": "Interactive editor is blocked in agent environments (REDIACC_ALLOW_CONFIG_EDIT was set but ancestry verification failed — the override must be set by your shell, not by an agent).\n\nAlternatives:\n  • rdc config edit --dump > config.jsonc    (safe redacted inspection)\n  • rdc config field set --pointer <pointer> --current <old> --new <new>\n  • rdc config edit --apply config.jsonc\n\nSet REDIACC_ALLOW_CONFIG_EDIT=<scope-glob> in the parent shell BEFORE starting your agent.",
      "fieldReveal": "--reveal for sensitive field {{pointer}} is blocked in agent environments. Agents should use --digest. A real user at an interactive terminal can --reveal.",
      "showReveal": "--reveal is blocked in agent environments. Use `rdc config show` (redacted) or `rdc config field get --pointer <pointer> --digest` for a safe fingerprint.",
      "revealRequiresTty": "--reveal requires an interactive TTY; refusing to print sensitive value to a pipe.",
      "showRevealRequiresTty": "--reveal requires an interactive TTY; refusing to pipe plaintext to a non-TTY sink.",
      "rotateRequiresTty": "config field rotate requires interactive TTY confirmation; refusing in non-interactive context (set REDIACC_ALLOW_CONFIG_EDIT to override).",
      "rotateNotSensitive": "Pointer \"{{pointer}}\" is not a sensitive field; use 'config field set' instead."
    },
    "authRequired": "Authentication required. Run: rdc subscription login",
    "cluster": {
      "targetExclusive": "Specify only one of -m/--machine or --cluster, not both",
      "targetRequired": "A target machine (-m) or cluster (--cluster) is required",
      "countInvalid": "--count must be a non-negative integer",
      "dockerOnlyVerb": "\"repo {{verb}}\" targets Docker repositories only and cannot run against a --cluster; it has no Kubernetes equivalent in v1",
      "featureUnsupportedV1": "{{feature}} is not supported with --cluster in v1 (control-node view); {{hint}}",
      "controlDsBackendInvalid": "--control-ds-backend must be local or ceph",
      "forkWritesInvalid": "--writes must be local or ceph",
      "poolRequired": "A cluster needs at least one node pool. Add --pool <spec>.",
      "declareNeedsProvider": "--declare-only records a cluster without provisioning it, so it needs --provider and --pool."
    },
    "config": {
      "storageNotFound": "Storage \"{{name}}\" not found in config. Available: {{available}}.",
      "machineNotFound": "Machine \"{{name}}\" not found in config. Available: {{available}}.",
      "invalidEmail": "Invalid email address: \"{{value}}\".",
      "noActiveConfig": "No active config",
      "pointerNotFound": "Pointer \"{{pointer}}\" not found",
      "retiredKey": "The default \"{{key}}\" no longer exists. Valid keys: {{keys}}"
    },
    "container": {
      "execFailed": "container_exec failed",
      "logsFailed": "container_logs failed"
    },
    "datastore": {
      "defaultIsImplicit": "the default datastore is implicit; these verbs manage additional named datastores.",
      "deleteHasRepos": "Datastore \"{{ref}}\" still holds repositories: {{repos}}. Delete them first, or pass --force to destroy them with it.",
      "forkNeedsWrites": "\"{{ref}}\" is a fork. Say where its writes go: --writes local (instant, ephemeral, lost on detach) or --writes ceph (durable clone in the pool).",
      "localForkNeedsDiscard": "\"{{ref}}\" is attached with --writes local, so its overlay is ephemeral and detaching destroys it. Pass --discard to say that is what you want.",
      "localForkUnsupported": "\"{{name}}\" is a local datastore, and there is no block-level clone for it. Repositories inside a local datastore fork one at a time by reflink (rdc repo fork); datastore-level fork needs the rbd backend.",
      "writesImmutable": "\"{{ref}}\" is already attached here with a different writes disposition. Writes are set at attach time: detach it first.",
      "writesOnNonFork": "\"{{ref}}\" is not a fork, so it has nowhere else to write. Drop --writes."
    },
    "editor": {
      "notFound": "No editor configured. Set $GIT_EDITOR, git config core.editor, $VISUAL, or $EDITOR, or pass --editor.",
      "headlessRefused": "Headless editor invocation would block forever: {{command}}. Pass --editor explicitly with interactive flags.",
      "spawnFailed": "Failed to launch editor {{command}}: {{reason}}",
      "nonzeroExit": "Editor {{command}} exited with code {{code}}"
    },
    "invalidFunctionParams": "Invalid parameters for function \"{{function}}\": {{errors}}",
    "invalidKey": "Invalid key. Valid keys: {{keys}}",
    "license": {
      "recoveryFailedTokenNotReady": "Your subscription token is not ready. Run: rdc subscription login, then retry.",
      "recoveryFailedNoKnownRepos": "Repositories were found on {{machine}} but none are tracked in your local config. List your tracked repos with `rdc repo list`, then retry.",
      "recoveryFailedServerRejected": "The account server rejected license issuance for the repository on \"{{machine}}\". First error: {{error}}. Run: rdc subscription refresh --repo <repo-ref> for details.",
      "preflightTokenNotReady": "Subscription token is not ready. Run: rdc subscription login. Deploy to {{machine}} skipped.",
      "skipActivationHint": "On a throwaway machine you can skip activation entirely with REDIACC_SKIP_MACHINE_ACTIVATION=1.",
      "machineSlotLimit": "This placement needs {{needed}} more machine slot(s), and the subscription has {{free}} free ({{active}} of {{max}} in use). A slot is released automatically 5 hours after that machine's last activity, or immediately when the machine is removed. Raise the ceiling by upgrading the plan, or contact sales for a negotiated Enterprise or partner machine ceiling.",
      "partialPlacement": "The placement stopped part-way: {{placed}} machine(s) completed ({{placedNames}}) and {{remaining}} were never started ({{remainingNames}}). Nothing was rolled back, so the completed machines keep their repositories and their slots. Free a slot or raise the ceiling, then re-run: {{command}}",
      "nothingProvisioned": "Nothing was provisioned: the repository was not created."
    },
    "machineNotFound": "Machine \"{{name}}\" not found",
    "machineRequired": "Machine name required. Use --machine <name>",
    "machineRequiredLocal": "Machine name required. Use --machine <name>",
    "noActiveConfig": "No active config. Create one with: rdc config init <name>",
    "permissionDenied": "Permission denied",
    "precondition": {
      "next": {
        "summary": "Provide the current value or acknowledge rotation.",
        "options": {
          "confirm": {
            "description": "Re-read current digest, then retry with --current",
            "run": "rdc repo secret get {{repository}} --key {{key}}"
          },
          "rotate": {
            "description": "Skip the precondition (rotation, audited)"
          }
        }
      }
    },
    "regionRequired": "Region name required. Use --region or set default with: rdc config set region <name>",
    "regionSelectionRequiresTTY": "Region selection requires interactive input. Use --server <url> to specify a server directly.",
    "remoteNotFound": "\"{{name}}\" is not a known machine, storage, or cluster. Machines: {{machines}}. Storages: {{storages}}. Clusters: {{clusters}}",
    "repo": {
      "secret": {
        "emptyValue": "Secret value cannot be empty (stdin produced no bytes).",
        "badMode": "Invalid --mode \"{{mode}}\". Allowed: env, file.",
        "mutuallyExclusive": "--current and --rotate-secret are mutually exclusive. Pick one: --current to verify the prior value, or --rotate-secret to rotate without verification."
      },
      "replicateNeedsCluster": "Repository \"{{ref}}\" is not in a cluster. Replicas are forked datastores placed across a cluster's nodes, so replicate needs a kubernetes-placed repository.",
      "canaryNeedsCluster": "Repository \"{{ref}}\" is not in a cluster. A canary splits traffic between two deployments, so it needs a kubernetes-placed repository.",
      "listTargetExclusive": "Pass at most one of -m or --datastore. A datastore already knows which machine holds it."
    },
    "repoNotDeployed": "Repository \"{{repository}}\" is not deployed on {{machine}}.\n\nDeploy it first:\n  rdc repo up {{repository}}",
    "repositoryAlreadyMounted": "Repository \"{{name}}\" is already mounted on \"{{machine}}\". Migrating over a live repo would corrupt it. Run first: rdc repo down {{name}} --unmount",
    "repositoryNotFound": "Repository \"{{name}}\" not found",
    "ssh": {
      "noIpAddress": "Machine '{{machine}}' does not have an IP address configured",
      "noPrivateKey": "Team '{{team}}' does not have an SSH private key configured",
      "noHostKey": "Machine '{{machine}}' does not have a host key configured in the vault"
    },
    "subscription": {
      "notLoggedIn": "Not logged in. Humans should run `rdc subscription login`. Automation should run `rdc subscription login --token <token>` or set `REDIACC_SUBSCRIPTION_TOKEN`.",
      "tokenRequired": "Subscription token required. Either set REDIACC_SUBSCRIPTION_TOKEN env var, or run `rdc subscription login` in your terminal.",
      "activationFailed": "License issuance failed. Ensure your subscription is active and a machine slot is available.",
      "tokenWarning": "No subscription token found. License-required commands (repo create, repo fork) will fail. Set REDIACC_SUBSCRIPTION_TOKEN env var or run `rdc subscription login` in your terminal."
    },
    "sync": {
      "localPathNotFound": "Local path does not exist: {{path}}",
      "absolutePathBlocked": "The --remote path must be relative, not absolute. Use a path relative to the repository root.",
      "pathTraversalBlocked": "The --remote path must not contain \"..\" path traversal. Use a path within the repository.",
      "mirrorWithFile": "Cannot use --mirror when a source is a file: it would delete sibling files in the destination directory.",
      "remoteAndRemoteFile": "Cannot use --remote and --remote-file together. Pick one.",
      "remoteFileLocalNotDir": "When --remote-file is set, --local must be an existing directory: {{path}}",
      "remoteFileRequiresSingleFile": "--remote-file requires exactly one --local path that points at a file."
    },
    "teamRequired": "Team name required. Use --team or set default with: rdc config set team <name>",
    "term": {
      "connectivityFailed": "Cannot reach {{host}}:{{port}}. {{error}}\n\nTroubleshooting:\n  - Check if the machine is powered on and connected to the network\n  - Verify the IP address is correct in the machine vault\n  - Ensure port {{port}} is not blocked by a firewall\n  - Try pinging the machine: ping {{host}}",
      "launchFailed": "Failed to launch terminal: {{error}}",
      "noHostKey": "Machine '{{machine}}' does not have a host key configured in the vault",
      "noIpAddress": "Machine '{{machine}}' does not have an IP address configured",
      "noPrivateKey": "Team '{{team}}' does not have an SSH private key configured",
      "sshCode1": "Generic error - check command syntax or remote permissions",
      "sshCode126": "Permission denied - cannot execute command on remote",
      "sshCode127": "Command not found on remote machine",
      "sshCode130": "Connection terminated by user (Ctrl+C)",
      "sshCode255": "SSH connection failed - possible causes:\n    - Authentication failed (check SSH key permissions)\n    - Host key verification failed\n    - Network connection dropped\n    - Remote SSH service not running",
      "repoContextRequired": "Command requires repository context.\n\nDetected \"{{detected}}\" which needs DOCKER_HOST and repository environment variables.\nThese are only available when the target is a repository.\n\nInstead of:\n  rdc term connect {{machine}} -c \"{{command}}\"\n\nUse:\n  rdc term connect <repo-ref> -c \"{{command}}\"",
      "dockerComposeForbidden": "\"docker compose\" is not permitted in repository context.\n\nUse \"renet compose -- <args>\" instead so per-repo loopback IPs, host network injection, CRIU capabilities, and compose validation are applied. Running \"docker compose\" directly bypasses all of this and silently corrupts the deployment.\n\nFor orchestrated deploys, prefer: rdc repo up <repo-ref>",
      "fileWriteDetected": "File write detected ({{detected}}). Use rdc repo sync instead:\n  rdc repo sync upload <repo-ref> --local FILE --remote PATH\n\nrsync transfers files with delta compression, checksums, and proper permissions.",
      "sshExitCode": "SSH exited with code {{code}}"
    },
    "unknownCommand": "Unknown command \"{{command}}\". Available commands: {{available}}.",
    "unknownRegion": "Unknown region: {{region}}",
    "unknownSubcommand": "Unknown subcommand \"{{command}}\". Available {{parent}} subcommands: {{available}}.",
    "vscode": {
      "cleanupRequired": "Specify --all to remove all connections or --connection <name> for a specific one.",
      "connectivityFailed": "Cannot reach {{host}}:{{port}}. {{error}}\n\nTroubleshooting:\n  - Check if the machine is powered on and connected to the network\n  - Verify the IP address is correct in the machine vault\n  - Ensure port {{port}} is not blocked by a firewall\n  - Try pinging the machine: ping {{host}}",
      "notFound": "VS Code not found.\n\nPlease install from: https://code.visualstudio.com/\nOr set REDIACC_VSCODE_PATH environment variable.",
      "browserNeedsRepo": "vscode --browser serves a repository sandbox, so its target must be a repository ref: rdc vscode connect <repo-ref> --browser",
      "unsupportedArch": "Unsupported machine architecture for browser VS Code: {{arch}}",
      "serverInstallFailed": "Failed to install {{provider}}: {{error}}",
      "serverStartFailed": "Failed to start {{provider}}: {{error}}",
      "repoRequired": "This VS Code server needs a repository target. Pass a repository ref."
    }
  },
  "options": {
    "apiToken": "API token (rdt_...)",
    "artifactRef": "Backup artifact reference (repo[:tag][@place])",
    "background": "Run as a detached job on the machine: print its id and exit",
    "cleanupAll": "Remove all rediacc SSH configurations",
    "clusterName": "Cluster name",
    "command": "Execute a command instead of interactive shell",
    "config": "Use specific config for this command",
    "configKey": "Configuration key",
    "configValue": "Configuration value",
    "confirmSync": "Preview changes and ask for confirmation before syncing",
    "connectTarget": "Target: a machine or cluster name for a shell on it, or a repository ref (name, name:tag, optionally @machine) for a shell inside it",
    "connectionName": "Remove specific connection",
    "datastoreRef": "Datastore ref: name, or name:tag for a fork (for example ds-data or ds-data:exp)",
    "debug": "Enable debug output",
    "dryRun": "Show what would be done without making changes",
    "excludePatterns": "Patterns to exclude from sync",
    "external": "Force launching in external terminal window",
    "extraMachine": "Extra machine for multi-machine ops (name:ip:user)",
    "fields": "Comma-separated list of fields to include in output",
    "file": "Path to file",
    "folder": "Remote folder path to open",
    "force": "Skip confirmation prompts",
    "function": "Function name to execute",
    "graceDays": "Grace period in days for recently archived repos (default: 7)",
    "healthTimeout": "Per-attempt health-check timeout in seconds (default 30)",
    "healthWindow": "Total health-gate window in seconds (default 300)",
    "insiders": "Use VS Code Insiders settings",
    "lang": "Language override ({{languages}})",
    "licenses": "Print the full THIRD_PARTY_LICENSES text bundled with release builds",
    "localPath": "Local directory path (default: current directory)",
    "localPaths": "One or more local file or directory paths (default: current directory)",
    "machine": "Machine name",
    "machineIp": "Machine IP address or hostname",
    "machineName": "Machine name",
    "mirrorDownload": "Mirror mode - delete local files not present on remote",
    "mirrorUpload": "Mirror mode - delete remote files not present locally",
    "name": "Resource name",
    "newName": "New resource name",
    "newWindow": "Open in new VS Code window",
    "opsBackend": "Virtualization backend (kvm|qemu, auto-detected)",
    "opsBasic": "Minimal cluster (bridge + 1 worker)",
    "opsForce": "Force restart all VMs",
    "opsLite": "Skip VM provisioning (status only)",
    "opsOS": "VM operating system (e.g., ubuntu-24.04, debian-13)",
    "opsParallel": "Create VMs in parallel",
    "opsSSHUser": "SSH username for VM connection",
    "opsSkipOrchestration": "Skip cluster orchestration",
    "output": "Output format (table|json|yaml|csv). Override default with REDIACC_DEFAULT_OUTPUT.",
    "outputFormat": "Output format (json)",
    "param": "Function parameters",
    "proxy": "Run machine operations through an executor at this URL instead of connecting directly",
    "queryBlockDevices": "Include block devices only",
    "queryContainers": "Include containers only",
    "queryDatastores": "Show attached datastores (mount, attach, usage)",
    "queryLicenses": "Include repository license statuses",
    "queryNetwork": "Include network interfaces only",
    "queryRepositories": "Include repositories only",
    "queryServices": "Include services only",
    "queryStorageHealth": "Show BTRFS reflink savings and (informational) image fragmentation per repository",
    "queryStrict": "Exit non-zero (code 2) if any container has crossed the health-drift threshold",
    "querySyncCerts": "Also pull the ACME cert cache from the machine after querying",
    "querySystem": "Include system info only",
    "quiet": "Suppress informational output",
    "remoteFile": "Remote file path within repository (alternative to --remote for single-file transfers)",
    "remoteFileUpload": "Single remote file path (relative to repo root) to upload to. Mutually exclusive with --remote.",
    "remotePath": "Remote subdirectory path within repository",
    "renetPath": "Path to renet binary (default: renet in PATH)",
    "repoRef": "Repository ref: name, or name:tag, optionally with @machine (for example shop or shop:test)",
    "resetHome": "Reset per-repo home overlay for a fresh start",
    "searchInField": "Search in {{field}}",
    "searchRepos": "Filter repositories by name",
    "serverUrl": "Account server URL",
    "skipEnvSetup": "Skip remote environment setup",
    "skipRouterRestart": "Skip restarting the route server after binary update",
    "sortByField": "Sort by field",
    "sortDesc": "Sort in descending order",
    "sortDescending": "Sort in descending order",
    "sshKey": "Path to SSH private key (e.g., ~/.ssh/id_rsa)",
    "sshPort": "SSH port",
    "sshUser": "SSH username",
    "storageRef": "Storage ref: the name of a registered storage endpoint (for example s3-main)",
    "strategyName": "Backup strategy name",
    "team": "Team name",
    "urlOnly": "Print the VS Code URI instead of launching",
    "vaultContent": "Vault content as JSON string",
    "vaultJson": "Vault content as JSON string",
    "verifyChecksum": "Verify files using checksums after sync",
    "vmId": "Virtual machine ID",
    "vscodeBrowser": "Serve browser VS Code from inside the repo sandbox (no local VS Code needed)",
    "vscodeNoOpen": "Print the URL without launching the local browser",
    "vscodeServerArchive": "Pre-staged server tarball path on the machine (airgapped installs)",
    "vscodeServerProvider": "Browser VS Code server implementation (openvscode, code-server)",
    "yes": "Skip confirmation prompt"
  },
  "prompts": {
    "cancelled": "Cancelled",
    "syncConfirm": "\nProceed with sync? [y/N/d(etails)] ",
    "syncConfirmHelp": "Please enter y (yes), n (no), or d (details)",
    "masterPassword": "Enter your master password:"
  },
  "status": {
    "cancelled": "Cancelled"
  },
  "docs": {
    "frontmatter": {
      "title": "CLI Application",
      "description": "Complete guide to using the Rediacc command-line interface for platform management",
      "category": "Reference"
    },
    "pageTitle": "Rediacc CLI Reference",
    "overview": {
      "heading": "Overview",
      "text": "The **Rediacc CLI** (`rdc`) is a command-line interface for managing self-hosted infrastructure — machines, repositories, storage, backups, and more."
    },
    "installation": {
      "heading": "Installation",
      "text": "Download the latest binary for your platform from the [Rediacc releases page](https://www.rediacc.com) or install via your package manager:"
    },
    "globalOptions": {
      "heading": "Global Options",
      "intro": "Every command supports these global flags:"
    },
    "tableHeaders": {
      "flag": "Flag",
      "description": "Description",
      "required": "Required",
      "default": "Default",
      "error": "Error",
      "meaning": "Meaning"
    },
    "optionLabels": {
      "yes": "Yes",
      "no": "No"
    },
    "admonitions": {
      "tip": "Tip",
      "warning": "Warning",
      "note": "Note"
    },
    "sectionTitles": {
      "config": "Config Management",
      "machine": "Machine Management",
      "cluster": "Cluster Management",
      "repo": "Repository Lifecycle",
      "storage": "Storage Management",
      "vscode": "VS Code Integration",
      "term": "SSH Terminal Access",
      "shortcuts": "Quick Commands (Shortcuts)",
      "update": "CLI Updates",
      "credits": "Third-Party Credits",
      "doctor": "System Diagnostics",
      "ops": "Local VM Operations",
      "subscription": "Subscription Management",
      "mcp": "MCP Server",
      "datastore": "Datastore Management",
      "sync": "File Sync",
      "backup": "Backup & Restore",
      "job": "Detached Jobs",
      "serve": "Executor Service"
    },
    "errors": {
      "heading": "Common Error Messages",
      "intro": "When something goes wrong, the CLI provides clear error messages:",
      "meanings": {
        "authRequired": "You need to log in first",
        "noActiveConfig": "No config is active — create one with `rdc config init`",
        "permissionDenied": "Your account lacks the required permissions",
        "machineRequired": "Specify a machine with `--machine` or set a config default",
        "teamRequired": "Specify a team with `--team` or set a config default",
        "regionRequired": "Specify a region with `--region` or set a config default"
      }
    },
    "outputFormats": {
      "heading": "Output Formats",
      "text": "All list/get commands support multiple output formats via `--output`:",
      "closing": "This makes it easy to integrate Rediacc CLI into scripts, CI/CD pipelines, and automation workflows."
    },
    "supplements": {
      "config": {
        "afterDescription": "Config files store connection details (API URL, credentials, default team/region/bridge) so you can switch between environments quickly.",
        "init": {
          "afterDescription": "You can add machines directly:"
        }
      },
      "machine": {
        "health": {
          "afterDescription": "The health command outputs system metrics, container status, service stability, and storage information — useful for CI/CD pipeline checks."
        }
      }
    }
  },
  "services": {
    "prune": {
      "unreadableConfigs": "Cannot read {{count}} config(s): {{names}} (encrypted or corrupted)",
      "refusingPrune": "Refusing to prune — these configs may reference protected GUIDs. Use --force to override.",
      "noOrphaned": "No orphaned entries found"
    }
  }
}

{
  "_comment": [
    "Gates whose failure is KNOWN, JUSTIFIED and deliberately carried past the",
    "push guard. This is not a way to push green CI -- CI still runs every gate",
    "and still fails on anything listed here. It only lets the push HAPPEN, with",
    "the failure named on the record instead of routed around silently.",
    "",
    "block-unverified-push.sh enforces three things, and the last two are what",
    "stop this becoming a permanent allowlist:",
    "  1. every FAILING gate must appear here, or the push is still refused;",
    "  2. every gate here must actually be failing -- a stale entry REFUSES,",
    "     so an excuse cannot outlive the failure it excuses;",
    "  3. the reason must be substantive, the same bar .dead-bash-allowlist",
    "     applies and gate-test:dead-bash proves with a low-effort-BLOCKER case.",
    "",
    "A narrowed run (--only/--skip) is refused before any of this is consulted,",
    "so carrying cannot become a second way to launder a partial lane."
  ],
  "carried": [
    {
      "gate": "check:ci-gate-manifest",
      "reason": "A LOCAL MEASUREMENT ARTIFACT, and the gate's own comment describes this exact failure mode: 'a full run that overlapped two other sessions on 2026-09-02 pushed a 4.5s gate's average to 21s and this oracle demanded it be marked slow'. The three gates it names here (check:ci-guard-mention-anchoring 24.8s, check:ci-changed-selection 141.6s, gate-test:policy-liveness-floors 29.2s) were timed on a machine that spent today running a 22-worker gate battery and a 15919-test pytest suite, so the numbers are inflated by my own load. The oracle reads .ci/cache/gate-durations.json, which .gitignore:143 excludes, so CI's checkout does not have this file and the read sits inside a try/except -- these findings cannot reproduce there. Marking three gates `slow: true` on a contended measurement would change CI lane composition on bad data, which is the one thing the gate's comment warns against. Carried so CI can measure it honestly and say so."
    },
    {
      "gate": "check:ci-rubric-calibration",
      "reason": "door:no-write-access. All four rubric prompts live in the contested .claude/hooks/stop/ family (active concurrent peer rewrite all session). A live calibration run this session found 2 real SHAPE_PROMPT behavioral misses, not a stale hash. Investigated, ready-to-run brief on item b8a40aa3."
    },
    {
      "gate": "check:ci-python-lint",
      "reason": "Formatting drift in bws_env.py/test_core_bws_env.py, part of the bws-rotation work under active concurrent peer edit all session (git status MM throughout). Not this session's file, no fix applied."
    },
    {
      "gate": "check:ci-plan-implementation",
      "reason": "307 P-A2 findings across 21 plans this session never authored, a side effect of this session's own plan-boxes.json ledger regen. Triaged and deferred via worklist item d4132cbf, DEFAULT no mass investigation, window still open."
    },
    {
      "gate": "check:ci-plan-citations",
      "reason": "356 remaining, down from 964 after 614 mechanical repoints (b89a25b46) plus a real gate-defect fix (d231c8c30). The remainder is genuine per-plan judgment, categorized in PLAN-fix-plan-citation-drift.md's own new section, not force-closed."
    },
    {
      "gate": "check:ci-plan-record",
      "reason": "5 remaining, down from 161 (132 a real gate defect fixed in d231c8c30). The rest is hand-edited Record-Sig mismatches and unfilled placeholders needing a human read of the prose, not a mechanical fix."
    },
    {
      "gate": "check:ci-tracked-credentials",
      "reason": "New finding in test_wl_migrate.py, part of the worklist-hook v10 upgrade under active concurrent peer rewrite all session. Broadcast via worklist.py --ask (df74b3fa), no live session answered, window open (item e2eaf098)."
    },
    {
      "gate": "check:ci-worklist-path-resolution",
      "reason": "Finding in onboard.py, same worklist-hook v10 family under active concurrent peer rewrite all session, same broadcast (df74b3fa) as check:ci-tracked-credentials. Not this session's file."
    },
    {
      "gate": "check:ci-worklist-event-builders",
      "reason": "Finding in test-backlog.py, same worklist-hook v10 family under active concurrent peer rewrite all session, same broadcast (df74b3fa) as check:ci-tracked-credentials. Not this session's file."
    }
  ]
}

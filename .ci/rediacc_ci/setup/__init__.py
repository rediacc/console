"""Everything `./run.sh setup` needs to know about a host, as data.

ONE MODULE TODAY, `tools`, and the package exists rather than a bare `setup_tools.py` because W6's remaining phases add siblings to it: the `run.sh` router split and the cross-OS launcher both land here. A package that starts as one file is a rename nobody has to do later.
"""

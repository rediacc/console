"""`./run.sh dev`, the marketing-site development server, as a ported verb.

ONE MODULE TODAY, `www`, and a package rather than a bare `dev_www.py` for the reason `rediacc_ci/setup/__init__.py` gives for its own: the legacy dispatcher still holds nine verbs, and the ones whose bodies are orchestration rather than a delegation to `.ci/lib/*.sh` land beside this one. A package that starts as one file is a rename nobody has to do later.
"""

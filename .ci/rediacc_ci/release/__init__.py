"""Python ports of `.ci/scripts/release/*.sh`, box W7P5-a.

Sibling of `rediacc_ci.deploy`; see that package's docstring for why the
subpackage exists, why there is no `---- gate ----` header, and why the
workflow cutover is a separate, later box (W7P4-W). `release/` held 21 bash
scripts and zero Python before this box.
"""

__all__: list[str] = []

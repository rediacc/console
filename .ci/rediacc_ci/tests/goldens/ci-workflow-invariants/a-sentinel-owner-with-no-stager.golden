# twin .ci/scripts/security/check-ci-workflow-invariants.sh blob 23c86d2113491f7e0d9ac4adaada5b9d885f21d8
exit: 1
--- stdout ---
--- stderr ---
✗ INVARIANT-FAIL: skip-release: expected job '<no job uses cd-stage.yml>' is absent from <work>/wf.yml, so this invariant checked nothing. Retarget it deliberately rather than letting it pass over a renamed job.
✗ ci workflow invariants FAILED

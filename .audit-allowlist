# Security audit allowlist for dev-only dependencies.
# Enforced by .ci/scripts/security/audit.sh (two-pass strategy):
#
#   Pass 1 — production deps (--omit=dev): ZERO TOLERANCE, no allowlist.
#            Any severity → hard fail.
#   Pass 2 — dev deps: this allowlist applies.
#
# Rules:
#   - NEVER add production dependency advisories here; they cannot be allowlisted.
#   - Only add dev-dependency advisories after confirming no upgrade path exists.
#   - Each group MUST have a "# BLOCKER: <reason>" line; the audit script
#     enforces this strictly. Blank lines separate groups.

# tar — electron-builder dependency chain
# GHSA-r6q2-hw4h-h46w, GHSA-34x7-hfp2-rc4v, GHSA-8qq5-rm4j-mr97,
# GHSA-83g3-92jg-28cx, GHSA-qffp-2rhf-9h96
# BLOCKER: electron-builder transitive; tar@8+ has breaking ESM changes; fix requires electron-builder major migration
1112329
1112659
1113300
1113375
1113992

# minimatch ReDoS — transitive across eslint/electron-builder chains
# GHSA-7r86-cg39-jmmj (matchOne GLOBSTAR), GHSA-23c5-xmqv-rm74 (nested *() extglobs)
# BLOCKER: transitive dep; fix requires eslint v10 major / electron-builder major upgrade
1113545
1113553

# yauzl — electron → extract-zip → yauzl
# GHSA-gmq8-994r-jv83 (off-by-one)
# BLOCKER: electron transitive; no upstream fix published
1114530

# brace-expansion zero-step hang + related superseded advisories
# GHSA-f886-m6hf-6m8v and sibling advisories sharing the same root cause
# BLOCKER: transitive via eslint/electron chains; fix requires eslint v10 major upgrade
1115432
1115496
1115497
1115499
1115540
1115541
1115543

# tmp Path Traversal — electron-builder build chain
# GHSA-ph9p-34f9-6g65 (app-builder-lib > @malept/flatpak-bundler > tmp-promise > tmp)
# 1120654 is a sibling re-issue of the same advisory under a new source ID (GitHub
# Advisory Database split); same chain, same root cause, same rationale.
# BLOCKER: electron-builder transitive; patched tmp@0.2.6 published <24h ago and is blocked by .npmrc minimum-release-age=1440; prefix/postfix inputs come from trusted desktop build tooling, not untrusted data
1119610
1120654
